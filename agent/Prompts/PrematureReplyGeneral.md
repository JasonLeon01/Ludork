The task is not complete yet.

Do **not** use type `"reply"` while you still need to search, read files, or modify the blueprint.

- Use `readfile` with paths like `"Source/NodeFunctions/Scene.py"`.
- Or use `fetchfile` with up to 4 keywords such as `"ShowMessage, TriggerEventBus, SetAutoPathToDestination"`.

Do **not** ask the user to provide file contents.
