The task is not complete yet.

Do **not** respond with text only while you still need to search, read files, or modify the blueprint.

- Use `read_file` with paths like `"Source/NodeFunctions/Scene.py"`.
- Or use `search_project` with up to 4 keywords such as `"ShowMessage, TriggerEventBus, SetAutoPathToDestination"`.

Do **not** ask the user to provide file contents.
