-- Generated by ScriptTools ui-assets generate. Do not edit.
local UiView = require("Source.UIBase.UiView")

---@class Source.UI.ParticleShowcase
local View = {}

View.assetKey = "ParticleShowcase"
View.designSize = sf.Vector2f.new(640, 480)

function View:init(instance)
    UiView.init(self, instance)
    local controls = {
        ResidentAura = assert(self.instance:requireControl("ResidentAura")),
        Root = assert(self.instance:requireControl("Root")),
        Smoke = assert(self.instance:requireControl("Smoke")),
        Sparks = assert(self.instance:requireControl("Sparks")),
        UiSparkle = assert(self.instance:requireControl("UiSparkle"))
    }
    ---@cast controls Source.UIBase.GeneratedControls.ParticleShowcase
    self.controls = controls
    local assets = {}
    ---@cast assets Source.UIBase.GeneratedAssets.ParticleShowcase
    self.assets = assets
end

return class(View, UiView)
