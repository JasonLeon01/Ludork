---@meta Global.Utils.Logging

local Logging = {}

function Logging.setLevel(level) end

function Logging.debug(formatValue, ...) end

function Logging.info(formatValue, ...) end

function Logging.warning(formatValue, ...) end

function Logging.error(formatValue, ...) end

return Logging
