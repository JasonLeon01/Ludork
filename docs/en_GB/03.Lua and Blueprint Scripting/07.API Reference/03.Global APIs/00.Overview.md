# Global APIs

Global APIs are project Lua modules that expose editor-visible members outside the Source class hierarchy. The current Sample provides the `Global.GameMap` API documented in this directory.

Use the complete module/type reference shown by metadata. Do not assume a similarly named Core module has the same ownership or lifecycle.

