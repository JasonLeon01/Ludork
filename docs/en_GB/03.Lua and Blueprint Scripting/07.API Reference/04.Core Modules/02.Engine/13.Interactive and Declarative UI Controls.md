# Engine: Interactive and Declarative UI Controls

## CheckBox

Direct metadata bases: `{ "Engine", "ControlBase" }`, `{ "Engine", "FunctionalBase" }`

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `isChecked` | `function` | — | return: bool | Pure |
| `setChecked` | `function` | checked: bool | — | — |
| `toggle` | `function` | — | — | — |
| `setWindowSkin` | `function` | windowSkin: sf.Image | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `setOnCheckedChanged` | `function` | callback: function | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onConfirm` | `function` | arguments: any | — | — |
| `onClick` | `function` | arguments: any | — | — |
| `onMouseButtonDown` | `function` | arguments: any | return: bool | — |
| `onKeyDown` | `function` | arguments: any | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## DropBox

Direct metadata bases: `{ "Engine", "ControlBase" }`, `{ "Engine", "FunctionalBase" }`

### Properties

No editable properties are declared.

### Overlay and measurement

When a DropBox has a Canvas ancestor, its expanded popup is drawn in the outermost ancestor Canvas overlay layer, after that Canvas's ordinary content and animations. It therefore escapes nested Canvas clipping and does not resize or reposition controls in its parent layout.

The popup's visible height is limited to the intersection of the root Canvas content rectangle and the game screen. It opens downwards when the complete list fits, otherwise upwards when the complete list fits there; if neither side fits, it uses the side with more space and scrolls its items internally.

`getSize()` always returns the collapsed layout size. `getLocalBounds()` returns that rectangle while collapsed and its union with the visible popup while expanded; an upward popup can make the local Y position negative. The inherited absolute bounds and default touch hit bounds follow this visual extent.

### Popup scrolling and selection

An ordinary mouse wheel scrolls towards a row-sized target; a precise wheel and mobile touch dragging apply their offsets directly. These inputs move only the popup viewport, not the cursor or committed selection, and releasing a drag never confirms an item. Directional keys move the cursor and reveal it as needed. Mouse hover previews a visible item, a desktop click confirms it, and mobile taps use the two-stage confirmation described below. All indices still address the complete `items` array.

### Mobile touch confirmation

On mobile, an expanded DropBox uses two-stage confirmation for captured touch input. Tapping an item other than the cursor-highlighted item moves the highlight and plays the configured cursor sound only: it does not change `selectedIndex`, fire either selection callback, apply the item, play the select sound or collapse the DropBox. Tapping the highlighted item, including the item already highlighted when the DropBox opened, confirms it.

Cancelling after previewing another item keeps the committed `selectedIndex` and discards the preview.

### Selection callbacks

`setOnSelectedIndexChanged(callback)` fires with the new index only when `setSelectedIndex` resolves to a different selection; during confirmation it runs before the confirmation callback. `setOnSelectionConfirmed(callback)` fires with the current index when an expanded, non-empty DropBox confirms, even if unchanged. Programmatic selection or item replacement, cancellation and empty confirmation do not fire it; pass `nil` to either setter to clear it.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `setWindowSkin` | `function` | windowSkin: sf.Image; repeated: bool = false | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `getItems` | `function` | — | return: string[] | Pure |
| `setItems` | `function` | items: string[] | — | — |
| `getSelectedIndex` | `function` | — | return: int | Pure |
| `setSelectedIndex` | `function` | index: int | — | — |
| `getSelectedItem` | `function` | — | return: string | Pure |
| `isExpanded` | `function` | — | return: bool | Pure |
| `setExpanded` | `function` | expanded: bool | — | — |
| `open` | `function` | — | — | — |
| `cancel` | `function` | — | — | — |
| `setOnSelectedIndexChanged` | `function` | callback: function | — | — |
| `setOnSelectionConfirmed` | `function` | callback: function | — | — |
| `setOnExpandedChanged` | `function` | callback: function | — | — |
| `setOpenSound` | `function` | filename: string | — | — |
| `getOpenSound` | `function` | — | return: string | Pure |
| `setCursorSound` | `function` | filename: string | — | — |
| `getCursorSound` | `function` | — | return: string | Pure |
| `setSelectSound` | `function` | filename: string | — | — |
| `getSelectSound` | `function` | — | return: string | Pure |
| `setCancelSound` | `function` | filename: string | — | — |
| `getCancelSound` | `function` | — | return: string | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onConfirm` | `function` | arguments: any | — | — |
| `onCancel` | `function` | arguments: any | — | — |
| `onClick` | `function` | arguments: any | — | — |
| `onMouseButtonDown` | `function` | arguments: any | return: bool | — |
| `onMouseMoved` | `function` | arguments: any | — | — |
| `onMouseWheelScrolled` | `function` | arguments: any | — | — |
| `onKeyDown` | `function` | arguments: any | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## TabView

Direct metadata bases: `{ "Engine", "ControlBase" }`, `{ "Engine", "FunctionalBase" }`

### Selection and navigation

A TabView has a fixed, non-zero item count, zero-based clamped indices and an always-visible selection. The middle area contains equal tab slots; each side reserves 16 logical pixels for hints. The control never takes keyboard focus.

`setItems(items, callback)` atomically replaces same-count labels and the callback; a wrong count changes neither, and `nil` clears it. A real `setSelectedIndex` change calls the callback without sound. `selectPrevious`, `selectNext` and real Q/E, LB/RB, mouse-move or touch-tap changes play the configured cursor sound once, then call the callback. Unchanged and boundary selections do neither, and tabs do not wrap.

Focused content calls `handleNavigationInput()`. It consumes Q/E/LB/RB while the TabView is visible and active; duplicate same-direction inputs move once and opposing inputs cancel. Mouse movement selects central slots. A left click there is consumed but does not select; right/middle clicks and both hint gutters pass through. Touch movement does not select; a tap does.

### Key hints

`setKeyHint(leftHint, rightHint)` atomically sets both visual hints. Only exact `Keyboard` and `Handle` keys are valid; their values must be a non-Unknown `sf.Keyboard.Key` and a canonical `Engine.JoystickButton`. Empty maps clear a side. A missing current-device entry hides it; invalid input errors without changing either side.

```lua
tabView:setKeyHint(
    { Keyboard = sf.Keyboard.Key.Q, Handle = Engine.JoystickButton.LB },
    { Keyboard = sf.Keyboard.Key.E, Handle = Engine.JoystickButton.RB }
)
```

Hints appear only where movement is possible. A connected joystick uses Handle; otherwise desktop, HarmonyOS 2in1 and HarmonyOS Mobile tablets use Keyboard, while Android, iOS and HarmonyOS phones hide hints. Hints never rebind or disable input.

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `setWindowSkin` | `function` | windowSkin: sf.Image | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `getItems` | `function` | — | return: string[] | Pure |
| `setItems` | `function` | items: string[]; callback: function \| nil | — | — |
| `getSelectedIndex` | `function` | — | return: int | Pure |
| `setSelectedIndex` | `function` | index: int | — | — |
| `getSelectedItem` | `function` | — | return: string | Pure |
| `selectPrevious` | `function` | — | return: bool | — |
| `selectNext` | `function` | — | return: bool | — |
| `handleNavigationInput` | `function` | — | return: bool | — |
| `setCursorSound` | `function` | filename: string | — | — |
| `getCursorSound` | `function` | — | return: string | Pure |
| `setKeyHint` | `function` | leftHint: any; rightHint: any | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onClick` | `function` | arguments: any | — | — |
| `onMouseButtonDown` | `function` | arguments: any | return: bool | — |
| `onMouseMoved` | `function` | arguments: any | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## ProgressBar

Direct metadata bases: `{ "Engine", "ControlBase" }`

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `getProgress` | `function` | — | return: float | Pure |
| `setProgress` | `function` | progress: float | — | — |
| `getBackgroundColor` | `function` | — | return: sf.Color | Pure |
| `setBackgroundColor` | `function` | color: sf.Color | — | — |
| `getFillColor` | `function` | — | return: sf.Color | Pure |
| `setFillColor` | `function` | color: sf.Color | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## Slider

Direct metadata bases: `{ "Engine", "ControlBase" }`, `{ "Engine", "FunctionalBase" }`

### Touch interaction

On touch capture, a Slider sets its value from the pointer's X position against its visual bounds, then continues updating and clamping until release even outside the hit bounds. `setTouchHitBounds` changes only the touch target, not visual or mouse bounds.

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `setVisible` | `function` | visible: bool | — | — |
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `getValue` | `function` | — | return: int | Pure |
| `setValue` | `function` | value: int | — | — |
| `setRange` | `function` | minValue: int; maxValue: int | — | — |
| `getRange` | `function` | — | minimum: int; maximum: int | Pure |
| `setValueFromRatio` | `function` | ratio: float | — | — |
| `setValueFromBoundsPosition` | `function` | bounds: sf.FloatRect; position: sf.Vector2f | — | — |
| `adjust` | `function` | delta: int | — | — |
| `getHandlePosition` | `function` | — | return: int | Pure |
| `setLineTexture` | `function` | texture: sf.Texture | — | — |
| `setHandleTexture` | `function` | texture: sf.Texture | — | — |
| `setOnValueChanged` | `function` | callback: function | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onClick` | `function` | arguments: any | — | — |
| `onMouseButtonDown` | `function` | arguments: any | return: bool | — |
| `onMouseMoved` | `function` | arguments: any | — | — |
| `onKeyDown` | `function` | arguments: any | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## AssetInstance

Direct metadata bases: —

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getRoot` | `function` | — | return: { "Engine", "ControlBase" } | Pure |
| `requireControl` | `function` | localName: string | return: { "Engine", "ControlBase" } | Pure |
| `getNodeByName` | `function` | name: string | return: { "Engine", "ControlBase" } | Pure |
| `requireAsset` | `function` | localName: string | return: { "Engine", "AssetInstance" } | Pure |
| `setProperty` | `function` | localName: string; propertyId: string; value: any | — | — |
| `setText` | `function` | localName: string; text: string | — | — |
| `reflow` | `function` | logicalSize: sf.Vector2u = nil | — | — |
