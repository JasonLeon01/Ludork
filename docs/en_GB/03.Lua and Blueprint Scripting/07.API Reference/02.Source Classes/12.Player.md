# Source.Player

Metadata source: `Scripts/Source/Player_meta.lua`

Direct metadata bases: `{ "Engine", "Character" }`, `{ "Source.Battler", "Battler" }`

Meta: `{ GeneralDataVars = { { "ID", "Player" } } }`

The editor shows a read-only generated `PlayerAttributeSet` preview; it is not serialised into Blueprint JSON.

## Properties

| Name | Type | Default | Metadata |
|---|---|---|---|
| `ID` | `string` | `"FILL_IT_BY_YOURSELF"` | GeneralDataVars = `Player` |
| `tickable` | `bool` | `true` | — |
| `collisionEnabled` | `bool` | `true` | — |
| `animatable` | `bool` | `true` | — |
| `speed` | `float` | `96` | — |

`attributes` is the generated Player Attribute Set; inventory, equipment, movement and the Ability System are runtime state.

## Blueprint functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `onFixedTick` | event | `fixedDelta: float` | — | default execution output |
| `addItem` | function | `itemID: string; count: int = 1` | — | Item selector |
| `removeItem` | function | `itemID: string; count: int = 1` | bool | success/failed split; Item selector |
| `getItemCount` | function | `itemID: string` | `count: int` | Pure; Item selector |
| `hasItem` | function | `itemID: string` | `value: bool` | Pure; Item selector |
| `addEquip` | function | `equipID: string; count: int = 1` | — | Equip selector |
| `removeEquip` | function | `equipID: string; count: int = 1` | bool | success/failed split; Equip selector |
| `equip` | function | `equipID: string` | — | Equip selector |
| `unequip` | function | `slotID: string` | — | default execution output |
| `getEquipCount` | function | `equipID: string` | `count: int` | Pure; Equip selector |
| `hasEquip` | function | `equipID: string` | `value: bool` | Pure; Equip selector |
| `getEquipInfo` | function | `slotID: string` | `value: string` | Pure |
| `getForbiddenMoving` | function | — | `value: bool` | Pure |
| `setForbiddenMoving` | function | `value: bool` | — | default execution output |

## Lua runtime API

| Member | Signature | Behaviour |
|---|---|---|
| `init` | `(texture?, tag?)` | Constructs the Attribute Set, Ability System and empty inventory/equipment state |
| `InitPlayer` | `(playerPath, applyInitialEquipment? = true) -> Player` | Constructs a Player from a Blueprint class path and optionally applies Class initial equipment |
| `FromDict` | `(data) -> Player` | Restores Bases, inventory abilities, equipment Effects and State Effects without applying Class initial equipment or executing member graphs |
| `asDict` | `() -> SaveData` | Serialises stable state, excluding runtime Effect handles |
| `activateItem` | `(itemID) -> GameplayAbilityResult` | Activates `GeneralData.Item.<id>.onUse` |
| `consumeMovementSpecialPath` | `() -> sf.Vector2i[]` | Takes and clears arrived map cells |
| `getClassPath` / `setClassPath` | `() -> string` / `(path)` | Accesses the reconstructable Blueprint path |

`InitPlayer` applies Class initial equipment; `FromDict` reconstructs saved equipment without replaying `onEquip`. Runtime equip/unequip changes the Infinite Effect before running the corresponding graph. Movement completion sends `Event.Movement.Step`.

HP Current is constrained to `0..MAXHP`. Permanent MAXHP and LEVEL growth uses Base deltas; Effect and Constraint changes never alter progression Bases.

## Related pages

- [Runtime Data and Saves](<../../06.Sample Gameplay/06.Runtime Data Configuration and Saves.md>)
- [Global Gameplay API](../03.Global APIs/02.Gameplay.md)
