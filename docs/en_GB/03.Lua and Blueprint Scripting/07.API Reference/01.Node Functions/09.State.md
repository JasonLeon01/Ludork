# NodeFunctions.State

Metadata source: `Scripts/Source/NodeFunctions/State_meta.lua`

| Types | Properties | Functions and events |
|---:|---:|---:|
| 1 | 0 | 11 |

## State

Direct metadata bases: —

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `GetStateOwner` | `function` | — | battler: { "Source.Battler", "Battler" } | Pure |
| `GetEventArg` | `function` | name: string; default: any | value: any | Pure |
| `GetBattlerAttr` | `function` | battler: { "Source.Battler", "Battler" }; attrName: string; default: any | value: any | Pure |
| `SetBattlerAttr` | `function` | battler: { "Source.Battler", "Battler" }; attrName: string; value: any | — | ExecSplit { [1] = "default", default = nil } |
| `DamageBattler` | `function` | battler: { "Source.Battler", "Battler" }; amount: int = 1 | — | ExecSplit { [1] = "default", default = nil } |
| `HealBattler` | `function` | battler: { "Source.Battler", "Battler" }; amount: int = 1 | — | ExecSplit { [1] = "default", default = nil } |
| `BattlerHasState` | `function` | battler: { "Source.Battler", "Battler" }; stateID: string | value: bool | Pure; Meta { GeneralDataVars = { [1] = { [1] = "stateID", [2] = "State" } } } |
| `AddStateTo` | `function` | battler: { "Source.Battler", "Battler" }; stateID: string; stacks: int = 1 | — | ExecSplit { [1] = "default", default = nil }; Meta { GeneralDataVars = { [1] = { [1] = "stateID", [2] = "State" } } } |
| `RemoveStateFrom` | `function` | battler: { "Source.Battler", "Battler" }; stateID: string | — | ExecSplit { [1] = "default", default = nil }; Meta { GeneralDataVars = { [1] = { [1] = "stateID", [2] = "State" } } } |
| `ReduceStateFrom` | `function` | battler: { "Source.Battler", "Battler" }; stateID: string; stacks: int = 1 | — | ExecSplit { [1] = "default", default = nil }; Meta { GeneralDataVars = { [1] = { [1] = "stateID", [2] = "State" } } } |
| `TriggerStateHook` | `function` | battler: { "Source.Battler", "Battler" }; stateID: string | — | ExecSplit { [1] = "default", default = nil }; Meta { GeneralDataVars = { [1] = { [1] = "stateID", [2] = "State" } } } |
