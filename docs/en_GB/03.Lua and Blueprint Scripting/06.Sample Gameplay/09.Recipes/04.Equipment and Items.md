# Equipment and Items

## Goal

Create item and equipment records with stable inventory IDs, valid slots and focused member-graph behaviour.

## Prerequisites

- Item, Equip and Class General Data with the required inventory and slot identifiers.
- Blueprint parents derived from `Source.Item` or `Source.Equip`.

## Steps

Item and Equip schemas separate reusable definitions from inventory instances.

- Use Item `usable` and `cost` to control menu availability and consumption.
- Put item-specific use/get flow in `onUse` and `onGet`.
- Match Equip `slot` to the selected Class slot dictionary.
- Store ordinary stat changes in `attrPlus`; reserve `onEquip` and `onUnequip` for special effects.
- Keep icons under `Assets/Icons` and use the resource selector.

## Sample equipment

`BP_Equip_Sword_A.json` derives from `Source.Equip` and sets `ID = "Sword_A"`. The ID resolves the Equip member, whose `slot` must be a key present in the active Class member's `slot` dictionary. Put ordinary additions in `attrPlus`; leave `onEquip`/`onUnequip` empty unless the item needs behaviour beyond those additions.

## Sample item

`BP_Item_Key_Y.json` derives from `Source.Item` and sets `ID = "KEY_Y"`, matching both the Item member and the yellow door's `needKeyID`. For an active item, set `usable` and `cost` deliberately, then implement `onUse`; use `onGet` for behaviour triggered when the runtime awards it.

Test buying, selling, obtaining, using, obtaining callbacks, equipping, replacing, unequipping, door consumption and restoring inventory/equipment from a save. Also test a missing icon, unknown ID, invalid slot and renamed member so reference failures are visible.

## Expected result

Inventory instances resolve stable records, equipment applies only valid slots and stat changes, and member graphs run only their record-specific behaviour.

## Verification

Execute every inventory, equipment and failure case listed above, including save restoration and packaged asset lookup.

## Limitations

Renaming a member does not repair stored references automatically. A Class slot mismatch and an unknown record ID remain explicit data errors.

## Related pages

- [Item General Data Reference](<../../../02.Editor User Guide/09.General Data Reference/04.Item.md>)
- [Equip General Data Reference](<../../../02.Editor User Guide/09.General Data Reference/03.Equip.md>)
