# Doors

## Goal

Create a door that opens through an inventory key or a named game-state condition.

## Prerequisites

- A valid `Source.DoorBase.DoorBase` Blueprint parent and door sprite sheet.
- Matching Item or game-variable identifiers for the selected workflow.

## Steps

A door Blueprint combines collision behaviour, opening animation/state and an optional key or condition. Map-to-map travel is a Teleporter concern, not a Door field.

### Key door

1. Derive a general door from `Source.DoorBase.DoorBase` and configure its texture, rectangle, collision and opening defaults.
2. Derive a colour-specific door. The Sample's `BP_Door_Y.json` derives from `BP_Door_General` and stores `needKeyID = "KEY_Y"`.
3. In `onCollision`, reject an already-opening door and require `MeetPlayer`.
4. Read `needKeyID`, call `GetItemCount`, and compare it with one.
5. On success, call the inherited collision flow through `SUPER`, then remove one key. Leave the failure branch disconnected or connect explicit feedback.

The key ID must match an Item member and its pickup Blueprint, such as `KEY_Y` and `BP_Item_Key_Y.json`.

### Conditional door

`BP_Door_Condition.json` stores `openConditionName` and `openConditionVal`. Its graphs initialise the variable, pass normal tick behaviour to `SUPER`, compare the game variable, ensure the opening runs once, and invoke `openDoor`. It unregisters its event-bus registration during destruction.

Test no key, one key, repeated collision while opening, missing/renamed key, condition below/equal/above threshold, saved variable restoration and map reload. Inspect references before renaming IDs.

## Expected result

The door opens once when its key or condition succeeds, consumes only the configured key where applicable, and preserves lifecycle cleanup.

## Verification

Execute every case listed above in the editor and in a packaged build, including map reload and restored game variables.

## Limitations

Door logic does not perform scene travel. Use Teleporter for map-to-map movement, and update every stored reference when renaming a key or condition identifier.

## Related pages

- [Script Mixin Authoring Workflow](<../../03.Script Mixins/01.Authoring Workflow.md>)
- [Teleporter API](<../../07.API Reference/02.Source Classes/14.Teleporter.md>)
