# Engine: Runtime Values and Functional Controls

## RuntimeValue

Direct metadata bases: —

`RuntimeValue` is registered under `Engine` for dynamic native and Lua values, and Lua `any` values keep their ordinary representations. C++ ownership and conversion rules are in [Core headers and Runtime boundary](<../../../../04.Native C++ Development/01.Build and Module Layout.md#core-headers-and-runtime-boundary>).

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `isNil` | `function` | self: { "Engine", "RuntimeValue" } = "self" | return: bool | Pure |
| `typeName` | `function` | self: { "Engine", "RuntimeValue" } = "self" | return: string | Pure |

## Button

Direct metadata bases: `{ "Engine", "Image" }`, `{ "Engine", "FunctionalBase" }`

A declarative Button with an empty `texture` uses the current window skin's selection-Rect image. While a gamepad is connected, that default background is hidden; size, global bounds, hit area and separate text controls stay unchanged. A custom texture stays visible, and disconnecting the gamepad restores the default background.

`gamepadButton` and `gamepadLongPress` add a gamepad binding. JSON stores a logical name such as `"X"`; `""` means unbound and long press defaults to `false`. Lua uses `button:setGamepadButton(Engine.JoystickButton.getX())` and `button:setGamepadLongPress(true)`; pass `nil` to clear the binding.

With a gamepad connected, the bound key is a 16 logical-pixel circle inside the button's right edge, vertically centred and independent of background stretching. It shares the white background, black key glyph and green clockwise long-press progress with [`GamepadHintBar`](<13.Interactive and Declarative UI Controls.md#gamepadhintbar>). A short press invokes `onConfirm` / `addConfirmCallback` once; a long press invokes it once at one second and needs a release before another trigger. The button need not have focus, but it and its owning window must be visible and interactive. Disabling or hiding either, deactivating the window or disconnecting the gamepad clears progress. A disabled button's visible hint is grey.

### Properties

Declarative properties include `gamepadButton` (empty by default) and `gamepadLongPress` (`false` by default), alongside the existing texture and colour settings.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `setVisible` | `function` | self: { "Engine", "Button" } = "self"; visible: bool | — | — |
| `setColour` | `function` | self: { "Engine", "Button" } = "self"; colour: sf.Color | — | — |
| `getColour` | `function` | self: { "Engine", "Button" } = "self" | return: sf.Color | Pure |
| `setHoverColour` | `function` | self: { "Engine", "Button" } = "self"; colour: sf.Color | — | — |
| `getHoverColour` | `function` | self: { "Engine", "Button" } = "self" | return: sf.Color | Pure |
| `setPressedColour` | `function` | self: { "Engine", "Button" } = "self"; colour: sf.Color | — | — |
| `getPressedColour` | `function` | self: { "Engine", "Button" } = "self" | return: sf.Color | Pure |
| `setGamepadButton` | `function` | self: { "Engine", "Button" } = "self"; button: { "Engine", "InputNamedValue" } \| nil | — | — |
| `getGamepadButton` | `function` | self: { "Engine", "Button" } = "self" | return: { "Engine", "InputNamedValue" } \| nil | Pure |
| `setGamepadLongPress` | `function` | self: { "Engine", "Button" } = "self"; longPress: bool | — | — |
| `getGamepadLongPress` | `function` | self: { "Engine", "Button" } = "self" | return: bool | Pure |

## Canvas

Direct metadata bases: `{ "Engine", "SpriteBase" }`, `{ "Engine", "FunctionalBase" }`

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getOrigin` | `function` | self: { "Engine", "Canvas" } = "self" | return: sf.Vector2f | Pure |
| `setOrigin` | `function` | self: { "Engine", "Canvas" } = "self"; origin: sf.Vector2f | — | — |
| `getSize` | `function` | self: { "Engine", "Canvas" } = "self" | return: sf.Vector2u | Pure |
| `resize` | `function` | self: { "Engine", "Canvas" } = "self"; size: sf.Vector2u | — | — |
| `getNoTranslationRect` | `function` | self: { "Engine", "Canvas" } = "self" | return: sf.IntRect | Pure |
| `getContentRect` | `function` | self: { "Engine", "Canvas" } = "self" | return: sf.IntRect | Pure |
| `getView` | `function` | self: { "Engine", "Canvas" } = "self" | return: sf.View | Pure |
| `getDefaultView` | `function` | self: { "Engine", "Canvas" } = "self" | return: sf.View | Pure |
| `setView` | `function` | self: { "Engine", "Canvas" } = "self"; view: sf.View | — | — |
| `getChildren` | `function` | self: { "Engine", "Canvas" } = "self" | return: { "Engine", "ControlBase[]" } | Pure |
| `addChild` | `function` | self: { "Engine", "Canvas" } = "self"; child: { "Engine", "ControlBase" } | — | — |
| `removeChild` | `function` | self: { "Engine", "Canvas" } = "self"; child: { "Engine", "ControlBase" } | — | — |
| `addAnim` | `function` | self: { "Engine", "Canvas" } = "self"; animation: { "Engine", "AnimSprite" } | — | — |
| `removeAnim` | `function` | self: { "Engine", "Canvas" } = "self"; animation: { "Engine", "AnimSprite" } | — | — |
| `clearAnims` | `function` | self: { "Engine", "Canvas" } = "self" | — | — |
| `getAnims` | `function` | self: { "Engine", "Canvas" } = "self" | return: { "Engine", "AnimSprite[]" } | Pure |
| `setZOrder` | `function` | self: { "Engine", "Canvas" } = "self"; zOrder: int | — | — |
| `getZOrder` | `function` | self: { "Engine", "Canvas" } = "self" | return: int | Pure |
| `update` | `function` | self: { "Engine", "Canvas" } = "self"; deltaTime: float | — | — |
| `render` | `function` | self: { "Engine", "Canvas" } = "self" | — | — |
| `lateUpdate` | `function` | self: { "Engine", "Canvas" } = "self"; deltaTime: float | — | — |
| `fixedUpdate` | `function` | self: { "Engine", "Canvas" } = "self"; fixedDelta: float | — | — |

## ControlBase

Direct metadata bases: —

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `getVisible` | `function` | self: { "Engine", "ControlBase" } = "self" | return: bool | Pure |
| `setVisible` | `function` | self: { "Engine", "ControlBase" } = "self"; visible: bool | — | — |
| `getName` | `function` | self: { "Engine", "ControlBase" } = "self" | return: string | Pure |
| `setName` | `function` | self: { "Engine", "ControlBase" } = "self"; name: string | — | — |
| `getParent` | `function` | self: { "Engine", "ControlBase" } = "self" | return: { "Engine", "ControlBase" } | Pure |
| `setParent` | `function` | self: { "Engine", "ControlBase" } = "self"; parent: { "Engine", "ControlBase" } | — | — |
| `getChildren` | `function` | self: { "Engine", "ControlBase" } = "self" | return: { "Engine", "ControlBase[]" } | Pure |
| `getSize` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Vector2f | Pure |
| `getLocalBounds` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.FloatRect | Pure |
| `getAbsoluteBounds` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.FloatRect | Pure |
| `getRenderStates` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.RenderStates | Pure |
| `setPosition` | `function` | self: { "Engine", "ControlBase" } = "self"; position: sf.Vector2f | — | — |
| `getPosition` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Vector2f | Pure |
| `move` | `function` | self: { "Engine", "ControlBase" } = "self"; offset: sf.Vector2f | — | — |
| `getRotation` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Angle | Pure |
| `setRotation` | `function` | self: { "Engine", "ControlBase" } = "self"; angle: sf.Angle | — | — |
| `rotate` | `function` | self: { "Engine", "ControlBase" } = "self"; angle: sf.Angle | — | — |
| `getScale` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Vector2f | Pure |
| `setScale` | `function` | self: { "Engine", "ControlBase" } = "self"; scale: sf.Vector2f | — | — |
| `scale` | `function` | self: { "Engine", "ControlBase" } = "self"; factor: sf.Vector2f | — | — |
| `getOrigin` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Vector2f | Pure |
| `setOrigin` | `function` | self: { "Engine", "ControlBase" } = "self"; origin: sf.Vector2f | — | — |
| `getTransform` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Transform | Pure |
| `getInverseTransform` | `function` | self: { "Engine", "ControlBase" } = "self" | return: sf.Transform | Pure |
| `draw` | `function` | self: { "Engine", "ControlBase" } = "self"; target: sf.RenderTarget; states: sf.RenderStates | — | — |

## FocusableMixin

Direct metadata bases: —

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `setCanReceiveFocus` | `function` | self: { "Engine", "FocusableMixin" } = "self"; canReceiveFocus: bool | — | — |
| `getCanReceiveFocus` | `function` | self: { "Engine", "FocusableMixin" } = "self" | return: bool | Pure |
| `getFocused` | `function` | self: { "Engine", "FocusableMixin" } = "self" | return: bool | Pure |
| `setFocused` | `function` | self: { "Engine", "FocusableMixin" } = "self"; focused: bool | — | — |
| `setFocusGroup` | `function` | self: { "Engine", "FocusableMixin" } = "self"; focusGroup: { "Engine", "RuntimeObject" } | — | — |
| `getFocusGroup` | `function` | self: { "Engine", "FocusableMixin" } = "self" | return: { "Engine", "RuntimeObject" } | Pure |
| `onFocusGained` | `function` | self: { "Engine", "FocusableMixin" } = "self" | — | — |
| `onFocusLost` | `function` | self: { "Engine", "FocusableMixin" } = "self" | — | — |

## FunctionalBase

Direct metadata bases: `{ "Engine", "FocusableMixin" }`

### Input event data

Input methods and callbacks receive `Engine.UiInputEventArguments`. Pointer events provide `position: sf.Vector2f`. Mouse-button events also provide `button: sf.Mouse.Button`, and wheel events also provide `delta: number`. All fields are optional and absent from keyboard events. Use `Engine.UiInputEventArguments.new({})` for programmatic confirmation or cancellation.

### Interaction eligibility

A `FunctionalBase` accepts focus and input only while it and every `FunctionalBase` ancestor are active, and while it and every `ControlBase` ancestor are visible. `getActive` and `getVisible` remain local-state queries. Losing eligibility clears pointer and touch capture throughout the subtree and keeps the cancelled gesture consumed until release, while ticking and rendering continue.

### Touch capture

`setTouchHitBounds` sets an optional touch-only hit rectangle in the control's local logical coordinates. The rectangle must have finite coordinates and a non-negative size, and passing `nil` restores the default. `getAbsoluteTouchHitBounds` returns its axis-aligned screen rectangle after the runtime display scale and control hierarchy transforms are applied. A `FunctionalBase` mixed into a `ControlBase` defaults to the control's absolute bounds. A standalone `FunctionalBase` has no control coordinate space, so the getter returns `nil`.

An effectively enabled functional control captures a primary touch whose beginning position is inside its absolute touch hit bounds. The capturing control continues to receive movement and the final release even after the pointer leaves those bounds. Releasing outside clears the pressed state without invoking click or confirm, but any tap belonging to that capture is still consumed so it cannot activate a control underneath. Multi-touch cancellation also releases capture, and touch hit bounds do not affect mouse hit testing.

### Pointer click

A mouse press begins pointer capture but does not invoke `onClick`. The click fires only when the pressed button is released while the pointer is still hovering the same control; a release after the pointer leaves the control clears the pressed state without a click. The click arguments carry the released `button`, so callbacks that only handle the left button can filter on it. A touch tap follows the touch-capture rules above and prefers `onConfirm` when a confirm callback is present.

### Properties

No editable properties are declared.

### Functions and events

| Name | Kind | Parameters | Returns | Execution and metadata |
|---|---|---|---|---|
| `setKeyboardFocusResolver` | `function` | resolver: function | — | — |
| `setDirectionalFocusRequester` | `function` | requester: function | — | — |
| `setKeyboardFocusSetter` | `function` | setter: function | — | — |
| `setKeyboardCursorResolver` | `function` | resolver: function | — | — |
| `canReceiveFocus` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | Pure |
| `shouldDispatchKeyboardInput` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | Pure |
| `requestDirectionalFocusMove` | `function` | self: { "Engine", "FunctionalBase" } = "self"; direction: string | return: bool | — |
| `requestKeyboardFocus` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | — |
| `ownsKeyboardCursorFocus` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | Pure |
| `isHovered` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | Pure |
| `isPressed` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | Pure |
| `getActive` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: bool | Pure |
| `setActive` | `function` | self: { "Engine", "FunctionalBase" } = "self"; active: bool | — | — |
| `setTouchHitBounds` | `function` | self: { "Engine", "FunctionalBase" } = "self"; bounds: sf.FloatRect \| nil | — | — |
| `getAbsoluteTouchHitBounds` | `function` | self: { "Engine", "FunctionalBase" } = "self" | return: sf.FloatRect \| nil | Pure |
| `addConfirmCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addCancelCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addClickCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addMouseButtonDownCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addHoverCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addUnHoverCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addMouseMovedCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addMouseWheelScrolledCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addKeyDownCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `addKeyUpCallback` | `function` | self: { "Engine", "FunctionalBase" } = "self"; callback: function | — | — |
| `update` | `function` | self: { "Engine", "FunctionalBase" } = "self"; deltaTime: float | — | — |
| `lateUpdate` | `function` | self: { "Engine", "FunctionalBase" } = "self"; deltaTime: float | — | — |
| `fixedUpdate` | `function` | self: { "Engine", "FunctionalBase" } = "self"; fixedDelta: float | — | — |
| `onConfirm` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onCancel` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onClick` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseButtonDown` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | return: bool | — |
| `onHover` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onUnHover` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseMoved` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseWheelScrolled` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onKeyDown` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onKeyUp` | `function` | self: { "Engine", "FunctionalBase" } = "self"; arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onTick` | `function` | self: { "Engine", "FunctionalBase" } = "self"; deltaTime: float | — | — |
| `onLateTick` | `function` | self: { "Engine", "FunctionalBase" } = "self"; deltaTime: float | — | — |
| `onFixedTick` | `function` | self: { "Engine", "FunctionalBase" } = "self"; fixedDelta: float | — | — |

## FunctionalImage

Direct metadata bases: `{ "Engine", "Image" }`, `{ "Engine", "FunctionalBase" }`

### Properties

No editable properties are declared.

### Functions and events

No Blueprint functions or events are declared.

## FunctionalPlainText

Direct metadata bases: `{ "Engine", "PlainText" }`, `{ "Engine", "FunctionalBase" }`

### Properties

No editable properties are declared.

### Functions and events

No Blueprint functions or events are declared.

## FunctionalRichText

Direct metadata bases: `{ "Engine", "RichText" }`, `{ "Engine", "FunctionalBase" }`

### Properties

No editable properties are declared.

### Functions and events

No Blueprint functions or events are declared.
