# UI Asset Editor

## Goal

Create and arrange a declarative UI asset without writing its runtime Controller.

## Prerequisites

- A saved project with valid UI assets under `Data/UI/Assets`.
- Planned asset ownership: complete UI, owner-specific part or shared part.
- Project resource keys for any textures, text configurations or nested assets.

## Open or create an asset

Choose **Game → New UI Asset**, or create/open a JSON asset beneath `Data/UI/Assets` from the project file explorer. Complete screens and windows belong directly under `Assets`; parts owned by one UI belong under `Assets/Parts/<Owner>`; genuinely shared parts belong under `Assets/Parts/Shared`.

![UI Asset Editor showing Palette, Hierarchy, Designer and Inspector](../../_images/editor/ui-asset-editor.png)

*The UI Asset Editor separates available controls, tree structure, visual placement and selected-node properties.*

## Steps

1. Set the design width and height for the asset.
2. Add a root control. A new screen normally starts with `Engine.Canvas`.
3. Drag system controls or exposed project assets from the Palette into the Hierarchy.
4. Select each node and edit control properties in the Inspector.
5. Configure the child Slot. Canvas children use anchors, offsets, alignment, auto-size and z-order; List children use list-owned placement.
6. Give every node a meaningful name that is unique within the asset. Runtime Controllers use these names for lookup.
7. Set **Exposed** only when the asset is a complete, reusable item that should appear in the Project Palette.
8. Save and review validation results.

## Asset paths and node identity

The asset path relative to `Data/UI/Assets`, with forward slashes and no extension, is its only asset identity. Node `id` values are project-unique UUIDs managed by the editor. Moving or renaming an asset rewrites managed JSON nesting references; update matching `Ui.define`, `require` and LuaDoc paths manually. Copying an asset through the editor creates new node identities.

## Preview behaviour

The Designer uses native preview when the matching `UiPreviewHost` and adapter fingerprint are available. If native preview is unavailable, the editor reports the reason and uses its supported fallback. Design-only `previewText` helps place localised or runtime-generated text; the runtime ignores every `editor` object.

## Verification

- The Hierarchy contains no duplicate node names or IDs.
- Every non-root node has a Slot accepted by its parent.
- Resource properties use project keys rather than absolute paths.
- Save completes without path, nesting-cycle or adapter errors.
- Native preview and the running game produce the same layout boundaries.

## Expected result

The asset saves under one canonical relative key with stable node UUIDs, valid nesting and Slots, appears in the palette only when exposed, and produces the same layout contract in preview and at runtime.

## Limitations

The editor designs static structure and preview values; runtime data, callbacks and lifecycle belong to the Lua Controller. External JSON duplication can create node identity conflicts that the editor will reject.

## Related pages

- [Declarative UI](<../03.Lua and Blueprint Scripting/05.Declarative UI/00.Overview.md>)
- [Native UI Adapters](<../04.Native C++ Development/08.Native UI Adapters.md>)

Controller code, nested-instance ownership and asset validation are covered by [Declarative UI](<../03.Lua and Blueprint Scripting/05.Declarative UI/00.Overview.md>).
