# General Data Reference

This section is generated from the current Sample schemas beneath `Data/General`. It documents schema fields, member identifiers, linked runtime types and member events. Values are project data, not a fixed engine enumeration.

Each type uses `params` for field definitions and `members` for records. A `reference` narrows an editor selector. A `linkedType` connects member graphs to runtime metadata. Brace-wrapped display strings are covered by Plugin System and Localisation.
