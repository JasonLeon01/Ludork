# Class

Generated from the current Sample schemas beneath `Data/General`: schema fields, linked runtime types and member events. Values are project data, not a fixed engine enumeration.

Each type uses `params` for field definitions and `members` for records. A `reference` narrows an editor selector. A `linkedType` connects member graphs to runtime metadata. Brace-wrapped display strings follow [Game Localisation Workflow](<../../05.Plug-in Development and Installation/06.Game Localisation Workflow.md>).

Source: `Data/General/Class.json`

| Property | Value |
|---|---|
| Linked runtime type | — |
| Member events | — |

## Schema

| Field | Type | Default | Description | Reference |
|---|---|---|---|---|
| `name` | `string` | `""` | The display name of this class. | `—` |
| `desc` | `string` | `""` | The display description of this class. | `—` |
| `slot` | `Dict[string, string]` | `{}` | Equipment slot names mapped to the equipment initially equipped in each slot; use an empty value for an unequipped slot. | `—` |

## Editing notes

Add fields before populating many members. A reference controls the selector but the stored value remains the declared JSON shape. When events are listed, each member may store a graph under `_graph`; an absent start node means the optional event has no implementation.
