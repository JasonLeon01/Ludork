# 05. Player

Source file: `Sample/Source/NodeFunctions/Player.py`

#### `GetPlayer() -> Optional[Player]`

Original: `Source.NodeFunctions.Player.GetPlayer`

Get the primary player from the current scene.

**Outputs:** `player: Player`

---

#### `GetPlayerFrontPosition() -> Optional[Vector2i]`

Original: `Source.NodeFunctions.Player.GetPlayerFrontPosition`

Get the tile coordinate in front of the player based on the current facing direction.

**Outputs:** `position: Vector2i`

---

#### `AddItem(itemID: str, count: int) -> None`

Original: `Source.NodeFunctions.Player.AddItem`

Add item(s) to the player inventory.

**Parameters**
- `itemID` – ID of the item to add.
- `count` – Number of items to add.

**Exec Outputs:** `default`

---

#### `RemoveItem(itemID: str, count: int) -> int`

Original: `Source.NodeFunctions.Player.RemoveItem`

Remove item(s) from the player inventory. Fails if count is insufficient.

**Parameters**
- `itemID` – ID of the item to remove.
- `count` – Number of items to remove.

**Exec Outputs:** `Success`, `Failed`

---

#### `HasItem(itemID: str) -> bool`

Original: `Source.NodeFunctions.Player.HasItem`

Check whether the player owns at least one of the specified item.

**Parameters**
- `itemID` – ID of the item to check.

**Outputs:** `value: bool`

---

#### `GetItemCount(itemID: str) -> int`

Original: `Source.NodeFunctions.Player.GetItemCount`

Get the quantity of a specific item in the player inventory.

**Parameters**
- `itemID` – ID of the item to query.

**Outputs:** `count: int`

---

#### `AddEquip(equipID: str, count: int = 1) -> None`

Original: `Source.NodeFunctions.Player.AddEquip`

Add equip(s) to the player equipment bag.

**Parameters**
- `equipID` – ID of the equipment to add.
- `count` – Number to add (default `1`).

**Exec Outputs:** `default`

---

#### `RemoveEquip(equipID: str, count: int) -> int`

Original: `Source.NodeFunctions.Player.RemoveEquip`

Remove equip(s) from the player equipment bag. Fails if count is insufficient.

**Parameters**
- `equipID` – ID of the equipment to remove.
- `count` – Number to remove.

**Exec Outputs:** `Success`, `Failed`

---

#### `HasEquip(equipID: str) -> bool`

Original: `Source.NodeFunctions.Player.HasEquip`

Check whether the player owns at least one of the specified equip.

**Parameters**
- `equipID` – ID of the equipment to check.

**Outputs:** `value: bool`

---

#### `EquipItem(equipID: str) -> None`

Original: `Source.NodeFunctions.Player.EquipItem`

Equip a piece of equipment onto the player.

**Parameters**
- `equipID` – ID of the equipment to equip.

**Exec Outputs:** `default`

---

#### `UnequipSlot(slotID: str) -> None`

Original: `Source.NodeFunctions.Player.UnequipSlot`

Unequip the item occupying the given equipment slot.

**Parameters**
- `slotID` – Equipment slot ID to unequip.

**Exec Outputs:** `default`

---

#### `GetEquipInSlot(slotID: str) -> str`

Original: `Source.NodeFunctions.Player.GetEquipInSlot`

Get the equip ID in the specified slot, or an empty string if the slot is empty.

**Parameters**
- `slotID` – Equipment slot ID to query.

**Outputs:** `equipID: str`

---

#### `GetPlayerAttr(attrName: str) -> Any`

Original: `Source.NodeFunctions.Player.GetPlayerAttr`

Get a named attribute from the player (e.g. `HP`, `MAXHP`, `ATK`, `DEF`, `GOLD`, `EXP`, `LEVEL`).

**Parameters**
- `attrName` – Name of the attribute to read.

**Outputs:** `value: object`

---

#### `SetPlayerAttr(attrName: str, value: Any) -> None`

Original: `Source.NodeFunctions.Player.SetPlayerAttr`

Set a named attribute on the player (e.g. `HP`, `MAXHP`, `ATK`, `DEF`, `GOLD`, `EXP`, `LEVEL`).

**Parameters**
- `attrName` – Name of the attribute to write.
- `value` – New attribute value.

**Exec Outputs:** `default`

---

#### `HealPlayer(amount: int) -> None`

Original: `Source.NodeFunctions.Player.HealPlayer`

Restore HP to the player, capped at `MAXHP`.

**Parameters**
- `amount` – Amount of HP to restore.

**Exec Outputs:** `default`

---

#### `DamagePlayer(amount: int) -> None`

Original: `Source.NodeFunctions.Player.DamagePlayer`

Deal damage to the player, floored at `0`.

**Parameters**
- `amount` – Amount of damage to deal.

**Exec Outputs:** `default`

---

#### `AddGold(amount: int) -> None`

Original: `Source.NodeFunctions.Player.AddGold`

Add gold to the player (can be negative to subtract).

**Parameters**
- `amount` – Amount of gold to add (negative values subtract).

**Exec Outputs:** `default`

---

#### `AddEXP(amount: int) -> None`

Original: `Source.NodeFunctions.Player.AddEXP`

Add experience points to the player.

**Parameters**
- `amount` – Amount of EXP to add.

**Exec Outputs:** `default`

---

#### `MeetPlayer(actors: List[Actor]) -> Optional[Player]`

Original: `Source.NodeFunctions.Player.MeetPlayer`

Check whether any of the given actors is the player and return it.

**Parameters**
- `actors` – List of actors to check.

**Outputs:** `playerInfo: Player`
