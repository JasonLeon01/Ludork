# 06. Save

Source file: `Sample/Source/NodeFunctions/Save.py`

#### `SaveGame(filePath: str) -> None`

Original: `Source.NodeFunctions.Save.SaveGame`

Save the current game state to a file (`.json` or `.dat`).

**Parameters**
- `filePath` – Path to the save file.

**Exec Outputs:** `default`

---

#### `LoadGame(filePath: str) -> int`

Original: `Source.NodeFunctions.Save.LoadGame`

Load game state from a file and apply it to the current scene.

**Parameters**
- `filePath` – Path to the save file.

**Exec Outputs:** `Loaded`, `NotFound`

---

#### `GetSavePath(slot: int, ext: str) -> str`

Original: `Source.NodeFunctions.Save.GetSavePath`

Get the platform-specific save file path for a given slot.

**Parameters**
- `slot` – Save slot index.
- `ext` – File extension (e.g. `"json"` or `"dat"`).

**Outputs:** `path: str`
