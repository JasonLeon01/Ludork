# 08. State

Source file: `Sample/Source/NodeFunctions/State.py`

#### `GetStateOwner() -> Optional[object]`

Original: `Source.NodeFunctions.State.GetStateOwner`

Return the battler currently hosting this state.

**Outputs:** `battler: object`

---

#### `GetEventArg(name: str, default: Any) -> Any`

Original: `Source.NodeFunctions.State.GetEventArg`

Read a keyword argument injected into the current blueprint event (e.g. `battler`, `context`, `opponent`, `won`).

**Parameters**
- `name` – Name of the keyword argument.
- `default` – Value to return if the argument is not present.

**Outputs:** `value: object`

---

#### `GetBattlerAttr(battler: Any, attrName: str, default: Any) -> Any`

Original: `Source.NodeFunctions.State.GetBattlerAttr`

Read a named attribute from any battler (`HP`, `MAXHP`, `ATK`, `DEF`, …).

**Parameters**
- `battler` – The battler to read from.
- `attrName` – Name of the attribute.
- `default` – Value to return if the attribute is not found.

**Outputs:** `value: object`

---

#### `SetBattlerAttr(battler: Any, attrName: str, value: Any) -> None`

Original: `Source.NodeFunctions.State.SetBattlerAttr`

Write a named attribute on any battler.

**Parameters**
- `battler` – The battler to write to.
- `attrName` – Name of the attribute.
- `value` – New attribute value.

**Exec Outputs:** `default`

---

#### `DamageBattler(battler: Any, amount: int) -> None`

Original: `Source.NodeFunctions.State.DamageBattler`

Subtract HP from any battler (floored at `0`).

**Parameters**
- `battler` – The battler to damage.
- `amount` – Amount of HP to subtract.

**Exec Outputs:** `default`

---

#### `HealBattler(battler: Any, amount: int) -> None`

Original: `Source.NodeFunctions.State.HealBattler`

Restore HP on any battler (capped at `MAXHP`).

**Parameters**
- `battler` – The battler to heal.
- `amount` – Amount of HP to restore.

**Exec Outputs:** `default`

---

#### `BattlerHasState(battler: Any, stateID: str) -> bool`

Original: `Source.NodeFunctions.State.BattlerHasState`

Check whether a battler currently carries the given state ID.

**Parameters**
- `battler` – The battler to check.
- `stateID` – State ID to look for.

**Outputs:** `value: bool`

---

#### `AddStateTo(battler: Any, stateID: str, stacks: int) -> None`

Original: `Source.NodeFunctions.State.AddStateTo`

Apply a state (by ID) to any battler, firing its `onAdd` blueprint.

**Parameters**
- `battler` – The battler to apply the state to.
- `stateID` – ID of the state to apply.
- `stacks` – Stack count to apply.

**Exec Outputs:** `default`

---

#### `RemoveStateFrom(battler: Any, stateID: str) -> None`

Original: `Source.NodeFunctions.State.RemoveStateFrom`

Remove a state (by ID) from any battler, firing its `onRemove` blueprint.

**Parameters**
- `battler` – The battler to remove the state from.
- `stateID` – ID of the state to remove.

**Exec Outputs:** `default`
