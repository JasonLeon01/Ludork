# 07. Scene

Source file: `Sample/Source/NodeFunctions/Scene.py`

#### `GotoMap(mapPath: str, blockTransition: bool, position: Optional[Union[Vector2i, Pair[int], List[int], Tuple[int, int]]] = None) -> None`

Original: `Source.NodeFunctions.Scene.GotoMap`

Transition to a map and optionally place the player at a tile coordinate.

`position` uses a map coordinate picker (`@Meta(Transfer)`): click a tile on the referenced map to set the destination. When `position` is `None`, the player keeps their current position.

**Parameters**
- `mapPath` – Path to the target map data file (relative to `Data/Maps/`).
- `blockTransition` – If `True`, skip the map transition effect.
- `position` – Target tile coordinate as `(x, y)`, or `None` to keep the current position.

**Exec Outputs:** `default`

---

#### `GameOver() -> None`

Original: `Source.NodeFunctions.Scene.GameOver`

Switch to the game over scene.

**Exec Outputs:** `default`

---

#### `LockCamera() -> None`

Original: `Source.NodeFunctions.Scene.LockCamera`

Lock the current map camera to the player.

**Exec Outputs:** `default`

---

#### `UnlockCamera() -> None`

Original: `Source.NodeFunctions.Scene.UnlockCamera`

Stop the current map camera from following the player.

**Exec Outputs:** `default`

---

#### `RecordTelepoint(mapPath: str, x: int, y: int) -> None`

Original: `Source.NodeFunctions.Scene.RecordTelepoint`

Record a telepoint for a map so it can be used by teleport items.

**Parameters**
- `mapPath` – Path to the target map.
- `x` – Telepoint X coordinate.
- `y` – Telepoint Y coordinate.

**Exec Outputs:** `default`

---

#### `DestroyTerrain(layerName: str, position: Union[Vector2i, Vector2u, Pair[int], List[int]], tileID: Any) -> None`

Original: `Source.NodeFunctions.Scene.DestroyTerrain`

Replace one tile on a layer of the current map with the specified tile ID.

`position` uses an x/y integer editor (`@Meta(Vector2iVars)`).

**Parameters**
- `layerName` – Name of the tile layer to edit.
- `position` – Tile coordinate.
- `tileID` – Replacement tile ID.

**Exec Outputs:** `default`

---

#### `DestroyTerrainList(layerName: str, positions: List[Any], tileID: Any) -> None`

Original: `Source.NodeFunctions.Scene.DestroyTerrainList`

Replace multiple tiles on a layer of the current map with the specified tile ID.

**Parameters**
- `layerName` – Name of the tile layer to edit.
- `positions` – List of tile coordinates.
- `tileID` – Replacement tile ID.

**Exec Outputs:** `default`

---

#### `GetTerrainTile(layerName: str, position: Union[Vector2i, Vector2u, Pair[int], List[int]]) -> Any`

Original: `Source.NodeFunctions.Scene.GetTerrainTile`

Get the tile ID at a layer position on the current map.

`position` uses an x/y integer editor (`@Meta(Vector2iVars)`).

**Parameters**
- `layerName` – Name of the tile layer to query.
- `position` – Tile coordinate.

**Outputs:** `tileID: Any`

---

#### `GetTerrainTilePositions(layerName: str, tileID: Any) -> List[Vector2i]`

Original: `Source.NodeFunctions.Scene.GetTerrainTilePositions`

Get all coordinates matching a tile ID on a layer of the current map.

**Parameters**
- `layerName` – Name of the tile layer to query.
- `tileID` – Tile ID to match.

**Outputs:** `positions: List[Vector2i]`

---

#### `OpenShop(items: List[str], canSell: bool) -> Callable[[], bool]`

Original: `Source.NodeFunctions.Scene.OpenShop`

Open the map shop with purchasable item IDs and whether selling is allowed.

**Parameters**
- `items` – List of purchasable item IDs.
- `canSell` – Whether the player can sell items.

**Latent Outputs:** `Closed`
