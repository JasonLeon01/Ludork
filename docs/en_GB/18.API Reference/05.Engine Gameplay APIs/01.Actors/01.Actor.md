# 01. Actor

Source file: `Sample/Engine/Gameplay/Actors/Actor.py`

## Class `Engine.Gameplay.Actors.Actor.Actor`

Game actor with collision, movement, and blueprint event support. Extends `_ActorBase` with grid-based movement, collision detection, routine (pathfinding) execution, and blueprint event dispatching.

**Class variables**

- `collisionEnabled`: type `bool`; default `False`
- `tickable`: type `bool`; default `False`
- `speed`: type `float`; default `64.0`
- `texturePath`: type `str`; default `''`
- `defaultRect`: type `Optional[Tuple[Pair[int], Pair[int]]]`; default `((0, 0), (32, 32))`
- `defaultTranslation`: type `Pair[float]`; default `(0.0, 0.0)`
- `defaultRotation`: type `float`; default `0.0`
- `defaultScale`: type `Pair[float]`; default `(1.0, 1.0)`
- `defaultOrigin`: type `Pair[float]`; default `(0.0, 0.0)`

**Blueprint API**

#### `onCreate() -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onCreate`

Blueprint event: called once when the actor is spawned into the scene.

**Event:** Blueprint event entry point.

---

#### `onTick(deltaTime: float) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onTick`

Blueprint event: called every frame while `tickable` is `True`.

**Parameters**
- `deltaTime` – Time elapsed since the last frame in seconds.

**Event:** Blueprint event entry point.

---

#### `onLateTick(deltaTime: float) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onLateTick`

Blueprint event: called after all actors' `onTick` in the same frame.

**Parameters**
- `deltaTime` – Time elapsed since the last frame in seconds.

**Event:** Blueprint event entry point.

---

#### `onFixedTick(fixedDelta: float) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onFixedTick`

Blueprint event: called at a fixed physics timestep.

**Parameters**
- `fixedDelta` – Fixed timestep duration in seconds.

**Event:** Blueprint event entry point.

---

#### `onDestroy() -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onDestroy`

Blueprint event: called when the actor is removed from the scene.

**Event:** Blueprint event entry point.

---

#### `onCollision(other: List[Actor]) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onCollision`

Blueprint event: called when movement is blocked by another actor.

**Parameters**
- `other` – List of actors at the collision target cell.

**Event:** Blueprint event entry point.

---

#### `onOverlap(other: List[Actor]) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.onOverlap`

Blueprint event: called when this actor enters a cell occupied by others.

**Parameters**
- `other` – List of actors sharing the same cell.

**Event:** Blueprint event entry point.

---

#### `destroy() -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.destroy`

Remove this actor from the current map and trigger `onDestroy`.

**Exec Outputs:** `default`

---

#### `isDestroyed() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.isDestroyed`

Check whether the actor has been destroyed.

**Outputs:** `destroyed: bool`

---

#### `MapMove(offset: Union[Vector2i, Pair[int], List[int]]) -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.MapMove`

Move the actor by one cell in the given direction. Validates boundaries and passability before initiating movement. Triggers `onCollision` on both parties if the target cell is blocked.

**Parameters**
- `offset` – Direction vector (clamped to unit: -1, 0, or 1 per axis).

**Exec Outputs:** `success`, `fail`

---

#### `getTickable() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.getTickable`

Check whether tick events are enabled.

**Outputs:** `tickable: bool`

---

#### `setTickable(tickable: bool, applyToChildren: bool = True) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.setTickable`

Enable or disable tick event dispatch. Optionally propagates the setting to all child actors.

**Parameters**
- `tickable` – Whether to enable tick events.
- `applyToChildren` – If `True`, propagate to all child actors.

**Exec Outputs:** `default`

---

#### `getVisible() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.getVisible`

Check whether this actor is visible.

**Outputs:** `visible: bool`

---

#### `setVisible(visible: bool, applyToChildren: bool = True) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.setVisible`

Set actor visibility. Optionally propagates the setting to all child actors.

**Parameters**
- `visible` – Whether the actor should be visible.
- `applyToChildren` – If `True`, propagate to all child actors.

**Exec Outputs:** `default`

---

#### `getCollisionEnabled() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.getCollisionEnabled`

Check whether this actor blocks movement of others.

**Outputs:** `collisionEnabled: bool`

---

#### `setCollisionEnabled(enabled: bool) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.setCollisionEnabled`

Enable or disable collision blocking.

**Parameters**
- `enabled` – Whether to enable collision blocking.

**Exec Outputs:** `default`

---

#### `intersects(other: Actor) -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.intersects`

Test whether this actor's bounding box overlaps another's.

**Parameters**
- `other` – The other actor to test against.

**Outputs:** `intersects: bool`

---

#### `isMoving() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.isMoving`

Check whether this actor is currently in motion.

**Outputs:** `isMoving: bool`

---

#### `isInRoute() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.isInRoute`

Check whether this actor is executing a movement routine.

**Outputs:** `isInRoute: bool`

---

#### `setRoute(routine: Optional[List[Vector2i]]) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.setRoute`

Set a sequence of grid offsets to walk automatically.

**Parameters**
- `routine` – List of grid offsets, or `None` to clear.

**Exec Outputs:** `default`

---

#### `getRoute() -> Optional[List[Vector2i]]`

Original: `Engine.Gameplay.Actors.Actor.Actor.getRoute`

Get the current movement routine.

**Outputs:** `routine: Optional[List[Vector2i]]`

---

#### `getMoveEnabled() -> bool`

Original: `Engine.Gameplay.Actors.Actor.Actor.getMoveEnabled`

Check whether movement is allowed.

**Outputs:** `moveEnabled: bool`

---

#### `setMoveEnabled(enabled: bool) -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.setMoveEnabled`

Enable or disable movement. Disabling also stops current motion.

**Parameters**
- `enabled` – Whether to enable movement.

**Exec Outputs:** `default`

---

#### `stop() -> None`

Original: `Engine.Gameplay.Actors.Actor.Actor.stop`

Immediately halt all movement and clear the current routine. Resets motion state and snaps map position to the current location.

**Exec Outputs:** `default`

---

#### `getVelocity() -> Optional[Vector2f]`

Original: `Engine.Gameplay.Actors.Actor.Actor.getVelocity`

Compute the current velocity vector. Accounts for the tile material speed rate at the current map position. Returns `None` if not moving.

**Outputs:** `velocity: Optional[Vector2f]`
