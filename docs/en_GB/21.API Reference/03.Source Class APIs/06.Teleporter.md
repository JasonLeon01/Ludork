# 06. Teleporter

Source file: `Sample/Source/Teleporter.py`

## Class `Source.Teleporter.Teleporter`

Actor used to move between neighbouring maps in the current region.

**Class variables**

- `Offset`: type `Pair[int]`; default `(0, 0)` — x/y integer editor (`@Meta(Vector2iVars)`).
- `transitionName`: type `str`; default `''`
- `transitionTime`: type `float`; default `0.5`

**Blueprint API**

#### `GoUpstairs() -> None`

Original: `Source.Teleporter.Teleporter.GoUpstairs`

Move to the next map in the current region.

**Exec Outputs:** `default`

---

#### `GoDownstairs() -> None`

Original: `Source.Teleporter.Teleporter.GoDownstairs`

Move to the previous map in the current region.

**Exec Outputs:** `default`
