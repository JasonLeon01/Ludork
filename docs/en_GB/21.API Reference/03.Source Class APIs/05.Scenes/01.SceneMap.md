# 01. SceneMap

Source file: `Sample/Source/Scenes/SceneMap.py`

## Class `Source.Scenes.SceneMap.Scene`

Main gameplay scene handling map rendering, actors, and player interaction.

**Class variables**

- No public class variables declared here.

**Blueprint API**

#### `showMessage(refActorTag: str, name: str, message: str) -> Callable[[], bool]`

Original: `Source.Scenes.SceneMap.Scene.showMessage`

Show a dialogue message window.

**Parameters**
- `refActorTag` – Tag of the reference actor used for window positioning.
- `name` – Speaker name displayed in the window.
- `message` – Message text to display.

**Latent Outputs:** `FinishedDialogue`

---

#### `showSelection(refActorTag: str, name: str, options: List[str], allowCancel: bool) -> Callable[[], Optional[int]]`

Original: `Source.Scenes.SceneMap.Scene.showSelection`

Show a selection window with multiple options.

**Parameters**
- `refActorTag` – Tag of the reference actor used for window positioning.
- `name` – Window title.
- `options` – List of option strings.
- `allowCancel` – Whether the player can cancel the selection.

**Latent Outputs:** `Selected0`, `Selected1`, `Selected2`, `Selected3`, `Cancelled`

---

#### `applyLoadedGame(inst: GameInstance) -> None`

Original: `Source.Scenes.SceneMap.Scene.applyLoadedGame`

Apply a loaded game instance and force-reload the cached map.

**Parameters**
- `inst` – The restored game instance from a save file.

**Exec Outputs:** `default`

---

#### `showEnemyBook() -> None`

Original: `Source.Scenes.SceneMap.Scene.showEnemyBook`

Show the current-map monster handbook.

**Exec Outputs:** `default`

---

#### `showFloorTeleporter() -> None`

Original: `Source.Scenes.SceneMap.Scene.showFloorTeleporter`

Show the visited-floor teleporter preview window.

**Exec Outputs:** `default`

---

#### `recordAddedActor(actor: Actor) -> None`

Original: `Source.Scenes.SceneMap.Scene.recordAddedActor`

Record an added actor for persistence. The actor must already be spawned on the current map and must have a stable tag.

**Parameters**
- `actor` – The added actor to record.

**Exec Outputs:** `default`

---

#### `recordActorPosition(actor: Actor) -> None`

Original: `Source.Scenes.SceneMap.Scene.recordActorPosition`

Record an actor position change for persistence. The actor must have a stable tag.

**Parameters**
- `actor` – The actor whose current tile position should be recorded.

**Exec Outputs:** `default`

---

#### `recordDestroyedActor(actor: Actor) -> None`

Original: `Source.Scenes.SceneMap.Scene.recordDestroyedActor`

Record a destroyed actor for persistence.

**Parameters**
- `actor` – The destroyed actor to record.

**Exec Outputs:** `default`
