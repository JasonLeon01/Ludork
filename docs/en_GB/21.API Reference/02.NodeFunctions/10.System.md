# 10. System

Source file: `Sample/Source/NodeFunctions/System.py`

#### `EditSoundFilter(attr: str, value: Any) -> None`

Original: `Source.NodeFunctions.System.EditSoundFilter`

Set a property on the sound filter.

**Parameters**
- `attr` – Name of the filter property to set.
- `value` – New property value.

**Exec Outputs:** `default`

---

#### `EditMusicFilter(attr: str, value: Any) -> None`

Original: `Source.NodeFunctions.System.EditMusicFilter`

Set a property on the music filter.

**Parameters**
- `attr` – Name of the filter property to set.
- `value` – New property value.

**Exec Outputs:** `default`

---

#### `PlaySound(soundFileName: str, applyFilter: bool = False) -> None`

Original: `Source.NodeFunctions.System.PlaySound`

Play a sound effect, optionally with filter.

**Parameters**
- `soundFileName` – Filename of the sound effect.
- `applyFilter` – Whether to apply the current sound filter.

**Exec Outputs:** `default`

---

#### `PlayMusic(musicFileName: str, applyFilter: bool = False) -> None`

Original: `Source.NodeFunctions.System.PlayMusic`

Play background music, optionally with filter.

**Parameters**
- `musicFileName` – Filename of the music track.
- `applyFilter` – Whether to apply the current music filter.

**Exec Outputs:** `default`

---

#### `PlayVideo(videoFileName: str, mute: bool = False, skipable: bool = True) -> None`

Original: `Source.NodeFunctions.System.PlayVideo`

Play a video file with mute and skippable options.

**Parameters**
- `videoFileName` – Filename of the video.
- `mute` – Whether to mute the audio.
- `skipable` – Whether the player can skip the video.

**Exec Outputs:** `default`

---

#### `FreezeTransitionBackground() -> Callable[[], bool]`

Original: `Source.NodeFunctions.System.FreezeTransitionBackground`

Capture the current frame as the next transition background, then continue when ready.

**Latent Outputs:** `Frozen`

---

#### `RequestTransition(transitionName: str = "", transitionTime: float = 1.0) -> Callable[[], bool]`

Original: `Source.NodeFunctions.System.RequestTransition`

Start a screen transition (an empty `transitionName` uses a plain fade), then continue when it finishes.

**Parameters**
- `transitionName` – Name of the transition effect; leave empty for a plain fade.
- `transitionTime` – Duration of the transition in seconds.

**Latent Outputs:** `Finished`

---

#### `SetBgmFilter(attr: str, value: Any) -> None`

Original: `Source.NodeFunctions.System.SetBgmFilter`

Apply filter properties to the currently playing BGM on the current map scene.

**Parameters**
- `attr` – Name of the filter property to set.
- `value` – New property value.

**Exec Outputs:** `default`

---

#### `SetBgsFilter(attr: str, value: Any) -> None`

Original: `Source.NodeFunctions.System.SetBgsFilter`

Apply filter properties to the currently playing BGS on the current map scene.

**Parameters**
- `attr` – Name of the filter property to set.
- `value` – New property value.

**Exec Outputs:** `default`

---

#### `FlashScreen(red: int = 255, green: int = 255, blue: int = 255, alpha: int = 255, duration: float = 0.5) -> None`

Original: `Source.NodeFunctions.System.FlashScreen`

Trigger a screen flash with the given colour and duration. Colour channels and alpha range from `0` to `255`; duration is in seconds.

**Parameters**
- `red` – Red channel (0–255).
- `green` – Green channel (0–255).
- `blue` – Blue channel (0–255).
- `alpha` – Alpha channel (0–255).
- `duration` – Flash duration in seconds.

**Exec Outputs:** `default`

---

#### `StopFlashScreen() -> None`

Original: `Source.NodeFunctions.System.StopFlashScreen`

Immediately stop any in-progress screen flash effect.

**Exec Outputs:** `default`

---


#### `ChangeScreenTone(red: int = 0, green: int = 0, blue: int = 0, gray: int = 0, duration: float = 0.0) -> None`

Original: `Source.NodeFunctions.System.ChangeScreenTone`

Change the screen tone over time. Red, green, and blue range from `-255` to `255`; gray ranges from `0` to `255`; duration is in seconds.

**Parameters**
- `red` — Red tone adjustment.
- `green` — Green tone adjustment.
- `blue` — Blue tone adjustment.
- `gray` — Gray/desaturation amount.
- `duration` — Time in seconds to reach the target tone.

**Exec Outputs:** `default`

---

#### `ClearScreenTone(duration: float = 0.0) -> None`

Original: `Source.NodeFunctions.System.ClearScreenTone`

Fade the screen tone back to normal over the given duration in seconds.

**Parameters**
- `duration` — Time in seconds to return to the normal tone.

**Exec Outputs:** `default`

---
#### `ScreenShake(power: float = 4.0, speed: float = 10.0, duration: float = 0.5) -> None`

Original: `Source.NodeFunctions.System.ScreenShake`

Start an RMXP-style screen shake with power, speed, and duration.

**Parameters**
- `power` – Shake amplitude.
- `speed` – Shake speed.
- `duration` – Shake duration in seconds.

**Exec Outputs:** `default`

---

#### `StopScreenShake() -> None`

Original: `Source.NodeFunctions.System.StopScreenShake`

Immediately stop any in-progress screen shake effect.

**Exec Outputs:** `default`

---

#### `SetWeather(weatherType: WeatherType, power: int = 40, maxCount: int = 80) -> None`

Original: `Source.NodeFunctions.System.SetWeather`

Set the screen weather effect (`0` = None, `1` = Rain, `2` = Storm, `3` = Snow). Power and max density range from `0` to `100`. iOS uses a particle fallback.

**Parameters**
- `weatherType` – Weather type enum value.
- `power` – Effect intensity (0–100).
- `maxCount` – Maximum particle density (0–100).

**Exec Outputs:** `default`

---

#### `ClearWeather() -> None`

Original: `Source.NodeFunctions.System.ClearWeather`

Stop the active weather effect.

**Exec Outputs:** `default`
