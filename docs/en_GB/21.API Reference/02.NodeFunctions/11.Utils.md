# 11. Utils

Source file: `Sample/Source/NodeFunctions/Utils.py`

#### `IF(condition: bool = False) -> int`

Original: `Source.NodeFunctions.Utils.IF`

Select the `True` or `False` branch by condition.

**Parameters**
- `condition` – The boolean condition to evaluate.

**Exec Outputs:** `TRUE`, `FALSE`

---

#### `SetLocalValue(valueName: str, value: Any = None) -> None`

Original: `Source.NodeFunctions.Utils.SetLocalValue`

Set a graph local value by name.

**Parameters**
- `valueName` – Name of the local value.
- `value` – Value to assign.

**Exec Outputs:** `default`

---

#### `GetLocalValue(valueName: str, default: Any = None) -> Any`

Original: `Source.NodeFunctions.Utils.GetLocalValue`

Get a graph local value by name, with default.

**Parameters**
- `valueName` – Name of the local value.
- `default` – Value to return if not found.

**Outputs:** `value: Any`

---

#### `GetLocalValueRef(valueName: str, default: Any = None) -> Any`

Original: `Source.NodeFunctions.Utils.GetLocalValueRef`

Get a readable/writable reference to a local value.

**Parameters**
- `valueName` – Name of the local value.
- `default` – Initial value if not found.

**Outputs:** `value: Any`

---

#### `SetGameVariable(valueName: str, value: Any = None) -> None`

Original: `Source.NodeFunctions.Utils.SetGameVariable`

Set a persistent game variable (survives across scenes).

**Parameters**
- `valueName` – Name of the game variable.
- `value` – Value to assign.

**Exec Outputs:** `default`

---

#### `GetGameVariable(valueName: str, default: Any = None) -> Any`

Original: `Source.NodeFunctions.Utils.GetGameVariable`

Get a persistent game variable value.

**Parameters**
- `valueName` – Name of the game variable.
- `default` – Value to return if not found.

**Outputs:** `value: Any`

---

#### `GetGameVariableRef(valueName: str, default: Any = None) -> Any`

Original: `Source.NodeFunctions.Utils.GetGameVariableRef`

Get a readable/writable reference to a persistent game variable.

**Parameters**
- `valueName` – Name of the game variable.
- `default` – Initial value if not found.

**Outputs:** `value: Any`

---

#### `AddPlayerByClass(playerClass: str) -> None`

Original: `Source.NodeFunctions.Utils.AddPlayerByClass`

Add a player to the party by class path.

**Parameters**
- `playerClass` – Fully qualified class path of the player class.

**Exec Outputs:** `default`

---

#### `RemovePlayerByClass(playerClass: str) -> None`

Original: `Source.NodeFunctions.Utils.RemovePlayerByClass`

Remove a player from the party by class path (at least one member always remains).

**Parameters**
- `playerClass` – Fully qualified class path of the player class.

**Exec Outputs:** `default`

---

#### `AddAnim(animName: str, position: Pair[float] = (0.0, 0.0), rotation: float = 0.0, scale: Pair[float] = (1.0, 1.0)) -> None`

Original: `Source.NodeFunctions.Utils.AddAnim`

Create and add an animation sprite to the scene.

`position` and `scale` use x/y numeric editors (`@Meta(Vector2fVars)`).

**Parameters**
- `animName` – Name of the animation asset.
- `position` – World position `(x, y)` where the animation spawns.
- `rotation` – Initial rotation in degrees.
- `scale` – Initial scale `(x, y)`.

**Exec Outputs:** `default`

---

#### `AddAnimOn(animName: str, actorTag: str, rotation: float = 0.0, scale: Pair[float] = (1.0, 1.0)) -> None`

Original: `Source.NodeFunctions.Utils.AddAnimOn`

Create and add an animation at the position of the actor with the given tag.

`scale` uses an x/y numeric editor (`@Meta(Vector2fVars)`).

**Parameters**
- `animName` – Name of the animation asset.
- `actorTag` – Tag of the reference actor.
- `rotation` – Initial rotation in degrees.
- `scale` – Initial scale `(x, y)`.

**Exec Outputs:** `default`

---

#### `GetAnimLength(animName: str) -> float`

Original: `Source.NodeFunctions.Utils.GetAnimLength`

Get the full duration of an animation in seconds, including sound track length when present.

**Parameters**
- `animName` – Name of the animation asset.

**Outputs:** `value: float`

---

#### `GetAnimVisualLength(animName: str) -> float`

Original: `Source.NodeFunctions.Utils.GetAnimVisualLength`

Get the visual duration of an animation in seconds, excluding sound track length.

**Parameters**
- `animName` – Name of the animation asset.

**Outputs:** `value: float`

---

#### `SUPER(obj: object) -> object`

Original: `Source.NodeFunctions.Utils.SUPER`

Call the parent implementation for the current method.

**Parameters**
- `obj` – The object instance calling super.

**Outputs:** `value: object`

---

#### `SELF() -> object`

Original: `Source.NodeFunctions.Utils.SELF`

Get the object instance that owns the current graph.

**Outputs:** `value: object`

---

#### `GetAttrRef(obj: object, attrName: str) -> Any`

Original: `Source.NodeFunctions.Utils.GetAttrRef`

Get a readable/writable reference to an object attribute.

**Parameters**
- `obj` – The object to get the reference from.
- `attrName` – Name of the attribute.

**Outputs:** `value: object`

---

#### `GetAttr(obj: object, attrName: str) -> Any`

Original: `Source.NodeFunctions.Utils.GetAttr`

Read an attribute value from an object.

**Parameters**
- `obj` – The object to read from.
- `attrName` – Name of the attribute.

**Outputs:** `value: object`

---

#### `SetAttr(obj: object, attrName: str, value: Any) -> None`

Original: `Source.NodeFunctions.Utils.SetAttr`

Set an attribute value on an object.

**Parameters**
- `obj` – The object to write to.
- `attrName` – Name of the attribute.
- `value` – New attribute value.

**Exec Outputs:** `default`

---

#### `GetScene() -> Optional[SceneBase]`

Original: `Source.NodeFunctions.Utils.GetScene`

Get the current running scene.

**Outputs:** `value: SceneBase`

---

#### `IsValidValue(value: Any = None) -> bool`

Original: `Source.NodeFunctions.Utils.IsValidValue`

Check whether the value is not `None`.

**Parameters**
- `value` – Value to check.

**Outputs:** `value: bool`

---

#### `RunCommonFunction(commonFunctionName: str) -> Any`

Original: `Source.NodeFunctions.Utils.RunCommonFunction`

Execute a common function graph by name.

**Parameters**
- `commonFunctionName` – Name of the common function to execute.

**Exec Outputs:** `default`

---

#### `RegisterEventBus(key: str, obj: object, functionName: str) -> None`

Original: `Source.NodeFunctions.Utils.RegisterEventBus`

Subscribe an object's event or method to the shared EventBus.

**Parameters**
- `key` - EventBus key to subscribe to.
- `obj` - Target object that owns the event or method.
- `functionName` - Name of the event or method to invoke.

**Exec Outputs:** `default`

---

#### `RegisterEventBusEvent(key: str, obj: object, eventName: str) -> None`

Original: `Source.NodeFunctions.Utils.RegisterEventBusEvent`

Subscribe an object's blueprint event to the shared EventBus.

EventBus payload is ignored; the blueprint event is invoked without arguments.

**Parameters**
- `key` - EventBus key to subscribe to.
- `obj` - Target object that owns the blueprint event.
- `eventName` - Blueprint event name to invoke.

**Exec Outputs:** `default`

---

#### `UnregisterEventBus(key: str) -> bool`

Original: `Source.NodeFunctions.Utils.UnregisterEventBus`

Unsubscribe handlers from the shared EventBus by key.

**Parameters**
- `key` - EventBus key subscribed to.

**Outputs:** `value: bool`

**Exec Outputs:** `default`

---

#### `UnregisterEventBusEvent(key: str, obj: Optional[object] = None) -> bool`

Original: `Source.NodeFunctions.Utils.UnregisterEventBusEvent`

Unsubscribe blueprint event handlers from the shared EventBus.

**Parameters**
- `key` - EventBus key subscribed to.
- `obj` - Optional target object. If provided, only that object's handler is removed.

**Outputs:** `value: bool`

**Exec Outputs:** `default`

---

#### `TriggerEventBus(key: str, kwargs: Optional[Dict[str, Any]] = None) -> None`

Original: `Source.NodeFunctions.Utils.TriggerEventBus`

Post an EventBus event with keyword arguments.

**Parameters**
- `key` - EventBus key to trigger.
- `kwargs` - Keyword arguments passed to registered handlers.

**Exec Outputs:** `default`

---

#### `TriggerBlueprintEvent(obj: object, eventName: str) -> None`

Original: `Source.NodeFunctions.Utils.TriggerBlueprintEvent`

Trigger a blueprint event on an object without arguments.

**Parameters**
- `obj` - Target object that owns the blueprint event.
- `eventName` - Blueprint event name to invoke.

**Exec Outputs:** `default`

---

#### `BackToTitle() -> None`

Original: `Source.NodeFunctions.Utils.BackToTitle`

Return to the title scene after this command.

**Exec Outputs:** `default`

---

#### `Print(message: Any = "") -> None`

Original: `Source.NodeFunctions.Utils.Print`

Print a message in the game console.

**Parameters**
- `message` – Message to print.

**Exec Outputs:** `default`

---

#### `EXEC(script: str = "") -> None`

Original: `Source.NodeFunctions.Utils.EXEC`

Execute a Python script string.

**Parameters**
- `script` – Python script to execute.

**Exec Outputs:** `default`
