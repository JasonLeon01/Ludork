# 02. Math

Source file: `Sample/Source/NodeFunctions/Math.py`

#### `BuildVector2f(x: float = 0.0, y: float = 0.0) -> Vector2f`

Original: `Source.NodeFunctions.Math.BuildVector2f`

Build a `Vector2f` from x and y.

**Parameters**
- `x` – X component.
- `y` – Y component.

**Outputs:** `value: Vector2f`

---

#### `BuildVector2i(x: int = 0, y: int = 0) -> Vector2i`

Original: `Source.NodeFunctions.Math.BuildVector2i`

Build a `Vector2i` from x and y.

**Parameters**
- `x` – X component.
- `y` – Y component.

**Outputs:** `value: Vector2i`

---

#### `BuildVector2u(x: int = 0, y: int = 0) -> Vector2u`

Original: `Source.NodeFunctions.Math.BuildVector2u`

Build a `Vector2u` from x and y.

**Parameters**
- `x` – X component.
- `y` – Y component.

**Outputs:** `value: Vector2u`

---

#### `BuildVector3f(x: float = 0.0, y: float = 0.0, z: float = 0.0) -> Vector3f`

Original: `Source.NodeFunctions.Math.BuildVector3f`

Build a `Vector3f` from x, y, z.

**Parameters**
- `x` – X component.
- `y` – Y component.
- `z` – Z component.

**Outputs:** `value: Vector3f`

---

#### `BuildVector3i(x: int = 0, y: int = 0, z: int = 0) -> Vector3i`

Original: `Source.NodeFunctions.Math.BuildVector3i`

Build a `Vector3i` from x, y, z.

**Parameters**
- `x` – X component.
- `y` – Y component.
- `z` – Z component.

**Outputs:** `value: Vector3i`

---

#### `IsNearZero(num: Union[int, float], epsilon: float = 0.1) -> bool`

Original: `Source.NodeFunctions.Math.IsNearZero`

Check whether a number is near `0` (within epsilon).

**Parameters**
- `num` – Number to test.
- `epsilon` – Tolerance threshold (default `0.1`).

**Outputs:** `value: bool`

---

#### `IsVector2NearZero(v: Vector2f, epsilon: float = 0.1) -> bool`

Original: `Source.NodeFunctions.Math.IsVector2NearZero`

Check whether a `Vector2` is near the zero vector (within epsilon).

**Parameters**
- `v` – Vector to test.
- `epsilon` – Tolerance threshold (default `0.1`).

**Outputs:** `value: bool`

---

#### `IsVector3NearZero(v: Vector3f, epsilon: float = 0.1) -> bool`

Original: `Source.NodeFunctions.Math.IsVector3NearZero`

Check whether a `Vector3` is near the zero vector (within epsilon).

**Parameters**
- `v` – Vector to test.
- `epsilon` – Tolerance threshold (default `0.1`).

**Outputs:** `value: bool`

---

#### `Vector2fRound(v: Vector2f) -> Vector2f`

Original: `Source.NodeFunctions.Math.Vector2fRound`

Round each component of a `Vector2`.

**Parameters**
- `v` – Vector to round.

**Outputs:** `value: Vector2f`

---

#### `Vector2fFloor(v: Vector2f) -> Vector2f`

Original: `Source.NodeFunctions.Math.Vector2fFloor`

Floor each component of a `Vector2`.

**Parameters**
- `v` – Vector to floor.

**Outputs:** `value: Vector2f`

---

#### `Vector2fCeil(v: Vector2f) -> Vector2f`

Original: `Source.NodeFunctions.Math.Vector2fCeil`

Ceil each component of a `Vector2`.

**Parameters**
- `v` – Vector to ceil.

**Outputs:** `value: Vector2f`

---

#### `ToVector2f(v: Union[Vector2i, Vector2u]) -> Vector2f`

Original: `Source.NodeFunctions.Math.ToVector2f`

Convert `Vector2i` or `Vector2u` to `Vector2f`.

**Parameters**
- `v` – Vector to convert.

**Outputs:** `value: Vector2f`

---

#### `ToVector2i(v: Union[Vector2f, Vector2u]) -> Vector2i`

Original: `Source.NodeFunctions.Math.ToVector2i`

Convert `Vector2f` or `Vector2u` to `Vector2i`.

**Parameters**
- `v` – Vector to convert.

**Outputs:** `value: Vector2i`

---

#### `ToVector2u(v: Union[Vector2f, Vector2i]) -> Vector2u`

Original: `Source.NodeFunctions.Math.ToVector2u`

Convert `Vector2f` or `Vector2i` to `Vector2u`.

**Parameters**
- `v` – Vector to convert.

**Outputs:** `value: Vector2u`

---

#### `ToVector3f(v: Vector3i) -> Vector3f`

Original: `Source.NodeFunctions.Math.ToVector3f`

Convert `Vector3i` to `Vector3f`.

**Parameters**
- `v` – Vector to convert.

**Outputs:** `value: Vector3f`

---

#### `ToVector3i(v: Vector3f) -> Vector3i`

Original: `Source.NodeFunctions.Math.ToVector3i`

Convert `Vector3f` to `Vector3i`.

**Parameters**
- `v` – Vector to convert.

**Outputs:** `value: Vector3i`

---

#### `ToIntRect(x: int = 0, y: int = 0, width: int = 32, height: int = 32) -> IntRect`

Original: `Source.NodeFunctions.Math.ToIntRect`

Build an `IntRect` from x, y, width, height.

**Parameters**
- `x` – Left position.
- `y` – Top position.
- `width` – Width.
- `height` – Height.

**Outputs:** `value: IntRect`

---

#### `ToFloatRect(x: float = 0.0, y: float = 0.0, width: float = 32.0, height: float = 32.0) -> FloatRect`

Original: `Source.NodeFunctions.Math.ToFloatRect`

Build a `FloatRect` from x, y, width, height.

**Parameters**
- `x` – Left position.
- `y` – Top position.
- `width` – Width.
- `height` – Height.

**Outputs:** `value: FloatRect`

---

#### `Clamp(value=0.0, min_val=0.0, max_val=1.0) -> float`

Original: `Source.NodeFunctions.Math.Clamp`

Clamp a value between min and max.

**Parameters**
- `value` – Value to clamp.
- `min_val` – Minimum bound.
- `max_val` – Maximum bound.

**Outputs:** `value: float`

---

#### `Lerp(a: float = 0.0, b: float = 1.0, t: float = 0.5) -> float`

Original: `Source.NodeFunctions.Math.Lerp`

Linear interpolation between `a` and `b` by factor `t`.

**Parameters**
- `a` – Start value.
- `b` – End value.
- `t` – Interpolation factor (0.0–1.0).

**Outputs:** `value: float`

---

#### `Abs(value: Union[int, float] = 0) -> Union[int, float]`

Original: `Source.NodeFunctions.Math.Abs`

Get the absolute value.

**Parameters**
- `value` – Input number.

**Outputs:** `value: Union[int, float]`

---

#### `ToInt(value: Union[int, float] = 0) -> int`

Original: `Source.NodeFunctions.Math.ToInt`

Convert a number to `int`.

**Parameters**
- `value` – Number to convert.

**Outputs:** `value: Union[int, float]`

---

#### `ToFloat(value: Union[int, float] = 0) -> float`

Original: `Source.NodeFunctions.Math.ToFloat`

Convert a number to `float`.

**Parameters**
- `value` – Number to convert.

**Outputs:** `value: Union[int, float]`

---

#### `Max(values: List[Any] = None) -> Any`

Original: `Source.NodeFunctions.Math.Max`

Get the maximum value in a list.

**Parameters**
- `values` – List of values to compare.

**Outputs:** `value: List[Any]`

---

#### `Min(values: List[Any] = None) -> Any`

Original: `Source.NodeFunctions.Math.Min`

Get the minimum value in a list.

**Parameters**
- `values` – List of values to compare.

**Outputs:** `value: List[Any]`

---

#### `Sqrt(value: Union[int, float] = 0) -> Union[int, float]`

Original: `Source.NodeFunctions.Math.Sqrt`

Compute the square root.

**Parameters**
- `value` – Input number.

**Outputs:** `value: Union[int, float]`

---

#### `Pow(base: Union[int, float] = 1, exp: Union[int, float] = 2) -> Union[int, float]`

Original: `Source.NodeFunctions.Math.Pow`

Compute `base` raised to `exp`.

**Parameters**
- `base` – Base value.
- `exp` – Exponent.

**Outputs:** `value: Union[int, float]`

---

#### `Vector2Distance(v1: Union[Vector2f, Vector2i, Vector2u], v2: Union[Vector2f, Vector2i, Vector2u]) -> float`

Original: `Source.NodeFunctions.Math.Vector2Distance`

Compute distance between two 2D vectors.

**Parameters**
- `v1` – First vector.
- `v2` – Second vector.

**Outputs:** `value: float`

---

#### `Vector3Distance(v1: Vector3f, v2: Vector3f) -> float`

Original: `Source.NodeFunctions.Math.Vector3Distance`

Compute distance between two 3D vectors.

**Parameters**
- `v1` – First vector.
- `v2` – Second vector.

**Outputs:** `value: float`

---

#### `Vector2Dot(v1: Union[Vector2f, Vector2i, Vector2u], v2: Union[Vector2f, Vector2i, Vector2u]) -> float`

Original: `Source.NodeFunctions.Math.Vector2Dot`

Compute dot product for 2D vectors.

**Parameters**
- `v1` – First vector.
- `v2` – Second vector.

**Outputs:** `value: float`

---

#### `Vector3Dot(v1: Union[Vector3f, Vector3i], v2: Union[Vector3f, Vector3i]) -> float`

Original: `Source.NodeFunctions.Math.Vector3Dot`

Compute dot product for 3D vectors.

**Parameters**
- `v1` – First vector.
- `v2` – Second vector.

**Outputs:** `value: float`

---

#### `Vector2Cross(v1: Union[Vector2f, Vector2i, Vector2u], v2: Union[Vector2f, Vector2i, Vector2u]) -> Union[float, int]`

Original: `Source.NodeFunctions.Math.Vector2Cross`

Compute cross product for 2D vectors.

**Parameters**
- `v1` – First vector.
- `v2` – Second vector.

**Outputs:** `value: Union[float, int]`

---

#### `Vector3Cross(v1: Union[Vector3f, Vector3i], v2: Union[Vector3f, Vector3i]) -> Union[Vector3f, Vector3i]`

Original: `Source.NodeFunctions.Math.Vector3Cross`

Compute cross product for 3D vectors.

**Parameters**
- `v1` – First vector.
- `v2` – Second vector.

**Outputs:** `value: Union[Vector3f, Vector3i]`

---

#### `Vector2Length(v: Vector2f) -> float`

Original: `Source.NodeFunctions.Math.Vector2Length`

Compute length of a 2D vector.

**Parameters**
- `v` – Input vector.

**Outputs:** `value: float`

---

#### `Vector3Length(v: Vector3f) -> float`

Original: `Source.NodeFunctions.Math.Vector3Length`

Compute length of a 3D vector.

**Parameters**
- `v` – Input vector.

**Outputs:** `value: float`

---

#### `Vector2LengthSquared(v: Union[Vector2f, Vector2i, Vector2u]) -> Union[float, int]`

Original: `Source.NodeFunctions.Math.Vector2LengthSquared`

Compute squared length of a 2D vector.

**Parameters**
- `v` – Input vector.

**Outputs:** `value: Union[float, int]`

---

#### `Vector3LengthSquared(v: Union[Vector3f, Vector3i]) -> Union[float, int]`

Original: `Source.NodeFunctions.Math.Vector3LengthSquared`

Compute squared length of a 3D vector.

**Parameters**
- `v` – Input vector.

**Outputs:** `value: Union[float, int]`

---

#### `Vector2Normalized(v: Vector2f) -> Vector2f`

Original: `Source.NodeFunctions.Math.Vector2Normalized`

Normalize a 2D vector.

**Parameters**
- `v` – Vector to normalize.

**Outputs:** `value: Vector2f`

---

#### `Vector3Normalized(v: Vector3f) -> Vector3f`

Original: `Source.NodeFunctions.Math.Vector3Normalized`

Normalize a 3D vector.

**Parameters**
- `v` – Vector to normalize.

**Outputs:** `value: Vector3f`

---

#### `GetAngle(v: Vector2f) -> Angle`

Original: `Source.NodeFunctions.Math.GetAngle`

Get the angle of a 2D vector.

**Parameters**
- `v` – Input vector.

**Outputs:** `value: Angle`

---

#### `GetAngleTo(v1: Vector2f, v2: Vector2f) -> Angle`

Original: `Source.NodeFunctions.Math.GetAngleTo`

Get the angle from `v1` to `v2`.

**Parameters**
- `v1` – Start vector.
- `v2` – Target vector.

**Outputs:** `value: Angle`

---

#### `AsDegrees(angle: Angle) -> float`

Original: `Source.NodeFunctions.Math.AsDegrees`

Convert an `Angle` to degrees.

**Parameters**
- `angle` – Angle to convert.

**Outputs:** `value: float`

---

#### `AsRadians(angle: Angle) -> float`

Original: `Source.NodeFunctions.Math.AsRadians`

Convert an `Angle` to radians.

**Parameters**
- `angle` – Angle to convert.

**Outputs:** `value: float`

---

#### `Vector2ComponentWiseDiv(v: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i], div: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]) -> Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]`

Original: `Source.NodeFunctions.Math.Vector2ComponentWiseDiv`

Divide vectors component-wise.

**Parameters**
- `v` – Numerator vector.
- `div` – Divisor vector.

**Outputs:** `value: Union[Vector2f, Vector2i, Vector2u]`

---

#### `Vector2ComponentWiseMul(v: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i], mul: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]) -> Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]`

Original: `Source.NodeFunctions.Math.Vector2ComponentWiseMul`

Multiply vectors component-wise.

**Parameters**
- `v` – First vector.
- `mul` – Second vector.

**Outputs:** `value: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]`

---

#### `Vector2Perpendicular(v: Union[Vector2f, Vector2i, Vector2u]) -> Union[Vector2f, Vector2i, Vector2u]`

Original: `Source.NodeFunctions.Math.Vector2Perpendicular`

Get a vector perpendicular to the given 2D vector.

**Parameters**
- `v` – Input vector.

**Outputs:** `value: Union[Vector2f, Vector2i, Vector2u]`

---

#### `Vector2ProjectedOnto(v: Vector2f, axis: Vector2f) -> Vector2f`

Original: `Source.NodeFunctions.Math.Vector2ProjectedOnto`

Project a vector onto an axis.

**Parameters**
- `v` – Vector to project.
- `axis` – Axis vector to project onto.

**Outputs:** `value: Vector2f`

---

#### `Vector2RotatedBy(v: Vector2f, phi: Angle) -> Vector2f`

Original: `Source.NodeFunctions.Math.Vector2RotatedBy`

Rotate a 2D vector by an angle.

**Parameters**
- `v` – Vector to rotate.
- `phi` – Rotation angle.

**Outputs:** `value: Vector2f`

---

#### `DegreesToAngle(degrees_: float = 0.0) -> Angle`

Original: `Source.NodeFunctions.Math.DegreesToAngle`

Convert a degrees value to `Angle`.

**Parameters**
- `degrees_` – Angle in degrees.

**Outputs:** `value: Angle`

---

#### `RadiansToAngle(radians_: float = 0.0) -> Angle`

Original: `Source.NodeFunctions.Math.RadiansToAngle`

Convert a radians value to `Angle`.

**Parameters**
- `radians_` – Angle in radians.

**Outputs:** `value: float`

---

#### `RandomInt(min_val: int = 0, max_val: int = 100) -> int`

Original: `Source.NodeFunctions.Math.RandomInt`

Generate a random int in range.

**Parameters**
- `min_val` – Minimum value (inclusive).
- `max_val` – Maximum value (inclusive).

**Outputs:** `value: int`

---

#### `RandomFloat(min_val: float = 0.0, max_val: float = 1.0) -> float`

Original: `Source.NodeFunctions.Math.RandomFloat`

Generate a random float in range.

**Parameters**
- `min_val` – Minimum value (inclusive).
- `max_val` – Maximum value (inclusive).

**Outputs:** `value: float`

---

#### `GCD(a: int = 1, b: int = 1) -> int`

Original: `Source.NodeFunctions.Math.GCD`

Compute greatest common divisor.

**Parameters**
- `a` – First integer.
- `b` – Second integer.

**Outputs:** `value: int`

---

#### `LCM(a: int = 1, b: int = 1) -> int`

Original: `Source.NodeFunctions.Math.LCM`

Compute least common multiple.

**Parameters**
- `a` – First integer.
- `b` – Second integer.

**Outputs:** `value: int`

---

#### `ADD(a: Any = 0, b: Any = 0) -> Any`

Original: `Source.NodeFunctions.Math.ADD`

Compute `a + b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: Any`

---

#### `SUB(a: Any = 0, b: Any = 0) -> Any`

Original: `Source.NodeFunctions.Math.SUB`

Compute `a - b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: Any`

---

#### `MUL(a: Any = 1, b: Any = 1) -> Any`

Original: `Source.NodeFunctions.Math.MUL`

Compute `a * b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: Any`

---

#### `DIV(a: Any = 1, b: Any = 1) -> Any`

Original: `Source.NodeFunctions.Math.DIV`

Compute `a / b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: Any`

---

#### `MOD(a: Any = 0, b: Any = 1) -> Any`

Original: `Source.NodeFunctions.Math.MOD`

Compute `a % b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: Any`

---

#### `POW(a: Any = 1, b: Any = 1) -> Any`

Original: `Source.NodeFunctions.Math.POW`

Compute `a ** b`.

**Parameters**
- `a` – Base.
- `b` – Exponent.

**Outputs:** `value: Any`

---

#### `EQUALS(a: Any = 0, b: Any = 0) -> bool`

Original: `Source.NodeFunctions.Math.EQUALS`

Check whether `a` equals `b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: bool`

---

#### `NOT_EQUALS(a: Any = 0, b: Any = 0) -> bool`

Original: `Source.NodeFunctions.Math.NOT_EQUALS`

Check whether `a` does not equal `b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: bool`

---

#### `LESS(a: Any = 0, b: Any = 0) -> bool`

Original: `Source.NodeFunctions.Math.LESS`

Check whether `a` is less than `b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: bool`

---

#### `LESS_EQUALS(a: Any = 0, b: Any = 0) -> bool`

Original: `Source.NodeFunctions.Math.LESS_EQUALS`

Check whether `a` is less than or equal to `b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: bool`

---

#### `GREATER(a: Any = 0, b: Any = 0) -> bool`

Original: `Source.NodeFunctions.Math.GREATER`

Check whether `a` is greater than `b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: bool`

---

#### `GREATER_EQUALS(a: Any = 0, b: Any = 0) -> bool`

Original: `Source.NodeFunctions.Math.GREATER_EQUALS`

Check whether `a` is greater than or equal to `b`.

**Parameters**
- `a` – Left operand.
- `b` – Right operand.

**Outputs:** `value: bool`

---

#### `AND(a: bool = False, b: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.AND`

Boolean AND.

**Parameters**
- `a` – First operand.
- `b` – Second operand.

**Outputs:** `value: bool`

---

#### `OR(a: bool = False, b: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.OR`

Boolean OR.

**Parameters**
- `a` – First operand.
- `b` – Second operand.

**Outputs:** `value: bool`

---

#### `NOT(a: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.NOT`

Boolean NOT.

**Parameters**
- `a` – Operand.

**Outputs:** `value: bool`

---

#### `XOR(a: bool = False, b: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.XOR`

Boolean XOR.

**Parameters**
- `a` – First operand.
- `b` – Second operand.

**Outputs:** `value: bool`

---

#### `NAND(a: bool = False, b: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.NAND`

Boolean NAND.

**Parameters**
- `a` – First operand.
- `b` – Second operand.

**Outputs:** `value: bool`

---

#### `NOR(a: bool = False, b: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.NOR`

Boolean NOR.

**Parameters**
- `a` – First operand.
- `b` – Second operand.

**Outputs:** `value: bool`

---

#### `XNOR(a: bool = False, b: bool = False) -> bool`

Original: `Source.NodeFunctions.Math.XNOR`

Boolean XNOR.

**Parameters**
- `a` – First operand.
- `b` – Second operand.

**Outputs:** `value: bool`

---

#### `IADD(a: Any, b: Any = 1) -> Any`

Original: `Source.NodeFunctions.Math.IADD`

Apply `+=` to a reference/variable.

**Parameters**
- `a` – Target reference.
- `b` – Value to add.

**Exec Outputs:** `default`

---

#### `ISUB(a: Any, b: Any = 1) -> Any`

Original: `Source.NodeFunctions.Math.ISUB`

Apply `-=` to a reference/variable.

**Parameters**
- `a` – Target reference.
- `b` – Value to subtract.

**Exec Outputs:** `default`

---

#### `IMUL(a: Any, b: Any = 2) -> Any`

Original: `Source.NodeFunctions.Math.IMUL`

Apply `*=` to a reference/variable.

**Parameters**
- `a` – Target reference.
- `b` – Multiplier.

**Exec Outputs:** `default`

---

#### `IDIV(a: Any, b: Any = 2) -> Any`

Original: `Source.NodeFunctions.Math.IDIV`

Apply `/=` to a reference/variable.

**Parameters**
- `a` – Target reference.
- `b` – Divisor.

**Exec Outputs:** `default`

---

#### `IMOD(a: Any, b: Any = 2) -> Any`

Original: `Source.NodeFunctions.Math.IMOD`

Apply `%=` to a reference/variable.

**Parameters**
- `a` – Target reference.
- `b` – Modulus.

**Exec Outputs:** `default`

---

#### `IPOW(a: Any, b: Any = 2) -> Any`

Original: `Source.NodeFunctions.Math.IPOW`

Apply `**=` to a reference/variable.

**Parameters**
- `a` – Target reference.
- `b` – Exponent.

**Exec Outputs:** `default`
