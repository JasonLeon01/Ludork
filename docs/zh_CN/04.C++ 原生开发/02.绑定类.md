# 绑定类

## 目标

将一个 C++20 类一致地暴露给原生 Lua、LuaLS 与 Blueprint metadata catalogue。

## 前置条件

- 已确定所属模块与依赖方向。
- 该类在所属模块下具有公开头文件与实现文件。

## 步骤

### 1. 声明类

```cpp
#pragma once
#include <BindAnnotations.hpp>
#include <string>

BIND_CLASS()
class Counter {
public:
  BIND_INIT()
  explicit Counter(int initial = 0);

  BIND_PROPERTY()
  int value = 0;

  BIND_METHOD(Pure = true)
  int getValue() const;

  BIND_METHOD()
  void add(int amount);
};
```

把头文件放在所属模块的 `include/` 目录树，把实现放在 `src/` 目录树：

```cpp
#include "Counter.hpp"

Counter::Counter(int initial) : value(initial) {}

int Counter::getValue() const {
  return value;
}

void Counter::add(int amount) {
  value += amount;
}
```

模块的 CMake 文件会自动发现 `include/**/*.hpp` 与 `src/**/*.cpp`，不需要逐文件修改源文件列表。重新配置或构建后，bindgen 会生成 `<Module>.<NativeClass>.auto.cpp`、该模块唯一的 `<Module>.stub.auto.cpp`、`Scripts/stub/<Module>.d.lua` 和对应根 metadata。每类文件名使用原生 C++ 类名，不受 `BIND_CLASS` 指定的 Lua 名影响。对于放在 `Engine` 的 `Counter`，类源码是 `Engine.Counter.auto.cpp`，Lua 可这样调用：

```lua
local Engine = require("Engine")
local Logging = require("Global.Utils.Logging")

local counter = Engine.Counter.new(4)
counter:add(3)
Logging.info(counter:getValue())
```

模块根是该类唯一的公开 runtime 位置。`name` 用于选择该根字段的
Lua 标识符。不得为同一个原生类再创建第二条 table 路径。

`BIND_CLASS(callbacks = true)` 通过 Lua 派发当前类的非 static 虚绑定方法。opt-in 不继承，但基类已选择 callback 的 override 仍有效。

`BIND_METHOD(callback = false)` 排除当前类的方法，但不移除继承 callback。选择与 metadata 和 event 独立；不支持字符串清单或非 virtual 构造 callback。

Bindgen 会从 C++ 声明识别转换基类。`cast_bases = {"BaseA", "BaseB"}` 用于补充祖先，或在 `bind_bases = false` 时提供祖先；它必须是有序的非空类型名列表，不能写成逗号分隔字符串。

所有逻辑公开的 `BIND_METHOD` 默认进入编辑器 metadata。内部方法或次要适配重载若需要继续供原生 Lua 与 LuaLS 使用、但不应生成第二个 Blueprint 节点，应标记为 `metadata = false`：

```cpp
BIND_METHOD(metadata = false)
void _updateCache();
```

该选项不会删除运行时注册或 LuaLS stub。同一类型中两个未排除的方法不能公开相同的 metadata 名。

### 2. 声明属性

`BIND_PROPERTY()` 公开数据成员。安全的布尔、数值、字符串和 `{}` 初始化器可以生成 metadata 默认值。显式默认值使用注解原生的纯数据语法。无法从类型推导的属性编辑器语义写在 `BIND_PROPERTY(meta(...))` 内。

```cpp
BIND_PROPERTY(
    default = {0.0, 0.0})
sf::Vector2f defaultOrigin{0.0f, 0.0f};
```

位置项组成数组，命名项组成对象。这是 bindgen 读取的注解语法，不需要构成合法 C++ 表达式。通过 `BIND_METHOD(property = ...)` 声明的 getter 属性也使用相同的单数 `default` 语法。

`meta(...)` 内的结构化纯数据使用相同的花括号 DSL，两种形式都可以按相同规则嵌套：

```cpp
BIND_PROPERTY(
    meta(Rely = {source = "autoSound", op = "!=", value = ""}))
float autoSoundInterval = 0.0f;

BIND_METHOD(defaults = {{}},
            meta(MoveRouteVars = {"route"}),
            outpins(default = nil))
void setRoute(
    const std::optional<std::vector<sf::Vector2i>>& route = {});
```

其中布尔值使用小写 `true`、`false`。花括号语法、协议 `nil` 与带引号 JSON 的拒绝规则见[宏参考](<04.宏参考.md>)。

普通属性和方法不声明 `DisplayName` 或 `DisplayDesc`。编辑器自动格式化标识符，因此 `defaultOrigin` 显示为 `Default Origin`，C++ 与 Lua 成员名仍为 `defaultOrigin`。真正的运算符别名可以使用 `+` 这样的直接显示字面量，但不得使用 `LOC(...)` 或默认说明。

无参 getter 可公开为属性：

```cpp
BIND_METHOD(property = "volume", setter = "setVolume")
float getVolume() const;

void setVolume(float value);
```

默认使用 getter 返回类型作为属性类型，必要时用 `type` 覆盖。这种写法只公开一个属性，不公开 getter 或 setter 节点。

### 3. 检查构造与排除

`BIND_INIT` 标记构造函数。无需公开的声明不写绑定 marker；`BIND_METHOD(metadata = false)` 仍把方法保留在原生 Lua 与 LuaLS 中，只将其排除在 metadata 之外。类级值使用 `BIND_CLASS_PROPERTY`，模块级值使用 `BIND_MODULE_PROPERTY`。

### 4. 绑定 scoped enum

固定原生值集合需要与 Lua 共享时使用 `BIND_ENUM`：

```cpp
BIND_ENUM()
enum class WeatherType {
  NONE = 0,
  RAIN = 1,
};
```

Bindgen 生成 `Module.WeatherType` 及其 LuaLS 类型，不应再用模块属性重复枚举值。枚举值以底层整数跨越 Lua 边界，Blueprint metadata 使用 `int`。

## 限制

Bindgen 不会推断项目专用转换规则，也不会解析不完整的跨模块类型。Getter-backed property 只暴露一个属性，不额外生成 getter 与 setter 节点。

## 相关页面

- [生成 Metadata 与 Stub](<05.生成 Metadata 与 Stub.md>)
- [宏参考](<04.宏参考.md>)
