# 05. PlayerInfo

源文件：`Sample/Source/Infos/PlayerInfo.py`

## 类 `Source.Infos.PlayerInfo.PlayerInfo`

玩家数据层。按 ID 从 GeneralData 加载玩家身份信息。与 Actor 独立；通过 `Player` 上的多重继承桥接到场景实体。

**类变量**

- 此处未直接声明公共类变量。

**蓝图 API**

此类未声明蓝图可见方法。`name`、`desc` 等玩家身份字段通过 `Player` 上的 `PlayerInfoComponent` 编辑。
