# 12. GameMap

源文件：`Sample/Source/NodeFunctions/GameMap.py`

#### `GetActorByTag(tag: str) -> Optional[Actor]`

原始路径：`Source.NodeFunctions.GameMap.GetActorByTag`

在当前地图上按标签查找角色。

**参数**
- `tag` – 要搜索的标签。

**输出：** `actor: Optional[Actor]`

---

#### `GetAllActors() -> List[Actor]`

原始路径：`Source.NodeFunctions.GameMap.GetAllActors`

获取当前地图上所有图层的角色列表。

**输出：** `actors: List[Actor]`
