# Lua 基础语法

Ludork 内置 Lua 5.5。本页面向初次接触 Lua 的读者介绍这门语言。Ludork 在此基础上增加的原生能力另见 [Ludork Lua进阶](<04.Ludork Lua进阶.md>)，项目 Lua 能拿到的 SFML 类型见 [SFML 快速入门](<05.SFML 快速入门.md>)。

## 文件、注释与局部变量

Lua 文件使用 `.lua` 扩展名。`--` 开始单行注释，`--[[ ... ]]` 括住块注释。工作变量用 `local` 声明。未初始化的变量值为 `nil`。

```lua
-- 单行注释
local playerName = "Hero"
local level = 1
local alive = true
local target = nil
```

Lua 的值包括 `nil`、boolean、number、string、function、table、userdata 与 thread。`type(value)` 返回这些内建类别之一。由 table 或 userdata 表示的应用对象，仍然归入它底层的类别。

## 运算符

常用运算符如下：

| 类别 | 运算符 |
|---|---|
| 算术 | `+`、`-`、`*`、`/`、`//`、`%`、`^` |
| 比较 | `==`、`~=`、`<`、`<=`、`>`、`>=` |
| 逻辑 | `and`、`or`、`not` |
| 字符串 | `..` 拼接 |
| 长度 | `#value` |

Lua 只把 `nil` 与 `false` 当作假。`0`、空字符串、空 table 用作条件时都为真。

## 条件分支

```lua
if health <= 0 then
    state = "defeated"
elseif health < maxHealth then
    state = "hurt"
else
    state = "healthy"
end
```

需要把“缺失”与 `false`、`0` 或空值区分开时，应显式比较 `nil`。

## 表（table）

table 既能当数组，也能当字典或记录。数组索引从 1 开始。

```lua
local items = { "Key", "Potion" }
local player = {
    name = "Hero",
    hp = 100,
}

items[#items + 1] = "Gem"
player.hp = player.hp - 10
```

把普通 table 的键赋值为 `nil`，就等于删除该键。数组中要避免 `nil` 空洞，因为 `#`、`ipairs` 与数组辅助函数遇到第一个空洞就会停下。

## 循环

```lua
for index, item in ipairs(items) do
    useItem(index, item)
end

for key, value in pairs(player) do
    inspectValue(key, value)
end

local count = 3
while count > 0 do
    count = count - 1
end
```

稠密数组用 `ipairs` 遍历，字典用 `pairs` 遍历。数值型 `for` 循环会包含起点与终点，除非步长跳过最后一个值。

## 函数与方法

函数本身也是值，而且可以返回多个结果。实例方法用冒号语法定义，接收者会作为 `self` 传入。

```lua
local function clampHealth(value, maximum)
    return math.max(0, math.min(value, maximum))
end

local Character = {}

function Character:setHealth(value)
    self.health = clampHealth(value, self.maxHealth)
end

local value, found = findItem("Key")
```

`object:method(value)` 等价于 `object.method(object, value)`。定义和调用实例方法时都统一用冒号形式。

## 模块

模块就是一个把公开内容作为返回值导出的 Lua 文件。`require` 的路径以 `Scripts` 为根，用点号分隔目录层级，并且不带扩展名。

```lua
-- Scripts/Source/Score.lua
local Score = {}

function Score.Add(current, amount)
    return current + amount
end

return Score
```

```lua
local Score = require("Source.Score")

local total = Score.Add(10, 5)
```

先写完所有直接 `require`，再声明别名。不要创建目录级的 `init.lua` 聚合模块。

## 错误

必须成立的条件用 `assert(condition, message)` 检查，需要主动失败时用 `error(message)`。项目必需的数据必须在源头直接失败，而不是悄悄产生不完整的状态。

## 相关页面

- [Ludork Lua进阶](<04.Ludork Lua进阶.md>) — 类、项目真值、原生容器与 Standard 扩展。
- [Lua 运行时与模块](<../03.Lua 与蓝图脚本/01.Lua 运行时与模块.md>) — 项目模块顺序、文件分层与运行时契约。
- [默认模板概述](<../03.Lua 与蓝图脚本/05.Game 玩法/01.默认模板概述.md>) — Game 项目如何使用这些规则。
