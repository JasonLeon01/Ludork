# 蓝图、公共函数与 Actor

蓝图是 `Data/Blueprints` 下的 JSON 类定义。它选择一个原生或 Lua 父类，保存可编辑属性；处于图模式时还包含事件图。地图上放置的 Actor 实例引用这些类。

## 目标

创建一个图模式蓝图：设置类默认值、添加有效的事件图并放置实例，全程不把图模式与 Script Mixin 继承模式混用。

## 前置条件

- 用到的每个字段与节点，都有已生成或已加载的最新 metadata。

## 创建蓝图

使用 **游戏 → 新建蓝图**，或文件浏览器中的相应命令。选择一个最终继承 `BPBase` 的父类，再选择 `Data/Blueprints` 下的路径。编辑器打开该类时解析父类默认值与支持的事件。

## 类值与实例值

类属性为所有实例提供默认值，地图上的 Actor 可以覆盖其中单个值。组件由 metadata 声明，以结构化字段呈现。被 `InvalidVars` 隐藏的字段不算普通可编辑属性。

蓝图编辑器沿蓝图及 Lua／原生 metadata 类的完整继承链解析变量与组件声明。子类省略字段时，继续向上继承祖先类的值；子类显式设置的值优先。即使所有祖先类都没有提供默认值，可选组件仍可在 **组件** 中通过 **+** 添加，再双击编辑字段。没有默认值且尚未添加的组件不会写入新建蓝图的 JSON。

在蓝图编辑器与 **角色信息** 中，普通变量按灰色虚线框分组，排列在组件列表下方。每个框标明首次声明这些字段的类，把鼠标停在灰色标题上可查看完整类引用。继承来的字段只出现在它最初所在的分组里，即使子类覆盖了字段值也不变。Script Mixin 带来的字段与蓝图自定义字段，归到首次声明它们的那个 Script Mixin 或蓝图名下。

蓝图编辑器初始化时按字段所需的最小宽度设置变量栏，计算包含嵌套结构和完整数值的显示空间。增加的空间从右侧图与预览区域分配，窗口总宽度不变。拖动分隔条可以加宽变量栏，但不能缩窄到内容所需宽度以下。窗口容纳不下所需宽度时，变量栏保持在窗口内，通过横向滚动查看剩余内容。

当蓝图的 `ID` 选中某个通用数据类型时，编辑器会显示来自所选成员的只读 `attributes` 预览，该预览永远不会写入蓝图 JSON。运行时通过 `Source.Configs.GeneralDataTypes.Create` 构造生成的 Attribute Set，因此这些值要在通用数据中编辑。

## Actor 库与角色信息

Actor 库列出全部 Actor 蓝图，可按文本、顶层文件夹类别和范围筛选。**全部** 显示完整目录，**收藏** 显示加星条目，**最近使用** 保留最近选择或放置的 20 个条目。收藏保存在 `Main.proj` 的 `editor.actorFavorites` 下；在文件浏览器中移动或重命名蓝图时收藏会跟随更新，蓝图不存在后自动移除。右键单击条目可以打开或定位它的蓝图、切换收藏状态，或把条目从最近使用中移除。动态预览只为库视口当前可见的行播放。

选中地图上的 Actor 后，**角色信息** 显示它的蓝图引用以及 **打开** 和 **定位** 操作、网格位置 X/Y、tag 和公开的类字段。持续的聚焦编辑中，位置与文本的改动合并为一步撤销。覆盖过的字段旁有重置按钮，用于删除该实例的这项覆盖；**全部重置** 会删除该实例的全部类字段覆盖，重新显示继承自蓝图的默认值。Actor 所在图层被隐藏时，**角色信息** 仍可用于查看和跳转蓝图，但 tag、位置、重置操作与类字段都变为只读。

## 图模式

启用 **Script Mixin** 会隐藏右侧整个图与预览区域，变量区填满可用宽度；停用后立即恢复该区域和原先的分栏宽度。继承模式、撤销/重做和重新加载都会保持同样的布局，已有的图数据继续保留。

每个事件拥有自己的节点列表与连线。从选择器添加节点，连接执行 pin 与类型兼容的数据 pin，并为未连接的输入 pin 设置字面默认值。Pure 节点只有数据 pin；执行节点必须能从事件起点到达。

新建节点、粘贴节点和重复选区都使用鼠标在图中的当前位置。操作开始时，编辑器按当前的平移与缩放换算这个可见位置；鼠标不在图内时改用视口中心。重复的选区保留原有相对布局与内部连线，并把选区左上边界放到目标位置。

Windows 上的主修饰键是 `Ctrl`，macOS 上是 `Command`。

| 快捷键 | 操作 |
|---|---|
| `Ctrl/Command+N` | 在鼠标位置打开节点选择器 |
| `Ctrl/Command+A` | 选中当前图中全部可编辑节点 |
| `Ctrl/Command+C` | 复制选中节点及其内部连线 |
| `Ctrl/Command+V` | 在鼠标位置粘贴 |
| `Ctrl/Command+D` | 在鼠标位置重复选区 |
| `方向键` | 把选中的可编辑节点移动 1 个图单位 |
| `Delete` | 删除选中的可编辑节点 |

继承节点与事件参数节点都是虚拟节点，不参与全选、移动、重复和删除。参数编辑器或其他输入控件获得焦点时，图快捷键不会取代它们的键盘输入；只读图会禁用一切产生修改的快捷键。

公共函数提供可复用的图。把相同的节点序列复制进多个蓝图，不如改用公共函数。

通用数据的成员图是彼此独立、同步执行的 Ability。它的纯图编辑器使用该类型的事件列表，隐藏类属性并排除 latent 节点。运行时由 `Source.Gameplay.GeneralDataGraphAbility` 显式激活，并以自身的 `GameplayEventData` 作为图的 parent。

## 继承

子蓝图继承字段与行为。改动已保存的父类路径会连带改变继承字段、事件和兼容的组件数据，务必谨慎。同一条蓝图继承链必须自始至终使用图模式或 Script Mixin 模式，参见 [Script Mixin 编写工作流](<../03.Lua 与蓝图脚本/02.脚本 Mixin/01.编写工作流.md>)。

## 校验

校验检查父类解析、循环、图结构、事件起点节点、节点函数、连线、pin 与已保存的默认值。空的可选事件有效；格式错误或越界的连线无效。修正所有报错后再保存，然后运行一张有代表性的地图。

## 限制

校验无法修复指向已重命名运行时成员的引用，也无法证明每个可达的玩法状态都正确。

## 相关页面

- [事件、公共函数与蓝图流程](<../03.Lua 与蓝图脚本/03.蓝图脚本/01.执行流、事件与变量.md>)
- [Script Mixin 编写工作流](<../03.Lua 与蓝图脚本/02.脚本 Mixin/01.编写工作流.md>)
- [图块集、自动图块与地图](<03.图块集、自动图块与地图.md>)
