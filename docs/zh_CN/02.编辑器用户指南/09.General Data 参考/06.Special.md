# Special

来源：`Data/General/Special.json`

| 属性 | 值 |
|---|---|
| 关联运行时类型 | — |
| 成员事件 | — |
| 当前 Sample 成员 | `Poisoning`, `Weaken`, `Hard`, `Magic`, `MultiHit`, `Compete`, `Domain`, `Flank`, `Blockade`, `Reborn` |

## Schema

| Field | Type | Default | Description | Reference |
|---|---|---|---|---|
| `name` | `string` | `""` | — | `—` |
| `desc` | `string` | `""` | — | `—` |
| `icon` | `file` | `"Icons"` | — | `—` |
| `kwargs` | `dict` | `{}` | — | `—` |

## 编辑说明

应先添加字段，再大量创建成员。reference 控制选择器，但保存值仍使用声明的 JSON 形状。类型列出 events 时，每个成员可以在 `_graph` 下保存 graph；缺少 start node 表示该可选事件未实现。

`Reborn` 作为 Enemy special key 使用时，其 value 是原敌人被击败后要生成的非空 Blueprint 类路径。
