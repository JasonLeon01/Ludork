# General Data 参考

本节根据当前 Sample 的 `Data/General` schema 生成，记录 schema 字段、成员标识符、关联运行时类型和成员事件。具体值属于项目数据，不是引擎固定枚举。

每个类型使用 `params` 定义字段，使用 `members` 保存记录。`reference` 用于限定编辑器选择器；`linkedType` 把成员 graph 连接到运行时 metadata。花括号显示字符串归“插件系统与本地化”的工作流。
