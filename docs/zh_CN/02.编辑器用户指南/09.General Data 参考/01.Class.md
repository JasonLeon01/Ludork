# Class

来源：`Data/General/Class.json`

| 属性 | 值 |
|---|---|
| 关联运行时类型 | — |
| 成员事件 | — |
| 当前 Sample 成员 | `Warrior` |

## Schema

| Field | Type | Default | Description | Reference |
|---|---|---|---|---|
| `name` | `string` | `""` | — | `—` |
| `desc` | `string` | `""` | — | `—` |
| `slot` | `dict` | `{}` | — | `—` |

## 编辑说明

应先添加字段，再大量创建成员。reference 控制选择器，但保存值仍使用声明的 JSON 形状。类型列出 events 时，每个成员可以在 `_graph` 下保存 graph；缺少 start node 表示该可选事件未实现。
