# Engine：动画数据与图形

## 模块图形函数

| 名称 | 参数 | 返回 | 用途 |
|---|---|---|---|
| `BuildPixelGridVertices` | origin: sf.Vector2f; size: sf.Vector2u | sf.VertexArray | 仅供 Lua 使用且不生成 Blueprint 节点；每个像素构建六个三角形顶点。一个像素的所有顶点使用其中心纹理坐标；零尺寸返回空三角形数组，顶点数溢出时报错。 |
| `compressAnimation` | source: Engine.AnimationSourceData 或 nil; imageFormat: string = `"png"` | Engine.AnimationData | 压缩 `source.assets` 指定的帧图片；无资产根参数。 |

## AnimationKeyFrame

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `time` | `float` | `0` | — |
| `position` | `float[]` | — | — |
| `rotation` | `float` | `0` | — |
| `scale` | `float[]` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## AnimationSegment

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `type` | `string` | `"frame"` | — |
| `asset` | `int` | `-1` | — |
| `startFrame` | `{ "Engine", "AnimationKeyFrame" }` | — | — |
| `endFrame` | `{ "Engine", "AnimationKeyFrame" }` | — | — |
| `flipX` | `bool` | `false` | — |
| `originalDuration` | `float` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## AnimationTimeline

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `timeSegments` | `{ "Engine", "AnimationSegment[]" }` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## AnimationTimeTag

直接 metadata 基类：—

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `tag` | `string` | — | — |
| `time` | `float` | `0` | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## AnimationSourceData

直接 metadata 基类: —

非空 `assets` 条目与输出的 `AnimationSoundEntry.asset` 均保留大小写精确的完整 `/Game/Assets/...` 路径。

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `type` | `string` | — | — |
| `name` | `string` | — | — |
| `frameRate` | `int` | `0` | — |
| `frameCount` | `int` | `0` | — |
| `visualFrameCount` | `int` | — | — |
| `duration` | `float` | — | — |
| `visualDuration` | `float` | — | — |
| `timeTags` | `{ "Engine", "AnimationTimeTag[]" }` | — | — |
| `timeLines` | `{ "Engine", "AnimationTimeline[]" }` | — | — |
| `assets` | `string[]` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## AnimationSoundEntry

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `asset` | `string` | — | — |
| `startFrame` | `int` | `0` | — |
| `endFrame` | `int` | `0` | — |
| `originalDuration` | `float` | — | — |
| `stopAtEndFrame` | `bool` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## AnimationData

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `type` | `string` | `"compressedAnimation"` | — |
| `name` | `string` | — | — |
| `frameRate` | `int` | `30` | — |
| `frameCount` | `int` | `0` | — |
| `visualFrameCount` | `int` | — | — |
| `duration` | `float` | — | — |
| `visualDuration` | `float` | — | — |
| `timeTags` | `{ "Engine", "AnimationTimeTag[]" }` | — | — |
| `frameEncoding` | `string` | `"base64+zlib"` | — |
| `frames` | `any[]` | — | — |
| `sounds` | `{ "Engine", "AnimationSoundEntry[]" }` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## RectBase

直接 metadata 基类: —

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `render` | `function` | dst: sf.RenderTexture; edge: sf.RenderTexture; edgeSprite: sf.Sprite; backSprite: sf.Sprite; cachedCorners: sf.Texture[]; cachedEdges: sf.Texture[]; renderStates: sf.RenderStates | — | — |

## TileLayerGraphics

直接 metadata 基类: —

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `setTileColor` | `function` | x: int; y: int; color: sf.Color | — | — |
| `resetTileColor` | `function` | x: int; y: int | — | — |
| `floodFillTransparent` | `function` | startX: int; startY: int; color: sf.Color | return: sf.Vector2i[] | — |
| `get` | `function` | position: sf.Vector2i | return: int | Pure |
| `getAutoTileAt` | `function` | position: sf.Vector2i | return: { "Engine", "AutoTile" } | Pure |
| `isPassable` | `function` | position: sf.Vector2i | return: bool | Pure |
| `getMaterial` | `function` | position: sf.Vector2i | return: { "Engine", "Material" } | Pure |
| `getLightBlockMap` | `function` | — | return: float[][] | — |
| `getReflectionStrengthMap` | `function` | — | return: float[][] | — |
| `getIgnoreLightingMap` | `function` | — | return: float[][] | — |
| `updateAutoTileAnimation` | `function` | deltaTime: float; frameInterval: float | — | — |
