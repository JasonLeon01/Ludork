# GlobalCore：地图与雾效

## FogController

直接 metadata 基类: —

### 属性

未声明可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `applyFromMapData` | `function` | mapData: any | — | — |
| `clearFog` | `function` | — | — | — |
| `update` | `function` | deltaTime: float | — | — |
| `drawOverlay` | `function` | — | — | — |

`applyFromMapData` 将可选的 `fog` 字段读取为字符串，并将 `fogPower`、`fogOx`、`fogOy` 与 `fogDistort` 读取为可由 `float` 表示的有限数值。缺失或为 `nil` 的 `fog` 视为空字符串，缺失或为 `nil` 的数值字段视为 `0`。空白 `fog` 或小于等于 `0` 的 `fogPower` 会禁用雾效。`fogPower` 与 `fogDistort` 会向下取整并 clamp 到 `0..100`；`fogOx` 与 `fogOy` 是滚动速度。已提供的值必须符合声明类型：不接受数值字符串、标量到字符串的转换、非有限数值或超出 float 范围的数值，并会连同字段路径直接报错。

## PathResult

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `offsets` | `sf.Vector2i[]` | — | — |
| `points` | `sf.Vector2i[]` | — | — |
| `route` | `sf.Vector2i[]` | — | — |

### 函数与事件

未声明 Blueprint 函数或事件。

## GameMapBase

直接 metadata 基类: —

### 属性

未声明可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `generateDataFromMap` | `function` | size: sf.Vector2u; materialMap: any[][]; smooth: bool | return: sf.Texture | — |
| `findPathExt` | `function` | start: sf.Vector2i; goal: sf.Vector2i; size: sf.Vector2u; movingActor: { "Engine", "Actor" }; excludedAnchors: sf.Vector2i[] = {  } | return: { "GlobalCore", "PathResult" } | — |
| `getMaterialPropertyMapExt` | `function` | width: int; height: int; propertyName: string; invalidValue: any | return: any[][] | — |
| `rebuildPassabilityCache` | `function` | size: sf.Vector2u | return: bool[][] | — |
| `updateActorOccupancy` | `function` | actor: { "Engine", "Actor" } | — | — |
| `getActorsAt` | `function` | x: int; y: int | return: { "Engine", "Actor[]" } | — |
| `getActorsInRange` | `function` | x: int; y: int; radius: int | return: { "Engine", "Actor[]" } | — |
| `getCollisionAt` | `function` | x: int; y: int; selfActor: { "Engine", "Actor" } | return: { "Engine", "Actor[]" } | — |
| `getOverlapsAt` | `function` | x: int; y: int; selfActor: { "Engine", "Actor" } | return: { "Engine", "Actor[]" } | — |
| `setTilemap` | `function` | tilemap: { "Engine", "Tilemap" } | — | — |
| `syncActorsRef` | `function` | actors: any | — | — |

Runtime-only（`metadata = false`）方法包括稀疏区域就绪、返回 1-based index 的 `getSparseWorldRegionIndexAt(position)`、静态灯光 occupancy 与批量遮挡分析。

## WorldStreamingState

`WorldRegionState` 暴露 `Unloaded`、`Reading`、`Prepared`、`Active` 和 `Dormant`；`WorldRegionDemand` 暴露 `None`、`Prepared` 和 `Active`。`WorldStreamingState` 使用 1-based index，原生持有 demand/队列、Camera 排序（Active 优先，再按区域中心平方距离，最后用 region index 稳定打破平局）、last-used/缓存账本、淘汰及统计。其方法均为 `metadata = false`；Blueprint 使用带上下文的世界地图节点。

Lua 调用 `updateCameraCenter`/`updateDemand`，消费 `takeReadBatch`/`takePublishItem`，报告读/发布完成或取消，标记激活/停用/淘汰，并读取 `getEvictionList`/`getStats`；文件、builder 和 payload 仍由 Lua 持有。
