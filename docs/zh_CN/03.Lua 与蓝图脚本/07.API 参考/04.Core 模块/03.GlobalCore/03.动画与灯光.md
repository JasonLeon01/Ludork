# GlobalCore：动画与灯光

## AnimationPlayingSound

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `sound` | `sf.Sound` | — | — |
| `endFrame` | `int` | `-1` | — |

### 函数与事件

未声明 Blueprint 函数或事件。

## Animation

直接 metadata 基类: `{ "Engine", "AnimSprite" }`

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `soundEntries` | `{ "Engine", "AnimationSoundEntry[]" }` | — | — |
| `soundIndex` | `int` | `0` | — |
| `playingSounds` | `{ "GlobalCore", "AnimationPlayingSound[]" }` | — | — |

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `setData` | `function` | animationData: { "Engine", "AnimationData" } | — | — |
| `update` | `function` | deltaTime: float | — | — |
| `playSoundsUpToFrame` | `function` | frameIndex: int | — | — |
| `stopSoundsAtFrame` | `function` | frameIndex: int | — | — |

## Light

直接 metadata 基类: —

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `position` | `sf.Vector2f` | `{ [1] = 0, [2] = 0 }` | — |
| `colour` | `sf.Color` | `{ [1] = 255, [2] = 255, [3] = 255, [4] = 255 }` | — |
| `radius` | `float` | `256` | — |
| `intensity` | `float` | `1` | — |

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `asDict` | `function` | — | return: any | — |
| `fromDict` | `function` | data: any | return: { "GlobalCore", "Light" } | — |

