# Engine：交互与声明式 UI 控件

## TextBox

直接 metadata 基类：`{ "Engine", "ControlBase" }`、`{ "Engine", "FunctionalBase" }`

TextBox 是默认不处于编辑状态的单行 UTF-8 输入框。点击、触摸或确认操作会开始编辑，仅获得焦点不会。桌面端编辑支持闪烁光标、选区、横向滚动、按字素移动与删除、Home、End 以及剪贴板快捷键。

Enter 或 `finishEdit` 保留编辑后的值，Escape、已连接手柄的 B/Cancel 按钮或 `cancelEdit` 恢复激活时的值，两者都不会提交外层表单。失焦会保留已提交的文本并丢弃未完成的组合文本。隐藏、禁用、移除或销毁控件都会结束编辑。同一时刻只有一个 TextBox 处于编辑状态。

iOS、Android 和 HarmonyOS Mobile 使用系统输入对话框，HarmonyOS 2in1 使用桌面端编辑。原生的完成按钮更新文本，取消按钮恢复对话框打开前的值。应用进入后台会关闭会话，返回时不会自动重新打开。

| 方法 | 参数 | 返回 |
|---|---|---|
| `new` | size: sf.Vector2f; windowSkin: sf.Image; textConfig: Engine.PlainTextConfig; text: string = "" | TextBox |
| `getString` / `setString` | — / text: string | string / — |
| `isEditing` / `beginEdit` | — | bool |
| `finishEdit` / `cancelEdit` | — | — |
| `setInputDialogLabels` | title: string; done: string; cancel: string | — |
| `setOnTextChanged` | callback: function(text: string) or nil | — |
| `setOnEditingChanged` | callback: function(editing: bool) or nil | — |
| `getSize` / `resize` | — / size: sf.Vector2f | sf.Vector2f / — |
| `setWindowSkin` / `setTextConfig` | windowSkin: sf.Image / textConfig: Engine.PlainTextConfig | — |

`setOnTextChanged` 会上报已提交值的变化，`setString` 与取消回滚同样计入；输入法组合不会改变 `getString()`。换行和制表符会变成空格，其他控制字符会被移除。表单校验由 Controller 负责。

在 Windows 上，嵌入运行与独立窗口都支持字符输入和 IME 组合。嵌入运行时由编辑器持有 IME，并按游戏光标定位候选框。桥接连接断开时会结束编辑，已提交文本的处理规则与失焦相同。

## CheckBox

直接 metadata 基类：`{ "Engine", "ControlBase" }`、`{ "Engine", "FunctionalBase" }`

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `isChecked` | `function` | — | return: bool | Pure |
| `setChecked` | `function` | checked: bool | — | — |
| `toggle` | `function` | — | — | — |
| `setWindowSkin` | `function` | windowSkin: sf.Image | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `setOnCheckedChanged` | `function` | callback: function | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onConfirm` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onClick` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseButtonDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | return: bool | — |
| `onKeyDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## DropBox

直接 metadata 基类：`{ "Engine", "ControlBase" }`、`{ "Engine", "FunctionalBase" }`

### 属性

未声明任何可编辑属性。

### 浮层与尺寸测量

DropBox 有 Canvas 祖先时，展开的列表绘制在最外层 Canvas 的浮层中，位于该 Canvas 的普通内容和动画之后。因此它不受中间各层 Canvas 裁剪的限制，也不会改变父布局中其他控件的尺寸或位置。

展开列表的可见高度限制在根 Canvas 内容矩形与游戏屏幕的交集之内。完整列表在下方放得下时向下展开，在下方放不下而上方的空间足够时改为向上展开。两侧都放不下时，改用空间较大的一侧，并让列表在内部滚动。

`getSize()` 始终返回折叠状态的布局尺寸。`getLocalBounds()` 在折叠时返回该矩形，展开时返回它与可见列表的并集；向上展开会让局部 Y 坐标变为负数。继承的绝对边界和默认触摸命中范围都跟随这一可见范围。

### 列表滚动与选择

普通鼠标滚轮以行为单位滚动到目标位置，精确滚轮和移动端触摸拖动则直接应用各自的偏移量。这些输入只移动列表视口，不改变游标或已确认的选择，松开拖动也绝不会确认选项。方向键移动游标，并在需要时把它滚动到可见位置。鼠标悬停会预览可见选项，桌面端点击确认，移动端点击采用下文的两步确认。所有索引仍然对应完整的 `items` 数组。

### 移动端触摸确认

移动端展开的 DropBox 采用两步确认：点击未高亮的选项时，只移动高亮并播放游标音效，不改变 `selectedIndex`，也不触发选择回调。点击当前高亮项才会确认，包括展开时已高亮的选项。取消会丢弃预览，并保留原来的 `selectedIndex`。

### 选择回调

`setOnSelectedIndexChanged(callback)` 只在 `setSelectedIndex` 解析出不同的选择时触发，并传入新索引；在确认过程中，它先于确认回调执行。`setOnSelectionConfirmed(callback)` 在已展开且非空的 DropBox 确认时传入当前索引并触发，即使索引没有改变。通过代码选择或替换选项、取消，以及确认空列表都不会触发它。向任一 setter 传入 `nil` 都可清除对应回调。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `setWindowSkin` | `function` | windowSkin: sf.Image; repeated: bool = false | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `getItems` | `function` | — | return: string[] | Pure |
| `setItems` | `function` | items: string[] | — | — |
| `getSelectedIndex` | `function` | — | return: int | Pure |
| `setSelectedIndex` | `function` | index: int | — | — |
| `getSelectedItem` | `function` | — | return: string | Pure |
| `isExpanded` | `function` | — | return: bool | Pure |
| `setExpanded` | `function` | expanded: bool | — | — |
| `open` | `function` | — | — | — |
| `cancel` | `function` | — | — | — |
| `setOnSelectedIndexChanged` | `function` | callback: function | — | — |
| `setOnSelectionConfirmed` | `function` | callback: function | — | — |
| `setOnExpandedChanged` | `function` | callback: function | — | — |
| `setOpenSound` | `function` | filename: string | — | — |
| `getOpenSound` | `function` | — | return: string | Pure |
| `setCursorSound` | `function` | filename: string | — | — |
| `getCursorSound` | `function` | — | return: string | Pure |
| `setSelectSound` | `function` | filename: string | — | — |
| `getSelectSound` | `function` | — | return: string | Pure |
| `setCancelSound` | `function` | filename: string | — | — |
| `getCancelSound` | `function` | — | return: string | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onConfirm` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onCancel` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onClick` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseButtonDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | return: bool | — |
| `onMouseMoved` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseWheelScrolled` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onKeyDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## TabView

直接 metadata 基类：`{ "Engine", "ControlBase" }`、`{ "Engine", "FunctionalBase" }`

### 选择与导航

TabView 的选项数量固定且不能为零，索引从零开始并在越界时取最近的有效值，选中项始终可见。中间区域等分给各个标签页，两侧各留 16 个逻辑像素作为提示区。控件从不获取键盘焦点。声明式资产通过必填且非空的 `items: string[]` 属性确定这一固定数量，`tabCount` 不是资产属性。

`setItems(items)` 在数量相同时替换标签文字，不改变当前选择与回调；数量不符时控件保持原样。`setOnSelectedIndexChanged(callback)` 绑定选择回调，传入 `nil` 可清除。`setSelectedIndex` 真正改变选择时只调用回调，不播放音效；`selectPrevious`、`selectNext` 以及真正生效的 Q/E、LB/RB、鼠标移动或触摸点击切换，会先播放一次配置好的游标音效，再调用回调。选择未变或已到边界时两者都不发生，标签页也不会循环。

持有焦点的内容调用 `handleNavigationInput()`。TabView 可见且处于激活状态时，它会消耗 Q、E、LB 和 RB；重复的同方向输入只移动一次，相反方向则互相抵消。鼠标移动会选择中间的标签页。在该区域左键点击会被消耗但不选择，右键、中键以及两侧提示区的输入继续传递。触摸移动不会选择，但点击会。

### 按键提示

`setKeyHint(leftHint, rightHint)` 接收两个 `Engine.KeyHint` 值，其可选字段 `Keyboard` 与 `Joystick` 分别存放非 `Unknown` 的 `sf.Keyboard.Key` 和 `Engine.JoystickButton` 取值方法返回的按钮值。空提示会清除对应的一侧，缺少当前设备字段的提示则隐藏。输入无效时，两侧提示都保持原样。

```lua
tabView:setKeyHint(
    Engine.KeyHint.new({ Keyboard = sf.Keyboard.Key.Q, Joystick = Engine.JoystickButton.getLB() }),
    Engine.KeyHint.new({ Keyboard = sf.Keyboard.Key.E, Joystick = Engine.JoystickButton.getRB() })
)
```

提示只在可以移动的位置显示。连接手柄时使用 `Joystick` 提示；未连接手柄时，桌面端、HarmonyOS 2in1 和 HarmonyOS Mobile 平板使用 `Keyboard` 提示，Android、iOS 和 HarmonyOS 手机则隐藏提示。提示不会重新绑定输入，也不会禁用输入。

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `setWindowSkin` | `function` | windowSkin: sf.Image | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `getItems` | `function` | — | return: string[] | Pure |
| `setItems` | `function` | items: string[] | — | — |
| `setOnSelectedIndexChanged` | `function` | callback: function \| nil | — | — |
| `getSelectedIndex` | `function` | — | return: int | Pure |
| `setSelectedIndex` | `function` | index: int | — | — |
| `getSelectedItem` | `function` | — | return: string | Pure |
| `selectPrevious` | `function` | — | return: bool | — |
| `selectNext` | `function` | — | return: bool | — |
| `handleNavigationInput` | `function` | — | return: bool | — |
| `setCursorSound` | `function` | filename: string | — | — |
| `getCursorSound` | `function` | — | return: string | Pure |
| `setKeyHint` | `function` | leftHint: { "Engine", "KeyHint" }; rightHint: { "Engine", "KeyHint" } | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onClick` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseButtonDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | return: bool | — |
| `onMouseMoved` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## GamepadHintBar

直接 metadata 基类：`{ "Engine", "ControlBase" }`、`{ "Engine", "FunctionalBase" }`

GamepadHintBar 最多显示三条手柄提示，在自身尺寸内居中。它仅在连接手柄时绘制与响应。每条提示是一个 `Engine.GamepadHint`，包含 `Button`（`Engine.JoystickButton` getter 值）、`LongPress` 与 `Text`，`setHints` 会拒绝超过三条。默认窗口通过 `WindowBase` 把它放在底部边框。

短按提示在直径 16 逻辑像素的白色圆底上绘制当前手柄的按键图案，随后是文本。长按提示叠加一个绿色顺时针扇形，在一秒内填满。按住按键满一秒会在达标瞬间以零基下标调用一次 `setOnHintTriggered`，释放则重置进度。`setHintEnabled` 会使提示变灰、停止进度并阻止触发。

### 属性

除 `size` 与纯文本配置外未声明可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行与 metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `setTextConfig` | `function` | textConfig: { "Engine", "PlainTextConfig" } | — | — |
| `setHints` | `function` | hints: { "Engine", "GamepadHint" }[] | — | — |
| `getHintCount` | `function` | — | return: int | Pure |
| `setHintEnabled` | `function` | index: int; enabled: bool | — | — |
| `isHintEnabled` | `function` | index: int | return: bool | Pure |
| `isGamepadConnected` | `function` | — | return: bool | Pure |
| `setOnHintTriggered` | `function` | callback: function \| nil | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## SectorShape

直接 metadata 基类：`{ "sf", "Drawable" }`、`{ "sf", "Transformable" }`

SectorShape 是基础图形而非 UI 控件，因此不出现在调色板中。它从 12 点方向顺时针填充扇形，角度限制在 0–360 度。`GamepadHintBar` 与 `Button` 用它绘制长按进度。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行与 metadata |
|---|---|---|---|---|
| `new` | `function` | radius: float = 0.0; fillColour: sf.Color = 白色 | SectorShape | — |
| `getRadius` / `setRadius` | `function` | — / radius: float | float / — | Pure / — |
| `getAngle` / `setAngle` | `function` | — / angle: sf.Angle | sf.Angle / — | Pure / — |
| `getFillColour` / `setFillColour` | `function` | — / colour: sf.Color | sf.Color / — | Pure / — |
| `getLocalBounds` / `getGlobalBounds` | `function` | — | return: sf.FloatRect | Pure |

## ProgressBar

直接 metadata 基类：`{ "Engine", "ControlBase" }`

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `getProgress` | `function` | — | return: float | Pure |
| `setProgress` | `function` | progress: float | — | — |
| `getBackgroundColor` | `function` | — | return: sf.Color | Pure |
| `setBackgroundColor` | `function` | color: sf.Color | — | — |
| `getFillColor` | `function` | — | return: sf.Color | Pure |
| `setFillColor` | `function` | color: sf.Color | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## Slider

直接 metadata 基类：`{ "Engine", "ControlBase" }`、`{ "Engine", "FunctionalBase" }`

### 触摸交互

Slider 被触摸捕获时，会依据指针 X 坐标在可见范围内的位置设置数值，随后持续更新并钳制到有效区间，直到释放为止，即使指针移出命中范围也是如此。`setTouchHitBounds` 只改变触摸命中范围，不改变可见范围或鼠标命中范围。

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `setVisible` | `function` | visible: bool | — | — |
| `getSize` | `function` | — | return: sf.Vector2f | Pure |
| `resize` | `function` | size: sf.Vector2f | — | — |
| `getValue` | `function` | — | return: int | Pure |
| `setValue` | `function` | value: int | — | — |
| `setRange` | `function` | minValue: int; maxValue: int | — | — |
| `getRange` | `function` | — | minimum: int; maximum: int | Pure |
| `setValueFromRatio` | `function` | ratio: float | — | — |
| `setValueFromBoundsPosition` | `function` | bounds: sf.FloatRect; position: sf.Vector2f | — | — |
| `adjust` | `function` | delta: int | — | — |
| `getHandlePosition` | `function` | — | return: int | Pure |
| `setLineTexture` | `function` | texture: sf.Texture | — | — |
| `setHandleTexture` | `function` | texture: sf.Texture | — | — |
| `setOnValueChanged` | `function` | callback: function | — | — |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `onClick` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onMouseButtonDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | return: bool | — |
| `onMouseMoved` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `onKeyDown` | `function` | arguments: { "Engine", "UiInputEventArguments" } | — | — |
| `draw` | `function` | target: sf.RenderTarget; states: sf.RenderStates | — | — |

## AssetInstance

生成 View 通过 `.instance` 暴露这个原生对象。原生 `requireAsset` 返回 `Engine.AssetInstance`，`UiController:requireAsset` 返回生成的子 View。详见 [声明式 UI 运行时契约](<../../../04.声明式 UI/02.运行时契约.md>)。

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `getRoot` | `function` | — | return: { "Engine", "ControlBase" } | Pure |
| `requireControl` | `function` | localName: string | return: { "Engine", "ControlBase" } | Pure |
| `getNodeByName` | `function` | name: string | return: { "Engine", "ControlBase" } | Pure |
| `requireAsset` | `function` | localName: string | return: { "Engine", "AssetInstance" } | Pure |
| `setProperty` | `function` | localName: string; propertyId: string; value: Union[nil, bool, int, float, string, sf.Vector2f, sf.Vector2u, sf.IntRect, sf.Color, string[]] | — | — |
| `setText` | `function` | localName: string; text: string | — | — |
| `reflow` | `function` | logicalSize: sf.Vector2u = nil | — | — |
| `hasAnimation` | `function` | name: string; target: string = nil | return: bool | Pure |
| `playAnimation` | `function` | name: string; target: string = nil; onFinished: function = nil | return: bool | — |
| `stopAnimation` | `function` | name: string; target: string = nil | — | — |
