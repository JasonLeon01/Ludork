# Engine：玩法类型

## Actor

直接 metadata 基类：—

`collectTree()`（`metadata = false`）返回根节点在前的广度优先快照，保留子节点顺序和归属关系。出现重复项或环时报错。

`setGraph` 保存一个 `Engine.Graph` 实例，`getGraph` 会返回它。传入 nil 即清除图，此后 `getGraph` 返回 nil。

`visible` 是可编辑的布尔字段，默认值为 `true`，`getVisible()` 返回它的当前值。`isVisibleInHierarchy()` 还会检查全部祖先。Actor 不可见时仍保留实例、生命周期和已配置的 Tick 行为，但不产生绘制，不贡献附属光源与遮光，也不参与碰撞、Overlap 与寻路阻挡。渲染、交互和玩法缓存都会依据当前可见性判断。`setVisible(value, applyToChildren = true)` 默认也会设置子孙自身的标记。传入 `false` 可以保留这些标记，但父级的可见性仍然约束整棵层级。直接赋值只改变 Actor 自身的标记。

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `scriptMixin` | `bool` | `false` | Meta { BlueprintOnly = true } |
| `scriptPath` | `string` | `""` | Meta { Rely = { source = "scriptMixin", op = "==", value = true }, PathVars = "Scripts/Mixins", PathRoot = "Project", PathFilter = "*.lua", BlueprintOnly = true } |
| `tag` | `string` | `""` | — |
| `switchInterval` | `float` | `0.2` | — |
| `animatable` | `bool` | `false` | — |
| `visible` | `bool` | `true` | — |
| `material` | `{ "Engine", "Material" }` | `{  }` | — |
| `shaderPath` | `string` | `""` | Meta { PathVars = "/Game/Assets/Shaders", PathFilter = "*.frag" } |
| `hue` | `float` | `0` | Meta { ProgressVars = { [1] = 0, [2] = 360, [3] = 1 } } |
| `collisionEnabled` | `bool` | `false` | — |
| `tickable` | `bool` | `false` | — |
| `speed` | `float` | `64` | — |
| `autoSound` | `string` | `""` | Meta { PathVars = "/Game/Assets/Sounds" } |
| `autoSoundInterval` | `float` | `0` | Meta { Rely = { source = "autoSound", op = "!=", value = "" } } |
| `autoSoundParams` | `{ "Engine", "AutoSoundParams" }` | `{ volume = 100, minDistance = 64, attenuation = 1, loop = false, maxDistance = 0 }` | Meta { Rely = { source = "autoSound", op = "!=", value = "" } } |
| `texturePath` | `string` | `""` | Meta { PathVars = "/Game/Assets/Characters" } |
| `defaultRect` | `sf.IntRect` | `{ [1] = { [1] = 0, [2] = 0, [3] = 32, [4] = 32 } }` | Meta { RectRangeVars = "texturePath" } |
| `defaultTranslation` | `sf.Vector2f` | `{ [1] = 0, [2] = 0 }` | — |
| `defaultRotation` | `float` | `0` | — |
| `defaultScale` | `sf.Vector2f` | `{ [1] = 1, [2] = 1 }` | — |
| `defaultOrigin` | `sf.Vector2f` | `{ [1] = 0, [2] = 0 }` | — |
| `lightComp` | `{ "Engine", "LightComponent" }` | — | component |

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `getCollisionEnabled` | `function` | — | return: bool | Pure |
| `setCollisionEnabled` | `function` | enabled: bool | — | ExecSplit { [1] = "default", default = nil } |
| `getPathfindingBlocks` | `function` | — | return: bool | Pure |
| `setPathfindingBlocks` | `function` | blocks: bool | — | — |
| `isDestroyed` | `function` | — | return: bool | Pure |
| `markDestroyed` | `function` | destroyed: bool | — | — |
| `setShaderPath` | `function` | shaderPath: string | — | ExecSplit { [1] = "default", default = nil } |
| `getShaderPath` | `function` | — | shaderPath: string | Pure |
| `getShader` | `function` | — | shader: sf.Shader | Pure |
| `hasShaderError` | `function` | — | error: bool | Pure |
| `update` | `function` | deltaTime: float | — | — |
| `lateUpdate` | `function` | deltaTime: float | — | — |
| `fixedUpdate` | `function` | fixedDelta: float | — | — |
| `getPosition` | `function` | — | pos: sf.Vector2f | Pure |
| `getRelativePosition` | `function` | — | pos: sf.Vector2f | Pure |
| `getMapPosition` | `function` | — | pos: sf.Vector2i | Pure |
| `getOccupiedMapCells` | `function` | worldPosition: sf.Vector2f = nil | cells: sf.Vector2i[] | Pure |
| `getOccupiedMapCellsAtMapPosition` | `function` | mapPosition: sf.Vector2i | cells: sf.Vector2i[] | Pure |
| `getRelativeMapPosition` | `function` | — | pos: sf.Vector2i | Pure |
| `getLocalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `getGlobalBounds` | `function` | — | return: sf.FloatRect | Pure |
| `setPosition` | `function` | position: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `setRelativePosition` | `function` | position: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `setMapPosition` | `function` | position: sf.Vector2u | — | ExecSplit { [1] = "default", default = nil } |
| `setRelativeMapPosition` | `function` | position: sf.Vector2u | — | ExecSplit { [1] = "default", default = nil } |
| `move` | `function` | offset: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `getRotation` | `function` | — | angle: sf.Angle | Pure |
| `getRelativeRotation` | `function` | — | angle: sf.Angle | Pure |
| `setRotation` | `function` | angle: sf.Angle | — | ExecSplit { [1] = "default", default = nil } |
| `rotate` | `function` | angle: sf.Angle | — | ExecSplit { [1] = "default", default = nil } |
| `setRelativeRotation` | `function` | angle: sf.Angle | — | ExecSplit { [1] = "default", default = nil } |
| `getScale` | `function` | — | scale: sf.Vector2f | Pure |
| `getRelativeScale` | `function` | — | scale: sf.Vector2f | Pure |
| `setScale` | `function` | factors: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `scale` | `function` | factor: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `setRelativeScale` | `function` | scale: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `getOrigin` | `function` | — | origin: sf.Vector2f | Pure |
| `setOrigin` | `function` | origin: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `getTranslation` | `function` | — | translation: sf.Vector2f | Pure |
| `setTranslation` | `function` | translation: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `setAlignment` | `function` | alignment: sf.Vector2f | — | ExecSplit { [1] = "default", default = nil } |
| `getMap` | `function` | — | map_: { "Engine", "ActorMapService" } | Pure |
| `setMap` | `function` | inMap: { "Engine", "ActorMapService" } | — | ExecSplit { [1] = "default", default = nil } |
| `getParent` | `function` | — | return: { "Engine", "Actor" } | Pure |
| `setParent` | `function` | parent: { "Engine", "Actor" } | — | — |
| `getChildren` | `function` | — | return: { "Engine", "Actor[]" } | Pure |
| `addChild` | `function` | child: { "Engine", "Actor" } | — | ExecSplit { [1] = "default", default = nil } |
| `removeChild` | `function` | child: { "Engine", "Actor" } | — | — |
| `getVisible` | `function` | — | visible: bool | Pure |
| `isVisibleInHierarchy` | `function` | — | return: bool | Pure |
| `setVisible` | `function` | visible: bool = nil; applyToChildren: bool = true | — | ExecSplit { [1] = "default", default = nil } |
| `getAnimatable` | `function` | — | animatable: bool | Pure |
| `setAnimatable` | `function` | animate: bool = nil; applyToChildren: bool = true | — | ExecSplit { [1] = "default", default = nil } |
| `getSpriteTexture` | `function` | — | texture: sf.Texture | Pure |
| `getTexture` | `function` | — | texture: sf.Texture | Pure |
| `setSpriteTexture` | `function` | texture: sf.Texture = nil; resetRect: bool = false | — | ExecSplit { [1] = "default", default = nil } |
| `setTexture` | `function` | texture: sf.Texture = nil; resetRect: bool = false | — | ExecSplit { [1] = "default", default = nil } |
| `setTextureRect` | `function` | rectangle: sf.IntRect | — | — |
| `getTextureRect` | `function` | — | return: sf.IntRect | Pure |
| `getMaterial` | `function` | — | material: { "Engine", "Material" } | Pure |
| `setMaterial` | `function` | material: { "Engine", "Material" } | — | ExecSplit { [1] = "default", default = nil } |
| `getLightBlock` | `function` | — | lightBlock: float | Pure |
| `setLightBlock` | `function` | lightBlock: float | — | ExecSplit { [1] = "default", default = nil } |
| `getMirror` | `function` | — | mirror: bool | Pure |
| `setMirror` | `function` | mirror: bool | — | ExecSplit { [1] = "default", default = nil } |
| `getReflectionStrength` | `function` | — | reflectionStrength: float | Pure |
| `setReflectionStrength` | `function` | reflectionStrength: float | — | ExecSplit { [1] = "default", default = nil } |
| `getOpacity` | `function` | — | opacity: float | Pure |
| `setOpacity` | `function` | opacity: float | — | ExecSplit { [1] = "default", default = nil } |
| `getIgnoreLighting` | `function` | — | ignoreLighting: bool | Pure |
| `setIgnoreLighting` | `function` | ignoreLighting: bool | — | ExecSplit { [1] = "default", default = nil } |
| `setGraph` | `function` | graph: { "Engine", "Graph" } | — | ExecSplit { [1] = "default", default = nil } |
| `getGraph` | `function` | — | return: { "Engine", "Graph" } | Pure |
| `hasGraph` | `function` | — | return: bool | Pure |
| `getMapTag` | `function` | — | return: string | Pure |
| `setMapTag` | `function` | value: string | — | — |
| `ensureMapTag` | `function` | — | — | — |
| `onCreate` | `event` | — | — | ExecSplit { [1] = "default", default = nil } |
| `onTick` | `event` | deltaTime: float | — | ExecSplit { [1] = "default", default = nil } |
| `onLateTick` | `event` | deltaTime: float | — | ExecSplit { [1] = "default", default = nil } |
| `onFixedTick` | `event` | fixedDelta: float | — | ExecSplit { [1] = "default", default = nil } |
| `onDestroy` | `event` | — | — | ExecSplit { [1] = "default", default = nil } |
| `onWorldSleep` | `event` | — | — | ExecSplit { [1] = "default", default = nil } |
| `onWorldWake` | `event` | elapsedSeconds: float | — | ExecSplit { [1] = "default", default = nil } |
| `onCollision` | `event` | other: { "Engine", "Actor[]" } | — | ExecSplit { [1] = "default", default = nil } |
| `onOverlap` | `event` | other: { "Engine", "Actor[]" } | — | ExecSplit { [1] = "default", default = nil } |
| `destroy` | `function` | — | — | ExecSplit { [1] = "default", default = nil } |
| `MapMove` | `function` | offset: sf.Vector2i | return: bool | ExecSplit { [1] = "success", [2] = "fail", success = true, fail = false } |
| `getTickable` | `function` | — | tickable: bool | Pure |
| `setTickable` | `function` | tickable: bool = nil; applyToChildren: bool = true | — | ExecSplit { [1] = "default", default = nil } |
| `intersects` | `function` | other: { "Engine", "Actor" } | intersects: bool | Pure |
| `isMoving` | `function` | — | isMoving: bool | Pure |
| `isInRoute` | `function` | — | isInRoute: bool | Pure |
| `setRoute` | `function` | route: sf.Vector2i[] = {  } | — | Meta { MoveRouteVars = { [1] = "route" } }; ExecSplit { [1] = "default", default = nil } |
| `getRoute` | `function` | — | route: sf.Vector2i[] | Pure |
| `getMoveEnabled` | `function` | — | moveEnabled: bool | Pure |
| `setMoveEnabled` | `function` | enabled: bool | — | ExecSplit { [1] = "default", default = nil } |
| `stop` | `function` | — | — | ExecSplit { [1] = "default", default = nil } |
| `getVelocity` | `function` | — | velocity: sf.Vector2f | Pure |
| `BlueprintEvent` | `function` | object: any; objectType: any; eventName: string; keywordArguments: any = nil; onComplete: any = nil | — | — |
| `HasBlueprintEvent` | `function` | object: any; eventName: string | return: bool | — |
| `IsBlueprintEventEmpty` | `function` | object: any; eventName: string | return: bool | — |
| `GenActor` | `function` | actorModel: any; texture: any = nil; textureRect: any = nil; tag: string = nil | return: any | — |

大地图流送把 Actor 移出 `Active` 模拟时运行 `onWorldSleep`。Actor 重新变为 `Active` 时运行 `onWorldWake`，并报告休眠时长。这两个基类实现都是空的，普通地图不会派发其中任何一个事件。

## BPBase

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `BlueprintEvent` | `function` | object: any; objectType: any; eventName: string; keywordArguments: any = nil; onComplete: any = nil | — | — |
| `HasBlueprintEvent` | `function` | object: any; eventName: string | return: bool | — |
| `IsBlueprintEventEmpty` | `function` | object: any; eventName: string | return: bool | — |
| `ExecuteParentEvent` | `function` | object: any; classType: any; eventName: string; arguments: any = nil; keywordArguments: any = nil; localGraph: any = nil; onComplete: any = nil | return: bool | — |
| `ExecuteGraph` | `function` | graph: any; eventName: string; keywordArguments: any = nil; localGraph: any = nil; onComplete: any = nil | return: bool | 运行时 API；metadata = false |

`ExecuteGraph` 运行原生 `Engine.Graph` 事件，传入 nil 或非 Graph 值会引发类型错误。同一图与事件处于锁定状态，或者还有待处理的 latent 工作时，重复执行会被拒绝。

## Character

直接 metadata 基类：`{ "Engine", "Actor" }`

InvalidVars：`{ "defaultRect" }`

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `direction` | `int` | `0` | Meta { DropBox = { 0, 1, 2, 3 } } |
| `directionFix` | `bool` | `false` | — |
| `animateWithoutMoving` | `bool` | `false` | — |

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `setSpriteTexture` | `function` | texture: sf.Texture = nil; resetRect: bool = false | — | ExecSplit { [1] = "default", default = nil } |
| `setTexture` | `function` | texture: sf.Texture = nil; resetRect: bool = false | — | ExecSplit { [1] = "default", default = nil } |
| `setTextureRect` | `function` | rectangle: sf.IntRect | — | ExecSplit { [1] = "default", default = nil } |
| `MapMove` | `function` | offset: sf.Vector2i | return: bool | ExecSplit { [1] = "success", [2] = "fail", success = true, fail = false } |
| `update` | `function` | deltaTime: float | — | — |
| `GenActor` | `function` | actorModel: any; texture: any = nil; textureRect: any = nil; tag: string = nil | return: any | — |

## Component

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `onAttach` | `function` | owner: any | return: any[] | — |

## LightComponent

直接 metadata 基类：`{ "Engine", "Component" }`

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `lightColour` | `sf.Color` | `{ [1] = 255, [2] = 255, [3] = 255, [4] = 255 }` | — |
| `lightRadius` | `float` | `16` | — |
| `lightOffset` | `sf.Vector2f` | `{ [1] = 0, [2] = 0 }` | — |

`lightOffset` 以 Actor 局部包围盒的中心为基准，并随 Actor 的完整变换变化。墙面光源必须改用独立 Actor，并把该偏移放在房间一侧的火焰处。渲染器不会自行寻找可通行格。

### 函数与事件

未声明任何 Blueprint 函数或事件。
