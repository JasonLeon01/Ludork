# GlobalCore：地图与雾效

## FogController

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `applyFromMapData` | `function` | mapData: { "GlobalCore", "MapFogSettings" } | — | — |
| `clearFog` | `function` | — | — | — |
| `update` | `function` | deltaTime: float | — | — |
| `drawOverlay` | `function` | — | — | — |

`MapFogSettings` 接受一个 Lua 表：`fog` 为字符串，`fogPower`、`fogOx`、`fogOy` 和 `fogDistort` 为数值。这四个数值必须有限，且落在 float 范围内。`fog` 缺失或为 nil 时默认为 `""`，数值缺失或为 nil 时默认为 `0`。地图中的其他字段一律忽略。类型无效或数值越界会报错。`fog` 为空字符串，或 `fogPower` 为 `0` 或更小时，雾效关闭。`fogPower` 和 `fogDistort` 先向下取整，再限制在 `0..100`。`fogOx` 和 `fogOy` 决定滚动速度。`applyFromMapData` 与 `applyWorldFromMapData` 共用这份配置。

## PanoramaController

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `applyFromMapData` | `function` | mapData: { "GlobalCore", "MapPanoramaSettings" } | — | — |
| `clear` | `function` | — | — | — |
| `isActive` | `function` | — | return: bool | Pure |

`MapPanoramaSettings` 接受一个 Lua 表：`panorama` 为字符串。`panorama` 缺失或为 nil 时默认为 `""`。地图中的其他字段一律忽略。类型无效会报错。`panorama` 为空字符串时关闭底层远景。`applyFromMapData` 与 `applyWorldFromMapData` 共用这份配置。路径非空时立即加载贴图，加载失败会向上传播。

## PathResult

直接 metadata 基类：—

### 属性

| 名称 | 类型 | 默认值 | Metadata |
|---|---|---|---|
| `offsets` | `sf.Vector2i[]` | — | — |
| `points` | `sf.Vector2i[]` | — | — |
| `route` | `sf.Vector2i[]` | — | — |

### 函数与事件

未声明任何 Blueprint 函数或事件。

## GameMapBase

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `generateDataFromMap` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; size: sf.Vector2u; materialMap: any[][]; smooth: bool | return: sf.Texture | — |
| `findPathExt` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; start: sf.Vector2i; goal: sf.Vector2i; size: sf.Vector2u; movingActor: { "Engine", "Actor" }; excludedAnchors: sf.Vector2i[] = {  } | return: { "GlobalCore", "PathResult" } | — |
| `getMaterialPropertyMapExt` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; width: int; height: int; propertyName: string; invalidValue: any | return: any[][] | — |
| `rebuildPassabilityCache` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; size: sf.Vector2u | return: bool[][] | — |
| `updateActorOccupancy` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; actor: { "Engine", "Actor" } | — | — |
| `getActorsAt` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; x: int; y: int | return: { "Engine", "Actor[]" } | — |
| `getActorsInRange` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; x: int; y: int; radius: int | return: { "Engine", "Actor[]" } | — |
| `getCollisionAt` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; x: int; y: int; selfActor: { "Engine", "Actor" } | return: { "Engine", "Actor[]" } | — |
| `getOverlapsAt` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; x: int; y: int; selfActor: { "Engine", "Actor" } | return: { "Engine", "Actor[]" } | — |
| `setTilemap` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; tilemap: { "Engine", "Tilemap" } | — | — |
| `syncActorsRef` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; actors: any | — | — |
| `setHideDisconnectedRegions` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; enabled: bool | — | — |
| `setVisibilityObserver` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; position: sf.Vector2i? = nil | — | — |
| `isCellVisible` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; position: sf.Vector2i | return: bool | Pure |
| `isActorVisibleOnMap` | `function` | self: { "GlobalCore", "GameMapBase" } = "self"; actor: { "Engine", "Actor" } | return: bool | Pure |
| `getVisibilityRevision` | `function` | self: { "GlobalCore", "GameMapBase" } = "self" | return: int | Pure |

`GameMapBase` 中仅供运行时使用的方法（`metadata = false`）涵盖稀疏区域就绪状态、下标从 1 开始的 `getSparseWorldRegionIndexAt(position)`、静态灯光 Occupancy 与批量遮挡。

### 区域可见性

对于普通有限地图，`setHideDisconnectedRegions(enabled)` 开关区域隐藏。`setVisibilityObserver(position)` 接受 `sf.Vector2i` 格子坐标，供预览指定观察位置；传入 `nil` 恢复跟随玩家。`isCellVisible(position)` 查询当前格子掩码，`isActorVisibleOnMap(actor)` 还会检查 Actor 自身及祖先的可见性。`getHideDisconnectedRegions()` 是运行时查询当前开关的接口。`getVisibilityRevision()` 返回供显示与预览缓存使用的当前版本，关闭隐藏时也会跟踪地形变化。可见性查询在返回前处理待更新的变化。

地形变化后重新计算连通区域和替换源。同一区域内移动复用掩码，跨区域移动更新绘制与灯光。渲染器使用独立显示图层，保留真实 tilemap。选取规则和玩法效果见 [Global.GameMap](<../../01.Global APIs/01.GameMap.md#非连通区域隐藏>)。

## WorldStreamingState

`WorldRegionState` 暴露 `Unloaded`、`Reading`、`Prepared`、`Active` 和 `Dormant`。`WorldRegionDemand` 暴露 `None`、`Prepared` 和 `Active`。`WorldStreamingState` 使用从 1 开始的下标，持有 demand 队列，并负责 Camera 排序、last-used 与缓存核算、淘汰和统计。Camera 排序把 Active 区域排在前面，其次按区域中心的平方距离排序，距离相同时按 region index 决定先后。它的方法均为 `metadata = false`。Blueprint 使用带上下文的世界地图节点。

Lua 调用 `updateCameraCenter` 和 `updateDemand`，消费 `takeReadBatch` 和 `takePublishItem`，上报读取与发布的完成或取消，标记激活、停用和淘汰，并读取 `getEvictionList` 和 `getStats`。文件、builder 和 payload 仍由 Lua 持有。
