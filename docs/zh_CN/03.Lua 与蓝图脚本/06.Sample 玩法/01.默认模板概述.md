# 默认模板概览

Sample 既是可运行参考，也是可复用模板的来源。它展示推荐项目结构，而不是最小空白程序。它适合用来学习所有权与数据流；复制某个模式前，先弄清它的依赖。

Sample 是供取用的源素材（source material），而非隐式的引擎契约。要在另一个项目中依赖某项行为，应先确认它在 metadata、LuaLS stub 与 API 参考中属于公开行为。

## 入口与模块

`Scripts/Entry.lua` 加载 Core 模块，通过 `Source.Configs.Main` 读取或创建 `Main.ini`，初始化本地化与 latent 图、创建首个场景并进入 C++ 循环。新配置使用“中”档：渲染上限 `1`、60 FPS、抗锯齿 `2`、75% 光照分辨率。`SAVE_AS_LDC = false` 使用 `.json` 存档，`true` 使用加密 `.ldc`；它与 **Encrypt Data** 打包选项无关。Core 模块按以下顺序加载：

```lua
local Engine = require("Engine")
local GlobalCore = require("GlobalCore")
local GlobalFunctions = require("GlobalFunctions")

local Components = GlobalFunctions.Components
local UI = GlobalFunctions.UI
local NodeGraph = GlobalFunctions.NodeGraph
local Manager = GlobalFunctions.Manager
```

## 玩法布局

- `Scripts/Source` 保存可复用 Lua 类、场景、窗口、组件和节点函数。
- `Scripts/Mixins` 保存可直接挂载的 Lua 行为。
- `Data/Blueprints` 保存游戏专用 Actor 类。
- `Data/General` 提供物品、敌人、装备、职业、玩家、状态和特殊效果。
- `Data/Maps` 与 `Data/Tilesets` 提供可玩世界。
- `Core` 提供原生模块和生成绑定。

大型公开 Lua 宿主保持稳定模块文件，仅由该宿主使用的实现归入同名子目录。例如 `Global/GameMap.lua` 对应 `Global/GameMap/`、`Global/WorldGameMap.lua` 对应 `Global/WorldGameMap/`，以及 `Source/Scenes/SceneMap.lua` 对应 `Source/Scenes/SceneMap/`。这些子模块是私有实现服务，不得 require 或扩展宿主；已有独立 `.d.lua` 契约的模块仍保持独立及原有路径。

原生 `Core` 目录树遵循相同的依赖方向：绑定类的宿主 `.cpp` 拥有该类的全部成员定义，同名子目录只包含不依赖宿主的状态、算法和服务；CMake 的 `ImplBoundaryValidate` target 会检查这套源码布局约定。

## 模板变体

`Cpp` 保留原生源码并按项目构建；`Cpp-ffmpeg` 增加视频支持；Standalone 变体包含已构建好的运行时，供打包使用。iOS、HarmonyOS 与 Android 发行包只从 C++ Source 变体构建，并在启用视频时使用 C++ Source + FFmpeg 变体。修改 Sample 不会自动更新已经创建的项目，应重新生成模板或明确地把所需改动应用到该项目。

运行时 Lua、`Scripts/stub` 声明与 `_meta.lua` 文件分层规则见 [Lua 运行时与模块](../01.Lua 运行时与模块.md)。

## 相关页面

- [项目模板](../../01.快速入门/02.项目模板.md)
- [Lua 运行时与模块](../01.Lua 运行时与模块.md)
