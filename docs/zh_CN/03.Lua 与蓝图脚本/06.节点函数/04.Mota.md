# GlobalFunctions.Mota

Metadata 来源：`Scripts/GlobalFunctions/Mota_meta.lua`

`Mota` 声明怪物图鉴、区域与楼层传送节点。

## Mota

直接 metadata 基类：—

### 属性

未声明任何可编辑属性。

### 函数与事件

| 名称 | 种类 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|---|
| `OpenMonsterBook` | `function` | — | — | ExecSplit { [1] = "default", default = nil } |
| `OpenFloorTeleporter` | `function` | — | — | ExecSplit { [1] = "default", default = nil } |
| `GetCurrentRegion` | `function` | — | region: string | Pure |
| `SetCurrentRegion` | `function` | region: string | — | ExecSplit { [1] = "default", default = nil } |
| `CenterSymmetricTeleport` | `function` | — | return: int | ExecSplit { [1] = "Success", [2] = "Failed", Success = { [1] = 0 }, Failed = { [1] = 1 } } |
| `GoUpstairsSamePos` | `function` | — | return: int | ExecSplit { [1] = "Success", [2] = "Failed", Success = { [1] = 0 }, Failed = { [1] = 1 } } |
| `GoDownstairsSamePos` | `function` | — | return: int | ExecSplit { [1] = "Success", [2] = "Failed", Success = { [1] = 0 }, Failed = { [1] = 1 } } |
