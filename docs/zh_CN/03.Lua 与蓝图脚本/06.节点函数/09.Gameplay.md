# NodeFunctions.Gameplay

Metadata 来源：`Scripts/Source/NodeFunctions/Gameplay_meta.lua`

`Gameplay` 声明读取玩法事件上下文，以及施加属性、State 与事件的节点。

| 类型 | 属性 | 函数与事件 |
|---:|---:|---:|
| 1 | 0 | 16 |

## Gameplay

直接 metadata 基类：—

### 函数

| 名称 | 参数 | 返回 | 执行语义与 metadata |
|---|---|---|---|
| `GetContext` | — | `context: GameplayEventData` | Pure |
| `GetSource` | — | `source: any` | Pure |
| `GetTarget` | — | `target: any` | Pure |
| `GetEventTag` | — | `eventTag: string` | Pure |
| `GetPayload` | — | `payload: any` | Pure |
| `HasTag` | `target: Battler; tag: string` | `value: bool` | Pure |
| `GetNumericAttribute` | `target: Battler; attribute: string` | `value: any` | Pure；动态数值引脚 |
| `SetNumericAttributeBase` | `target: Battler; attribute: string; value: any` | — | 默认执行输出；动态数值引脚 |
| `ApplyAttributeDelta` | `target: Battler; attribute: string; magnitude: any` | — | 默认执行输出；动态数值引脚 |
| `ApplyState` | `target: Battler; stateID: string; stacks: int = 1` | `handle: int` | 默认执行输出；State 选择器 |
| `RemoveState` | `target: Battler; stateID: string` | — | 成功/失败分支；State 选择器 |
| `ReduceState` | `target: Battler; stateID: string; stacks: int = 1` | — | 成功/失败分支；State 选择器 |
| `RemovePlayerState` | `stateID: string` | — | 默认执行输出；State 选择器 |
| `ReducePlayerState` | `stateID: string; stacks: int = 1` | — | 默认执行输出；State 选择器 |
| `SendEvent` | `target: Battler; eventTag: string; payload: any = {}` | `results: any` | 默认执行输出 |
| `ApplyEffect` | `target: Battler; effect: any; stacks: int = 1; sourceKey: any` | `handle: int` | 默认执行输出 |

上下文读取节点与绑定上下文的变更节点，都要求通用数据图的 parent 为 `GameplayEventData`。在普通 Actor 图中使用它们会报错。`RemovePlayerState` 与 `ReducePlayerState` 作用于该上下文的 target；State 不存在时，两者仍只有单一执行输出。

动态 attribute pin 的类型为 `any`，但 Ability System 仍要求它取所选 schema 类型下的有限数值。`SetNumericAttributeBase` 写入 Base。`ApplyAttributeDelta` 应用 Instant Add modifier。

`SendEvent` 同步保留 instigator，并以传入的 Battler 为 target。`ApplyEffect` 接受 `GameplayEffect`。Instant Effect 不返回持久 handle，也不能授予 tag 或 ability。

## 相关页面

- [GlobalCore Gameplay API](<../08.全局与 Core 模块/01.Global APIs/02.Gameplay.md>)
- [Source Gameplay API](<../07.源码类/07.Gameplay.md>)
