# 01. Actor

源文件：`Sample/Engine/Gameplay/Actors/Actor.py`

## 类 `Engine.Gameplay.Actors.Actor.Actor`

具备碰撞、移动和蓝图事件支持的游戏 Actor。在 `_ActorBase` 基础上扩展了基于格子的移动、碰撞检测、路线（寻路）执行，以及蓝图事件分发。

**类变量**

- `collisionEnabled`: 类型 `bool`; 默认 `False`
- `tickable`: 类型 `bool`; 默认 `False`
- `speed`: 类型 `float`; 默认 `64.0`
- `autoSound`: 类型 `str`; 默认 `''` — `Assets/Sounds` 下自动播放的声音素材，会作为空间音频播放。
- `autoSoundInterval`: 类型 `float`; 默认 `0.0` — 一次自动音频播放结束后，到下一次开始播放前的等待秒数。仅在 `autoSound` 非空时可编辑。
- `autoSoundParams`: 类型 `AutoSoundParams`; 默认 `AutoSoundParams()` — 嵌套的空间音频参数。仅在 `autoSound` 非空时可编辑。
- `texturePath`: 类型 `str`; 默认 `''`
- `defaultRect`: 类型 `Optional[Tuple[Pair[int], Pair[int]]]`; 默认 `((0, 0), (32, 32))`
- `defaultTranslation`: 类型 `Pair[float]`; 默认 `(0.0, 0.0)` — x/y 数值编辑器（`@Meta(Vector2fVars)`）。
- `defaultRotation`: 类型 `float`; 默认 `0.0`
- `defaultScale`: 类型 `Pair[float]`; 默认 `(1.0, 1.0)` — x/y 数值编辑器（`@Meta(Vector2fVars)`）。
- `defaultOrigin`: 类型 `Pair[float]`; 默认 `(0.0, 0.0)` — x/y 数值编辑器（`@Meta(Vector2fVars)`）。
- `lightColour`: 类型 `Tuple[int, int, int, int]`; 默认 `(255, 255, 255, 255)` — 自发光颜色（RGBA）。在蓝图编辑器中显示为颜色方块。写入时会创建或更新 `lightComp`。
- `lightRadius`: 类型 `float`; 默认 `16.0` — 自发光半径（像素）。写入时会创建或更新 `lightComp`。

**自动空间音频**

设置 `autoSound` 后，Actor 会重复播放该声音，并将其作为空间音频处理。播放从 Actor 当前世界坐标开始；播放中的 `Sound` 对象会在每帧更新位置，使声音跟随 Actor。`autoSoundInterval` 指的是上一轮播放结束之后，到下一轮播放开始之前的间隔，不是从上一轮开始播放时计时。

Actor 自动音频固定使用 `spatial=True` 和 `relativeToListener=False`，因此声音位置使用世界坐标。`autoSoundParams` 是一个较小的嵌套 dataclass：

- `volume`: 类型 `float`; 默认 `100.0` — 应用全局音效音量前的基础音量。
- `minDistance`: 类型 `float`; 默认 `64.0` — 声音保持最大音量的距离。
- `attenuation`: 类型 `float`; 默认 `1.0` — 距离衰减系数。

地图会在每帧根据当前 player 更新 SFML audio listener。listener 的位置跟随 player 的世界坐标；若 player 是 `Character`，listener 的方向也会跟随 player 当前朝向。

**组件字段**

蓝图数据可为 Actor 附加可选组件。它们在编辑器中作为嵌套字段编辑，不属于节点函数 API。

- `lightComp`（`LightComponent`）— 自发光设置。
  - `lightColour`: 类型 `Tuple[int, int, int, int]`; 默认 `(255, 255, 255, 255)` — 颜色方块编辑器（`@Meta(ColourVars)`）。
  - `lightRadius`: 类型 `float`; 默认 `16.0`
旧蓝图字段 `bSelfLight` 在加载数据时会迁移为 `lightComp`。

**蓝图 API**

#### `onCreate() -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onCreate`

蓝图事件：Actor 生成到场景时调用一次。

**事件：** 蓝图事件入口点。

---

#### `onTick(deltaTime: float) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onTick`

蓝图事件：`tickable` 为 `True` 时每帧调用一次。

**参数**
- `deltaTime` – 上一帧至今的经过时间（秒）。

**事件：** 蓝图事件入口点。

---

#### `onLateTick(deltaTime: float) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onLateTick`

蓝图事件：同帧所有 Actor 的 `onTick` 执行完毕后调用。

**参数**
- `deltaTime` – 上一帧至今的经过时间（秒）。

**事件：** 蓝图事件入口点。

---

#### `onFixedTick(fixedDelta: float) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onFixedTick`

蓝图事件：以固定物理步长调用。

**参数**
- `fixedDelta` – 固定步长时长（秒）。

**事件：** 蓝图事件入口点。

---

#### `onDestroy() -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onDestroy`

蓝图事件：Actor 从场景移除时调用。

**事件：** 蓝图事件入口点。

---

#### `onCollision(other: List[Actor]) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onCollision`

蓝图事件：移动被另一个 Actor 阻挡时调用。

**参数**
- `other` – 碰撞目标格子上的 Actor 列表。

**事件：** 蓝图事件入口点。

---

#### `onOverlap(other: List[Actor]) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.onOverlap`

蓝图事件：此 Actor 进入已被其他 Actor 占据的格子时调用。

**参数**
- `other` – 共享同一格子的 Actor 列表。

**事件：** 蓝图事件入口点。

---

#### `destroy() -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.destroy`

从当前地图移除此 Actor 并触发 `onDestroy`。

**执行出口：** `default`

---

#### `isDestroyed() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.isDestroyed`

检查此 Actor 是否已被销毁。

**输出：** `destroyed: bool`

---

#### `MapMove(offset: Union[Vector2i, Pair[int], List[int]]) -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.MapMove`

将 Actor 向给定方向移动一个格子。移动前验证边界和可通行性；若目标格子被阻挡，则对双方触发 `onCollision`。

**参数**
- `offset` – 方向向量（每轴截断为单位值：-1、0 或 1）。

**执行出口：** `success`、`fail`

---

#### `getTickable() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.getTickable`

检查 Tick 事件是否已启用。

**输出：** `tickable: bool`

---

#### `setTickable(tickable: bool, applyToChildren: bool = True) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.setTickable`

启用或禁用 Tick 事件分发。可选地将该设置传播给所有子 Actor。

**参数**
- `tickable` – 是否启用 Tick 事件。
- `applyToChildren` – 若为 `True`，则传播给所有子 Actor。

**执行出口：** `default`

---

#### `getVisible() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.getVisible`

检查此 Actor 是否可见。

**输出：** `visible: bool`

---

#### `setVisible(visible: bool, applyToChildren: bool = True) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.setVisible`

设置 Actor 可见性。可选地将该设置传播给所有子 Actor。

**参数**
- `visible` – Actor 是否应当可见。
- `applyToChildren` – 若为 `True`，则传播给所有子 Actor。

**执行出口：** `default`

---

#### `getCollisionEnabled() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.getCollisionEnabled`

检查此 Actor 是否阻挡其他 Actor 的移动。

**输出：** `collisionEnabled: bool`

---

#### `setCollisionEnabled(enabled: bool) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.setCollisionEnabled`

启用或禁用碰撞阻挡。

**参数**
- `enabled` – 是否启用碰撞阻挡。

**执行出口：** `default`

---

#### `intersects(other: Actor) -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.intersects`

测试此 Actor 的包围盒是否与另一个 Actor 重叠。

**参数**
- `other` – 要测试的另一个 Actor。

**输出：** `intersects: bool`

---

#### `isMoving() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.isMoving`

检查此 Actor 当前是否正在移动。

**输出：** `isMoving: bool`

---

#### `isInRoute() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.isInRoute`

检查此 Actor 当前是否正在执行移动路线。

**输出：** `isInRoute: bool`

---

#### `setRoute(route: List[Vector2i] = []) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.setRoute`

设置一组自动行走的格子偏移序列。

**参数**
- `route` – 格子偏移列表。默认 `[]`；空列表表示清除路线。

**执行出口：** `default`

---

#### `getRoute() -> List[Vector2i]`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.getRoute`

获取当前移动路线。无活跃路线时返回空列表。

**输出：** `route: List[Vector2i]`

---

#### `getMoveEnabled() -> bool`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.getMoveEnabled`

检查移动是否被允许。

**输出：** `moveEnabled: bool`

---

#### `setMoveEnabled(enabled: bool) -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.setMoveEnabled`

启用或禁用移动。禁用时会同时停止当前的移动。

**参数**
- `enabled` – 是否允许移动。

**执行出口：** `default`

---

#### `stop() -> None`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.stop`

立即停止所有移动并清除当前路线。重置运动状态，并将地图坐标对齐至当前位置。

**执行出口：** `default`

---

#### `getVelocity() -> Optional[Vector2f]`

原始路径：`Engine.Gameplay.Actors.Actor.Actor.getVelocity`

计算当前速度向量。考虑当前地图格子的材质速度系数。未移动时返回 `None`。

**输出：** `velocity: Optional[Vector2f]`
