# 01. Container

源文件：`Sample/Source/NodeFunctions/Container.py`

#### `DictGet(dict_: Dict, key: Any) -> Any`

原始路径：`Source.NodeFunctions.Container.DictGet`

按 key 获取字典中的 value。

**输出：** `value: object`

---

#### `DictAdd(dict_: Dict, key: Any, value: Any) -> None`

原始路径：`Source.NodeFunctions.Container.DictAdd`

按 key 写入字典的 value。

**执行出口：** `default`

---

#### `DictRemove(dict_: Dict, key: Any) -> None`

原始路径：`Source.NodeFunctions.Container.DictRemove`

移除字典中的指定 key。

**执行出口：** `default`

---

#### `DictClear(dict_: Dict) -> None`

原始路径：`Source.NodeFunctions.Container.DictClear`

清空字典的所有内容。

**执行出口：** `default`

---

#### `DictContains(dict_: Dict, key: Any) -> bool`

原始路径：`Source.NodeFunctions.Container.DictContains`

判断字典是否包含指定 key。

**输出：** `value: bool`

---

#### `ListGet(list_: List[Any], index: int = 0) -> Any`

原始路径：`Source.NodeFunctions.Container.ListGet`

按索引获取列表元素。

**输出：** `value: object`

---

#### `ListAppend(list_: List[Any], value: Any) -> None`

原始路径：`Source.NodeFunctions.Container.ListAppend`

向列表尾部追加一个元素。

**执行出口：** `default`

---

#### `ListRemove(list_: List[Any], index: int = 0) -> None`

原始路径：`Source.NodeFunctions.Container.ListRemove`

按索引移除列表元素。

**执行出口：** `default`

---

#### `ListFind(list_: List[Any], value: Any) -> int`

原始路径：`Source.NodeFunctions.Container.ListFind`

查找元素在列表中的索引。

**输出：** `index: int`

---

#### `ListClear(list_: List[Any]) -> None`

原始路径：`Source.NodeFunctions.Container.ListClear`

清空列表的所有元素。

**执行出口：** `default`

---

#### `ListContains(list_: List[Any], value: Any) -> bool`

原始路径：`Source.NodeFunctions.Container.ListContains`

判断列表是否包含指定元素。

**输出：** `value: bool`
