# 05. Player

源文件：`Sample/Source/NodeFunctions/Player.py`

#### `GetPlayer() -> Optional[Player]`

原始路径：`Source.NodeFunctions.Player.GetPlayer`

获取当前场景的主角实例。

**输出：** `player: Player`

---

#### `GetPlayerFrontPosition() -> Optional[Vector2i]`

原始路径：`Source.NodeFunctions.Player.GetPlayerFrontPosition`

根据玩家当前朝向获取面前一格的地图坐标。

**输出：** `position: Vector2i`

---

#### `AddItem(itemID: str, count: int) -> None`

原始路径：`Source.NodeFunctions.Player.AddItem`

将道具添加到玩家背包。

**参数**
- `itemID` – 要添加的道具 ID。
- `count` – 添加数量。

**执行出口：** `default`

---

#### `RemoveItem(itemID: str, count: int) -> int`

原始路径：`Source.NodeFunctions.Player.RemoveItem`

从玩家背包移除道具，数量不足则失败。

**参数**
- `itemID` – 要移除的道具 ID。
- `count` – 移除数量。

**执行出口：** `Success`, `Failed`

---

#### `HasItem(itemID: str) -> bool`

原始路径：`Source.NodeFunctions.Player.HasItem`

检查玩家是否持有至少一个指定道具。

**参数**
- `itemID` – 要检查的道具 ID。

**输出：** `value: bool`

---

#### `GetItemCount(itemID: str) -> int`

原始路径：`Source.NodeFunctions.Player.GetItemCount`

获取玩家背包中指定道具的数量。

**参数**
- `itemID` – 要查询的道具 ID。

**输出：** `count: int`

---

#### `AddEquip(equipID: str, count: int = 1) -> None`

原始路径：`Source.NodeFunctions.Player.AddEquip`

将装备添加到玩家装备包。

**参数**
- `equipID` – 要添加的装备 ID。
- `count` – 添加数量（默认 `1`）。

**执行出口：** `default`

---

#### `RemoveEquip(equipID: str, count: int) -> int`

原始路径：`Source.NodeFunctions.Player.RemoveEquip`

从玩家装备包移除装备，数量不足则失败。

**参数**
- `equipID` – 要移除的装备 ID。
- `count` – 移除数量。

**执行出口：** `Success`, `Failed`

---

#### `HasEquip(equipID: str) -> bool`

原始路径：`Source.NodeFunctions.Player.HasEquip`

检查玩家是否持有至少一个指定装备。

**参数**
- `equipID` – 要检查的装备 ID。

**输出：** `value: bool`

---

#### `EquipItem(equipID: str) -> None`

原始路径：`Source.NodeFunctions.Player.EquipItem`

将装备穿戴到玩家身上。

**参数**
- `equipID` – 要装备的装备 ID。

**执行出口：** `default`

---

#### `UnequipSlot(slotID: str) -> None`

原始路径：`Source.NodeFunctions.Player.UnequipSlot`

卸下指定装备栏中的装备。

**参数**
- `slotID` – 要卸下的装备栏 ID。

**执行出口：** `default`

---

#### `GetEquipInSlot(slotID: str) -> str`

原始路径：`Source.NodeFunctions.Player.GetEquipInSlot`

获取指定装备栏中当前装备的 ID，空栏返回空字符串。

**参数**
- `slotID` – 要查询的装备栏 ID。

**输出：** `equipID: str`

---

#### `GetPlayerAttr(attrName: str) -> Any`

原始路径：`Source.NodeFunctions.Player.GetPlayerAttr`

获取玩家指定属性值（如 `HP`、`MAXHP`、`ATK`、`DEF`、`GOLD`、`EXP`、`LEVEL`）。

**参数**
- `attrName` – 要读取的属性名。

**输出：** `value: object`

---

#### `SetPlayerAttr(attrName: str, value: Any) -> None`

原始路径：`Source.NodeFunctions.Player.SetPlayerAttr`

设置玩家指定属性值（如 `HP`、`MAXHP`、`ATK`、`DEF`、`GOLD`、`EXP`、`LEVEL`）。

**参数**
- `attrName` – 要写入的属性名。
- `value` – 新属性值。

**执行出口：** `default`

---

#### `HealPlayer(amount: int) -> None`

原始路径：`Source.NodeFunctions.Player.HealPlayer`

恢复玩家生命值，上限为最大生命值。

**参数**
- `amount` – 恢复量。

**执行出口：** `default`

---

#### `DamagePlayer(amount: int) -> None`

原始路径：`Source.NodeFunctions.Player.DamagePlayer`

减少玩家生命值，下限为 `0`。

**参数**
- `amount` – 伤害量。

**执行出口：** `default`

---

#### `AddGold(amount: int) -> None`

原始路径：`Source.NodeFunctions.Player.AddGold`

为玩家增加（或减少）金币。

**参数**
- `amount` – 增加量（负值为减少）。

**执行出口：** `default`

---

#### `AddEXP(amount: int) -> None`

原始路径：`Source.NodeFunctions.Player.AddEXP`

为玩家增加经验值。

**参数**
- `amount` – 经验增加量。

**执行出口：** `default`

---

#### `MeetPlayer(actors: List[Actor]) -> Optional[Player]`

原始路径：`Source.NodeFunctions.Player.MeetPlayer`

检查给定 Actor 列表中是否有玩家并返回该玩家。

**参数**
- `actors` – 要检查的 Actor 列表。

**输出：** `playerInfo: Player`
