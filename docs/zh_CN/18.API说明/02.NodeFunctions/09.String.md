# 09. String

源文件：`Sample/Source/NodeFunctions/String.py`

#### `ToString(value: Any) -> str`

原始路径：`Source.NodeFunctions.String.ToString`

将任意值转换为字符串。

**参数**
- `value` – 要转换的值。

**输出：** `value: str`

---

#### `GetIntFromStr(value: str) -> int`

原始路径：`Source.NodeFunctions.String.GetIntFromStr`

将字符串解析为整数。

**参数**
- `value` – 要解析的字符串。

**输出：** `value: int`

---

#### `GetFloatFromStr(value: str) -> float`

原始路径：`Source.NodeFunctions.String.GetFloatFromStr`

将字符串解析为浮点数。

**参数**
- `value` – 要解析的字符串。

**输出：** `value: float`

---

#### `StringConcat(str1: str, str2: str) -> str`

原始路径：`Source.NodeFunctions.String.StringConcat`

拼接两个字符串。

**参数**
- `str1` – 第一个字符串。
- `str2` – 要拼接的第二个字符串。

**输出：** `value: str`

---

#### `StringContains(str1: str, str2: str) -> bool`

原始路径：`Source.NodeFunctions.String.StringContains`

判断字符串是否包含子串。

**参数**
- `str1` – 要搜索的字符串。
- `str2` – 要查找的子串。

**输出：** `value: bool`

---

#### `StringLength(str1: str) -> int`

原始路径：`Source.NodeFunctions.String.StringLength`

获取字符串长度。

**参数**
- `str1` – 要测量的字符串。

**输出：** `value: int`

---

#### `StringFind(str1: str, str2: str) -> Optional[int]`

原始路径：`Source.NodeFunctions.String.StringFind`

查找子串首次出现的位置。

**参数**
- `str1` – 要搜索的字符串。
- `str2` – 要查找的子串。

**输出：** `value: Optional[int]`

---

#### `StringReplace(str1: str, str2: str, str3: str) -> str`

原始路径：`Source.NodeFunctions.String.StringReplace`

将子串替换为新字符串。

**参数**
- `str1` – 原始字符串。
- `str2` – 要替换的子串。
- `str3` – 替换后的新字符串。

**输出：** `value: str`

---

#### `StringSplit(str1: str, str2: str) -> List[str]`

原始路径：`Source.NodeFunctions.String.StringSplit`

按分隔符拆分字符串。

**参数**
- `str1` – 要拆分的字符串。
- `str2` – 分隔符。

**输出：** `value: List[str]`

---

#### `StringSubstring(str1: str, start: int, end: int) -> str`

原始路径：`Source.NodeFunctions.String.StringSubstring`

按起止索引截取子串。

**参数**
- `str1` – 源字符串。
- `start` – 起始索引（含）。
- `end` – 结束索引（不含）。

**输出：** `value: str`

---

#### `StringToLower(str1: str) -> str`

原始路径：`Source.NodeFunctions.String.StringToLower`

将字符串转换为小写。

**参数**
- `str1` – 要转换的字符串。

**输出：** `value: str`

---

#### `StringToUpper(str1: str) -> str`

原始路径：`Source.NodeFunctions.String.StringToUpper`

将字符串转换为大写。

**参数**
- `str1` – 要转换的字符串。

**输出：** `value: str`

---

#### `StringStrip(str1: str) -> str`

原始路径：`Source.NodeFunctions.String.StringStrip`

移除字符串首尾空白字符。

**参数**
- `str1` – 要处理的字符串。

**输出：** `value: str`
