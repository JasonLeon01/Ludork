# 02. Math

源文件：`Sample/Source/NodeFunctions/Math.py`

#### `BuildVector2f(x: float = 0.0, y: float = 0.0) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.BuildVector2f`

从 x、y 构建 `Vector2f`。

**参数**
- `x` – X 分量。
- `y` – Y 分量。

**输出：** `value: Vector2f`

---

#### `BuildVector2i(x: int = 0, y: int = 0) -> Vector2i`

原始路径：`Source.NodeFunctions.Math.BuildVector2i`

从 x、y 构建 `Vector2i`。

**参数**
- `x` – X 分量。
- `y` – Y 分量。

**输出：** `value: Vector2i`

---

#### `BuildVector2u(x: int = 0, y: int = 0) -> Vector2u`

原始路径：`Source.NodeFunctions.Math.BuildVector2u`

从 x、y 构建 `Vector2u`。

**参数**
- `x` – X 分量。
- `y` – Y 分量。

**输出：** `value: Vector2u`

---

#### `BuildVector3f(x: float = 0.0, y: float = 0.0, z: float = 0.0) -> Vector3f`

原始路径：`Source.NodeFunctions.Math.BuildVector3f`

从 x、y、z 构建 `Vector3f`。

**参数**
- `x` – X 分量。
- `y` – Y 分量。
- `z` – Z 分量。

**输出：** `value: Vector3f`

---

#### `BuildVector3i(x: int = 0, y: int = 0, z: int = 0) -> Vector3i`

原始路径：`Source.NodeFunctions.Math.BuildVector3i`

从 x、y、z 构建 `Vector3i`。

**参数**
- `x` – X 分量。
- `y` – Y 分量。
- `z` – Z 分量。

**输出：** `value: Vector3i`

---

#### `IsNearZero(num: Union[int, float], epsilon: float = 0.1) -> bool`

原始路径：`Source.NodeFunctions.Math.IsNearZero`

判断数值是否接近 `0`（epsilon）。

**参数**
- `num` – 要检测的数值。
- `epsilon` – 容差阈值（默认 `0.1`）。

**输出：** `value: bool`

---

#### `IsVector2NearZero(v: Vector2f, epsilon: float = 0.1) -> bool`

原始路径：`Source.NodeFunctions.Math.IsVector2NearZero`

判断 `Vector2` 是否接近零向量（epsilon）。

**参数**
- `v` – 要检测的向量。
- `epsilon` – 容差阈值（默认 `0.1`）。

**输出：** `value: bool`

---

#### `IsVector3NearZero(v: Vector3f, epsilon: float = 0.1) -> bool`

原始路径：`Source.NodeFunctions.Math.IsVector3NearZero`

判断 `Vector3` 是否接近零向量（epsilon）。

**参数**
- `v` – 要检测的向量。
- `epsilon` – 容差阈值（默认 `0.1`）。

**输出：** `value: bool`

---

#### `Vector2fRound(v: Vector2f) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.Vector2fRound`

对 `Vector2` 的各分量进行四舍五入。

**参数**
- `v` – 要处理的向量。

**输出：** `value: Vector2f`

---

#### `Vector2fFloor(v: Vector2f) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.Vector2fFloor`

对 `Vector2` 的各分量向下取整。

**参数**
- `v` – 要处理的向量。

**输出：** `value: Vector2f`

---

#### `Vector2fCeil(v: Vector2f) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.Vector2fCeil`

对 `Vector2` 的各分量向上取整。

**参数**
- `v` – 要处理的向量。

**输出：** `value: Vector2f`

---

#### `ToVector2f(v: Union[Vector2i, Vector2u]) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.ToVector2f`

将 `Vector2i` 或 `Vector2u` 转换为 `Vector2f`。

**参数**
- `v` – 要转换的向量。

**输出：** `value: Vector2f`

---

#### `ToVector2i(v: Union[Vector2f, Vector2u]) -> Vector2i`

原始路径：`Source.NodeFunctions.Math.ToVector2i`

将 `Vector2f` 或 `Vector2u` 转换为 `Vector2i`。

**参数**
- `v` – 要转换的向量。

**输出：** `value: Vector2i`

---

#### `ToVector2u(v: Union[Vector2f, Vector2i]) -> Vector2u`

原始路径：`Source.NodeFunctions.Math.ToVector2u`

将 `Vector2f` 或 `Vector2i` 转换为 `Vector2u`。

**参数**
- `v` – 要转换的向量。

**输出：** `value: Vector2u`

---

#### `ToVector3f(v: Vector3i) -> Vector3f`

原始路径：`Source.NodeFunctions.Math.ToVector3f`

将 `Vector3i` 转换为 `Vector3f`。

**参数**
- `v` – 要转换的向量。

**输出：** `value: Vector3f`

---

#### `ToVector3i(v: Vector3f) -> Vector3i`

原始路径：`Source.NodeFunctions.Math.ToVector3i`

将 `Vector3f` 转换为 `Vector3i`。

**参数**
- `v` – 要转换的向量。

**输出：** `value: Vector3i`

---

#### `ToIntRect(x: int = 0, y: int = 0, width: int = 32, height: int = 32) -> IntRect`

原始路径：`Source.NodeFunctions.Math.ToIntRect`

从 x、y、width、height 构建 `IntRect`。

**参数**
- `x` – 左坐标。
- `y` – 上坐标。
- `width` – 宽度。
- `height` – 高度。

**输出：** `value: IntRect`

---

#### `ToFloatRect(x: float = 0.0, y: float = 0.0, width: float = 32.0, height: float = 32.0) -> FloatRect`

原始路径：`Source.NodeFunctions.Math.ToFloatRect`

从 x、y、width、height 构建 `FloatRect`。

**参数**
- `x` – 左坐标。
- `y` – 上坐标。
- `width` – 宽度。
- `height` – 高度。

**输出：** `value: FloatRect`

---

#### `Clamp(value=0.0, min_val=0.0, max_val=1.0) -> float`

原始路径：`Source.NodeFunctions.Math.Clamp`

将值限制在最小值与最大值之间。

**参数**
- `value` – 要限制的值。
- `min_val` – 最小边界。
- `max_val` – 最大边界。

**输出：** `value: float`

---

#### `Lerp(a: float = 0.0, b: float = 1.0, t: float = 0.5) -> float`

原始路径：`Source.NodeFunctions.Math.Lerp`

在 `a` 与 `b` 之间进行线性插值，插值因子为 `t`。

**参数**
- `a` – 起始值。
- `b` – 终止值。
- `t` – 插值因子（0.0–1.0）。

**输出：** `value: float`

---

#### `Abs(value: Union[int, float] = 0) -> Union[int, float]`

原始路径：`Source.NodeFunctions.Math.Abs`

返回数值的绝对值。

**参数**
- `value` – 输入数值。

**输出：** `value: Union[int, float]`

---

#### `ToInt(value: Union[int, float] = 0) -> int`

原始路径：`Source.NodeFunctions.Math.ToInt`

将数值转换为整数。

**参数**
- `value` – 要转换的数值。

**输出：** `value: Union[int, float]`

---

#### `ToFloat(value: Union[int, float] = 0) -> float`

原始路径：`Source.NodeFunctions.Math.ToFloat`

将数值转换为浮点数。

**参数**
- `value` – 要转换的数值。

**输出：** `value: Union[int, float]`

---

#### `Max(values: List[Any] = None) -> Any`

原始路径：`Source.NodeFunctions.Math.Max`

返回列表中的最大值。

**参数**
- `values` – 要比较的值列表。

**输出：** `value: List[Any]`

---

#### `Min(values: List[Any] = None) -> Any`

原始路径：`Source.NodeFunctions.Math.Min`

返回列表中的最小值。

**参数**
- `values` – 要比较的值列表。

**输出：** `value: List[Any]`

---

#### `Sqrt(value: Union[int, float] = 0) -> Union[int, float]`

原始路径：`Source.NodeFunctions.Math.Sqrt`

计算平方根。

**参数**
- `value` – 输入数值。

**输出：** `value: Union[int, float]`

---

#### `Pow(base: Union[int, float] = 1, exp: Union[int, float] = 2) -> Union[int, float]`

原始路径：`Source.NodeFunctions.Math.Pow`

计算 `base` 的 `exp` 次幂。

**参数**
- `base` – 底数。
- `exp` – 指数。

**输出：** `value: Union[int, float]`

---

#### `Vector2Distance(v1: Union[Vector2f, Vector2i, Vector2u], v2: Union[Vector2f, Vector2i, Vector2u]) -> float`

原始路径：`Source.NodeFunctions.Math.Vector2Distance`

计算两个二维向量之间的距离。

**参数**
- `v1` – 第一个向量。
- `v2` – 第二个向量。

**输出：** `value: float`

---

#### `Vector3Distance(v1: Vector3f, v2: Vector3f) -> float`

原始路径：`Source.NodeFunctions.Math.Vector3Distance`

计算两个三维向量之间的距离。

**参数**
- `v1` – 第一个向量。
- `v2` – 第二个向量。

**输出：** `value: float`

---

#### `Vector2Dot(v1: Union[Vector2f, Vector2i, Vector2u], v2: Union[Vector2f, Vector2i, Vector2u]) -> float`

原始路径：`Source.NodeFunctions.Math.Vector2Dot`

计算二维向量点积。

**参数**
- `v1` – 第一个向量。
- `v2` – 第二个向量。

**输出：** `value: float`

---

#### `Vector3Dot(v1: Union[Vector3f, Vector3i], v2: Union[Vector3f, Vector3i]) -> float`

原始路径：`Source.NodeFunctions.Math.Vector3Dot`

计算三维向量点积。

**参数**
- `v1` – 第一个向量。
- `v2` – 第二个向量。

**输出：** `value: float`

---

#### `Vector2Cross(v1: Union[Vector2f, Vector2i, Vector2u], v2: Union[Vector2f, Vector2i, Vector2u]) -> Union[float, int]`

原始路径：`Source.NodeFunctions.Math.Vector2Cross`

计算二维向量叉积。

**参数**
- `v1` – 第一个向量。
- `v2` – 第二个向量。

**输出：** `value: Union[float, int]`

---

#### `Vector3Cross(v1: Union[Vector3f, Vector3i], v2: Union[Vector3f, Vector3i]) -> Union[Vector3f, Vector3i]`

原始路径：`Source.NodeFunctions.Math.Vector3Cross`

计算三维向量叉积。

**参数**
- `v1` – 第一个向量。
- `v2` – 第二个向量。

**输出：** `value: Union[Vector3f, Vector3i]`

---

#### `Vector2Length(v: Vector2f) -> float`

原始路径：`Source.NodeFunctions.Math.Vector2Length`

计算二维向量长度。

**参数**
- `v` – 输入向量。

**输出：** `value: float`

---

#### `Vector3Length(v: Vector3f) -> float`

原始路径：`Source.NodeFunctions.Math.Vector3Length`

计算三维向量长度。

**参数**
- `v` – 输入向量。

**输出：** `value: float`

---

#### `Vector2LengthSquared(v: Union[Vector2f, Vector2i, Vector2u]) -> Union[float, int]`

原始路径：`Source.NodeFunctions.Math.Vector2LengthSquared`

计算二维向量长度的平方。

**参数**
- `v` – 输入向量。

**输出：** `value: Union[float, int]`

---

#### `Vector3LengthSquared(v: Union[Vector3f, Vector3i]) -> Union[float, int]`

原始路径：`Source.NodeFunctions.Math.Vector3LengthSquared`

计算三维向量长度的平方。

**参数**
- `v` – 输入向量。

**输出：** `value: Union[float, int]`

---

#### `Vector2Normalized(v: Vector2f) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.Vector2Normalized`

将二维向量归一化。

**参数**
- `v` – 要归一化的向量。

**输出：** `value: Vector2f`

---

#### `Vector3Normalized(v: Vector3f) -> Vector3f`

原始路径：`Source.NodeFunctions.Math.Vector3Normalized`

将三维向量归一化。

**参数**
- `v` – 要归一化的向量。

**输出：** `value: Vector3f`

---

#### `GetAngle(v: Vector2f) -> Angle`

原始路径：`Source.NodeFunctions.Math.GetAngle`

获取二维向量的角度。

**参数**
- `v` – 输入向量。

**输出：** `value: Angle`

---

#### `GetAngleTo(v1: Vector2f, v2: Vector2f) -> Angle`

原始路径：`Source.NodeFunctions.Math.GetAngleTo`

获取从 `v1` 指向 `v2` 的角度。

**参数**
- `v1` – 起始向量。
- `v2` – 目标向量。

**输出：** `value: Angle`

---

#### `AsDegrees(angle: Angle) -> float`

原始路径：`Source.NodeFunctions.Math.AsDegrees`

将角度对象转换为度数值。

**参数**
- `angle` – 要转换的角度。

**输出：** `value: float`

---

#### `AsRadians(angle: Angle) -> float`

原始路径：`Source.NodeFunctions.Math.AsRadians`

将角度对象转换为弧度值。

**参数**
- `angle` – 要转换的角度。

**输出：** `value: float`

---

#### `Vector2ComponentWiseDiv(v: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i], div: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]) -> Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]`

原始路径：`Source.NodeFunctions.Math.Vector2ComponentWiseDiv`

对向量的每个分量分别相除。

**参数**
- `v` – 被除向量。
- `div` – 除数向量。

**输出：** `value: Union[Vector2f, Vector2i, Vector2u]`

---

#### `Vector2ComponentWiseMul(v: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i], mul: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]) -> Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]`

原始路径：`Source.NodeFunctions.Math.Vector2ComponentWiseMul`

对向量的每个分量分别相乘。

**参数**
- `v` – 第一个向量。
- `mul` – 第二个向量。

**输出：** `value: Union[Vector2f, Vector2i, Vector2u, Vector3f, Vector3i]`

---

#### `Vector2Perpendicular(v: Union[Vector2f, Vector2i, Vector2u]) -> Union[Vector2f, Vector2i, Vector2u]`

原始路径：`Source.NodeFunctions.Math.Vector2Perpendicular`

获取与二维向量垂直的向量。

**参数**
- `v` – 输入向量。

**输出：** `value: Union[Vector2f, Vector2i, Vector2u]`

---

#### `Vector2ProjectedOnto(v: Vector2f, axis: Vector2f) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.Vector2ProjectedOnto`

将二维向量投影到指定轴向量上。

**参数**
- `v` – 要投影的向量。
- `axis` – 目标轴向量。

**输出：** `value: Vector2f`

---

#### `Vector2RotatedBy(v: Vector2f, phi: Angle) -> Vector2f`

原始路径：`Source.NodeFunctions.Math.Vector2RotatedBy`

按指定角度旋转二维向量。

**参数**
- `v` – 要旋转的向量。
- `phi` – 旋转角度。

**输出：** `value: Vector2f`

---

#### `DegreesToAngle(degrees_: float = 0.0) -> Angle`

原始路径：`Source.NodeFunctions.Math.DegreesToAngle`

将度数值转换为 `Angle`。

**参数**
- `degrees_` – 角度值（度）。

**输出：** `value: Angle`

---

#### `RadiansToAngle(radians_: float = 0.0) -> Angle`

原始路径：`Source.NodeFunctions.Math.RadiansToAngle`

将弧度值转换为 `Angle`。

**参数**
- `radians_` – 角度值（弧度）。

**输出：** `value: float`

---

#### `RandomInt(min_val: int = 0, max_val: int = 100) -> int`

原始路径：`Source.NodeFunctions.Math.RandomInt`

生成指定范围内的随机整数。

**参数**
- `min_val` – 最小值（含）。
- `max_val` – 最大值（含）。

**输出：** `value: int`

---

#### `RandomFloat(min_val: float = 0.0, max_val: float = 1.0) -> float`

原始路径：`Source.NodeFunctions.Math.RandomFloat`

生成指定范围内的随机浮点数。

**参数**
- `min_val` – 最小值（含）。
- `max_val` – 最大值（含）。

**输出：** `value: float`

---

#### `GCD(a: int = 1, b: int = 1) -> int`

原始路径：`Source.NodeFunctions.Math.GCD`

计算最大公约数。

**参数**
- `a` – 第一个整数。
- `b` – 第二个整数。

**输出：** `value: int`

---

#### `LCM(a: int = 1, b: int = 1) -> int`

原始路径：`Source.NodeFunctions.Math.LCM`

计算最小公倍数。

**参数**
- `a` – 第一个整数。
- `b` – 第二个整数。

**输出：** `value: int`

---

#### `ADD(a: Any = 0, b: Any = 0) -> Any`

原始路径：`Source.NodeFunctions.Math.ADD`

计算 `a + b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: Any`

---

#### `SUB(a: Any = 0, b: Any = 0) -> Any`

原始路径：`Source.NodeFunctions.Math.SUB`

计算 `a - b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: Any`

---

#### `MUL(a: Any = 1, b: Any = 1) -> Any`

原始路径：`Source.NodeFunctions.Math.MUL`

计算 `a * b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: Any`

---

#### `DIV(a: Any = 1, b: Any = 1) -> Any`

原始路径：`Source.NodeFunctions.Math.DIV`

计算 `a / b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: Any`

---

#### `MOD(a: Any = 0, b: Any = 1) -> Any`

原始路径：`Source.NodeFunctions.Math.MOD`

计算 `a % b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: Any`

---

#### `POW(a: Any = 1, b: Any = 1) -> Any`

原始路径：`Source.NodeFunctions.Math.POW`

计算 `a ** b`。

**参数**
- `a` – 底数。
- `b` – 指数。

**输出：** `value: Any`

---

#### `EQUALS(a: Any = 0, b: Any = 0) -> bool`

原始路径：`Source.NodeFunctions.Math.EQUALS`

判断 `a` 是否等于 `b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: bool`

---

#### `NOT_EQUALS(a: Any = 0, b: Any = 0) -> bool`

原始路径：`Source.NodeFunctions.Math.NOT_EQUALS`

判断 `a` 是否不等于 `b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: bool`

---

#### `LESS(a: Any = 0, b: Any = 0) -> bool`

原始路径：`Source.NodeFunctions.Math.LESS`

判断 `a` 是否小于 `b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: bool`

---

#### `LESS_EQUALS(a: Any = 0, b: Any = 0) -> bool`

原始路径：`Source.NodeFunctions.Math.LESS_EQUALS`

判断 `a` 是否小于等于 `b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: bool`

---

#### `GREATER(a: Any = 0, b: Any = 0) -> bool`

原始路径：`Source.NodeFunctions.Math.GREATER`

判断 `a` 是否大于 `b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: bool`

---

#### `GREATER_EQUALS(a: Any = 0, b: Any = 0) -> bool`

原始路径：`Source.NodeFunctions.Math.GREATER_EQUALS`

判断 `a` 是否大于等于 `b`。

**参数**
- `a` – 左操作数。
- `b` – 右操作数。

**输出：** `value: bool`

---

#### `AND(a: bool = False, b: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.AND`

布尔与运算。

**参数**
- `a` – 第一个操作数。
- `b` – 第二个操作数。

**输出：** `value: bool`

---

#### `OR(a: bool = False, b: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.OR`

布尔或运算。

**参数**
- `a` – 第一个操作数。
- `b` – 第二个操作数。

**输出：** `value: bool`

---

#### `NOT(a: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.NOT`

布尔非运算。

**参数**
- `a` – 操作数。

**输出：** `value: bool`

---

#### `XOR(a: bool = False, b: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.XOR`

布尔异或运算。

**参数**
- `a` – 第一个操作数。
- `b` – 第二个操作数。

**输出：** `value: bool`

---

#### `NAND(a: bool = False, b: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.NAND`

布尔与非运算。

**参数**
- `a` – 第一个操作数。
- `b` – 第二个操作数。

**输出：** `value: bool`

---

#### `NOR(a: bool = False, b: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.NOR`

布尔或非运算。

**参数**
- `a` – 第一个操作数。
- `b` – 第二个操作数。

**输出：** `value: bool`

---

#### `XNOR(a: bool = False, b: bool = False) -> bool`

原始路径：`Source.NodeFunctions.Math.XNOR`

布尔同或运算。

**参数**
- `a` – 第一个操作数。
- `b` – 第二个操作数。

**输出：** `value: bool`

---

#### `IADD(a: Any, b: Any = 1) -> Any`

原始路径：`Source.NodeFunctions.Math.IADD`

对引用/变量执行 `+=` 运算。

**参数**
- `a` – 目标引用。
- `b` – 要加的值。

**执行出口：** `default`

---

#### `ISUB(a: Any, b: Any = 1) -> Any`

原始路径：`Source.NodeFunctions.Math.ISUB`

对引用/变量执行 `-=` 运算。

**参数**
- `a` – 目标引用。
- `b` – 要减的值。

**执行出口：** `default`

---

#### `IMUL(a: Any, b: Any = 2) -> Any`

原始路径：`Source.NodeFunctions.Math.IMUL`

对引用/变量执行 `*=` 运算。

**参数**
- `a` – 目标引用。
- `b` – 乘数。

**执行出口：** `default`

---

#### `IDIV(a: Any, b: Any = 2) -> Any`

原始路径：`Source.NodeFunctions.Math.IDIV`

对引用/变量执行 `/=` 运算。

**参数**
- `a` – 目标引用。
- `b` – 除数。

**执行出口：** `default`

---

#### `IMOD(a: Any, b: Any = 2) -> Any`

原始路径：`Source.NodeFunctions.Math.IMOD`

对引用/变量执行 `%=` 运算。

**参数**
- `a` – 目标引用。
- `b` – 模数。

**执行出口：** `default`

---

#### `IPOW(a: Any, b: Any = 2) -> Any`

原始路径：`Source.NodeFunctions.Math.IPOW`

对引用/变量执行 `**=` 运算。

**参数**
- `a` – 目标引用。
- `b` – 指数。

**执行出口：** `default`
