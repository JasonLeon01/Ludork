-- Generated by ScriptTools ui-assets generate. Do not edit.
local UiView = require("Source.UIBase.UiView")
local ChildView1 = require("Source.UI.Parts.WindowEquip.EquipStatusRow")

---@class Source.UI.Parts.WindowEquip.WindowEquipStatus
local View = {}

View.assetKey = "Parts/WindowEquip/WindowEquipStatus"
View.designSize = sf.Vector2f.new(224, 320)

function View:init(instance)
    UiView.init(self, instance)
    local controls = {
        ChangeList = assert(self.instance:requireControl("ChangeList")),
        Description = assert(self.instance:requireControl("Description")),
        ItemName = assert(self.instance:requireControl("ItemName")),
        Root = assert(self.instance:requireControl("Root"))
    }
    ---@cast controls Source.UIBase.GeneratedControls.Parts.WindowEquip.WindowEquipStatus
    self.controls = controls
    local assets = {
        StatusPlaceholder1 = self:own(ChildView1.new(assert(self.instance:requireAsset("StatusPlaceholder1")))),
        StatusPlaceholder2 = self:own(ChildView1.new(assert(self.instance:requireAsset("StatusPlaceholder2")))),
        StatusPlaceholder3 = self:own(ChildView1.new(assert(self.instance:requireAsset("StatusPlaceholder3"))))
    }
    ---@cast assets Source.UIBase.GeneratedAssets.Parts.WindowEquip.WindowEquipStatus
    self.assets = assets
end

return class(View, UiView)
